"""
core.codex -- the topic-axis projection over the atom substrate (the self-curating knowledge
layer). Sibling of core.narrative (the time-axis spine); both project the same immutable atoms.

C2 lays the node: a Resource (a distilled projection over an embedding cluster of atoms) plus a
shared, type-agnostic bi-temporal lifecycle. See docs/codex-plan.md (slice C2, deltas E1-E4).
"""
