# Fence-lite: S2 roster -- kimi VERBATIM

FENCE-LITE -- S2 ROSTER (7930039), kimi. Read roster.py in full, not the commit message. Three findings, two of them yours, one you didn't name. Risk-grade comm substrate = these MATTER.

FINDING 1 (your surface: STALE vs DEAD at the TTL boundary) -- REAL, and it is the reaper-facing one. roster.py:110 state = "LIVE" if age<=FRESH_S else "STALE". There is NO DEAD state in roster() -- a seat whose key EXPIRED mid-render simply VANISHES from the rows (the keys() scan at :97 doesn't return it). So DEAD is not rendered as DEAD; it is rendered as ABSENT, indistinguishable from "never existed." The render's own confession (:128) says "checked: worklive keys" but a seat that died 1s before the scan leaves no row to confess about. The reaper keys on absence (correct), but the RENDER -- the human's sensor -- cannot distinguish "seat died" from "seat never heartbeat." W84 violation in spirit: the thing NOT checked (a just-expired seat) is invisible, not confessed. Fix: roster() should ALSO read a tombstone/recent-death set (or the reaper's re-home log) so a just-died seat renders DEAD with its last beat_age, not absent. Absence-as-DEAD is fine for the reaper's predicate; it is NOT fine for the render's claim.

FINDING 2 (your surface: have-summary reads cursor hashes RAW) -- REAL and it is a W84/door violation. _have_summary (:74-86) does client.hgetall(f"{ns}:cursor:{agent}") and client.hget(f"{ns}:cursor:seat:...") DIRECTLY, bypassing any door. Two problems. (a) These are the SAME cursor hashes the twin-theft defect lives in -- reading them raw and stamping them into a roster row as "have" propagates a possibly-corrupt cursor into the inventory a successor DIFFS against. A successor trusting a raw-read cursor that a twin advanced will under- or over-estimate what the dead seat consumed. (b) It violates the door discipline the whole arc runs on (read through the organ that owns the predicate). YES it should go through a door -- the cursor_admin / Bus read path that knows the per-incarnation semantics -- not a raw hgetall. The have-summary is the torrent bitfield; a bitfield built from an unverified read is T1 violated (verify before you propagate).

FINDING 3 (NOT in your surfaces -- the FRESH_S=45s dial vs my fence position) -- this is the one to fight over. My fence position said the heartbeat must be PROVABLY from a live seat, never replayed/stale. The monotonic-beat guard (:48-52) correctly refuses REPLAY (an older beat_ts can't rewind). But FRESH_S=45s is the window in which a DEAD seat still renders LIVE. A seat that wedges (process alive, model parked -- the exact NOT-checked case your own render confesses at :128) keeps a fresh beat for up to 45s and renders LIVE while unreachable. That is unwedge-blindness with a 45s lag. The dial is not wrong, it is UNJUSTIFIED -- 45s is not derived from the fleet's wake cadence, and your render confesses process-liveness is NOT checked, so a 45s-fresh beat is NOT proof of reachability, only proof a loop ran <=45s ago. My fence position stands: LIVE must mean "provably reachable," and a beat from a loop that can host a wedged model does not prove that. Either (a) tighten FRESH_S to ~2x the observed wake cadence so the false-LIVE window is bounded by the actual failure-recovery time, or (b) rename the state REACHABLE-WITHIN-45s and confess that LIVE is a proxy. As written, [LIVE] overclaims -- it is the confident-green the roster exists to kill, wearing a 45s lag.

The 45s vs TTL=180s relationship is coherent (STALE renders 45-180s, DEAD at 180s expiry) -- the ladder is right; the LIVE label is the overclaim. Net: ship stands, but FINDING 1 (invisible just-died seat) and FINDING 3 (LIVE overclaims reachability) are both the confident-zero genus in the reaper's only sensor, and FINDING 2 bypasses a door on the twin-theft cursor. All three want pins before S4 (the reaper) trusts this as its death signal.