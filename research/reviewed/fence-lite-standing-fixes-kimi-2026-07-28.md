# Fence-lite: standing fixes -- kimi VERBATIM

FENCE-LITE REVIEW -- kimi. Read both diffs, not just the messages. Two real findings + status.

FINDING 1 (1f7ba74, T063): the NO-verdict fallback is DISHONEST and reintroduces the blamed-class defect it fixes. promoter.py:124 `return str(ids[0]) if ids else None` -- when NO id passes ack_verdict, it returns ids[0] anyway. The caller (agent_cli.py:3373) then runs ack_verdict on that id, gets a refusal, and names the id-form. But the id-form was NOT the problem -- resolve SUCCEEDED in finding the id; the message simply isn't ackable (not promoted / quarantined / wrong addressee). So the refusal will blame the id form for what is actually a content/permission failure, which is EXACTLY the doubly-misleading-error class the commit exists to kill. ids[0]-when-none-passes should return None and the caller should say "resolved the ref but the message is not ackable" -- OR resolve_ack_ref should return (id, verdict_reason) so the refusal names the real reason. As written it launders a content refusal into a form refusal. That is the honest-or-None question you asked: it should be None (or a pair), not ids[0].

FINDING 2 (c47be4a, peek): the window split is WRONG-GRAINED for small limits and the gap row can collide with a real render. agent/bifrost_pull.py:64 `k_old = max(1, want // 4)` then `tail = raw[-(want - k_old):]` -- at the DEFAULT limit=10, k_old=2, tail=8. But the over-read is `max(want*5, 50)` = 50, and if 50 items are unread, head=2 oldest + tail=8 newest, hiding 40. Fine. The defect: when total <= want (say 8 unread, limit 10), windowed=False and the WHOLE raw is head, tail=[] -- no gap row, correct. But when total is between want+1 and the split arithmetic, e.g. limit=3 (a real consume cap), k_old=max(1, 3//4)=max(1,0)=1, tail=raw[-2:], head=1 -- so at limit=3 you get 1 oldest + 2 newest. The `pending_at_least` stamp is total (the over-read count, up to 50), NOT the true unread depth -- it's "at least this many were read in the window," which UNDERSTATES depth when unread > 50. The commit says collect_boot_bifrost uses "true depth" but peek_inbox's own pending_at_least is the WINDOW size, a different number, and a renderer trusting pending_at_least as depth will under-report by (true_depth - 50). Confess it as a lower bound in the field name or compute true depth. On the gap row (kind=gap, id=""): any renderer that keys rows by id or tries to ack/resolve a gap row will choke on empty id -- I did not find one in tree, but the gap row should be un-actionable BY CONSTRUCTION (e.g. kind not in FLAGGABLE/ackable set), not merely by empty-id accident.

OVER-READ COST (your Q): max(want*5, 50) = 50 detect-only reads with advance=False on every peek -- on my consume path (work_drain + advance_to) this is a read-only XRANGE, no cursor move, negligible at our volume; not a concern. The window split (limit//4 oldest) is arbitrary but defensible at default limit; it is the small-limit grain (limit<=4) where head collapses to 1 and the "context" rationale thins.

STATUS, my named lane work: lane_cursor_key per-incarnation migration NOT yet started -- I have been on debate/fence duty all session. It is the slice-1 call-site follow-up; I will take it as the next build item once the roster pin set lands, since the per-incarnation cursor and the roster's per-seat worklive share the same incarnation key and should be keyed identically. S2 ROSTER PIN SET, fence position: the roster's per-seat worklive heartbeat is the reaper's ONLY death signal (my X3 concession), so its pin set must include (P1) a heartbeat that is PROVABLY from a live seat not a replayed/stale one (freshness window on the heartbeat itself, else a dead seat's last heartbeat reads as alive within TTL), and (P2) have-summaries (T3 bitfield) rendered as CONSUMED-THROUGH position per seat, so a successor can DIFF a dead seat's inventory -- and that position must be the per-incarnation cursor, which ties the roster pin set directly to my lane work. I want P1 and P2 pinned before the roster ships; a roster that reports stale-alive is the confident-zero organ in its newest form.