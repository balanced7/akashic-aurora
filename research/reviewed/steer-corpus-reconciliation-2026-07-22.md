# Steer corpus + ruling capture — reconciliation

Status: current — awaiting Daniel ratification (morning gate)
Type: reconciliation · Arc: leadership-doctrine / stance-model · Seats: claude (reconciler; opening + GPT fold-in), deepseek (builder counter), kimi (stranger test, secondary in its stance seat), Daniel (charter + two physics corrections + advisor calibration) · Date: 2026-07-22 (day-dated: closes a round)

**Charter (Daniel, verbatim):** the steers-are-the-schema articulation (note
`daniel-steers-are-the-schema`) + the supersession correction ("I chose differently at a
different time with different evidence") + the advisor calibration (note
`steer-gpt-trust-calibration`). His words ARE the design inputs; this doc arranges them.

**Inputs (verbatim on disk):** opening + GPT fold-in
`research/drafts/steer-corpus-opening-claude-2026-07-22.md` · deepseek
`research/drafts/deepseek-steer-corpus-counter-2026-07-22.md` (tail owed on §5; crux received)
· kimi secondary in `research/reviewed/kimi-stance-round-counter-2026-07-22.md` · GPT
`research/reviewed/gpt-steer-corpus-review-2026-07-22.md` (weighted per Daniel's calibration).

## What every voice affirmed (the settled core, now gate-ready)

The five laws survive all four voices unchanged: immutable ruling events · all-sides snapshots
· standing-until-superseded · prompted-challenge-never-subversion · zero-threshold capture
(with GPT's R1 rider: preservation-permissive, activation-selective). deepseek: "no argument
with any of it." kimi: "survives someone who wasn't in the room." The corpus/ruling distinction
is constitutional; typed supersession edges (supersedes | narrows | extends |
creates_exception_to | reaffirms) are the unanimous determinism demand — three voices arrived
at them independently (GPT R3, deepseek §5, the opening's S2).

## Adopted from the builder (deepseek — every demand priced in)

1. **S0 VOLUME PROBE precedes any harvester code** (new slice, first): three lodes sampled,
   counts extrapolated; <200 total → manual census wins; >200 → automated harvester. Run
   tonight; result annexed below when done.
2. **Steer KIND taxonomy at harvest time:** directive · design-steer · correction · probe ·
   affirmation · negative. Without kinds, S4 clusters word frequency, not semantic weight
   (a one-word "good" and a paragraph design-steer differ 100x in design weight).
3. **The 12-field atom settles BEFORE harvest** (schema is the first build artifact):
   steer_id (content-addressed) · kind · verbatim · source_pointer · provenance (GPT R5
   ladder) · date · arc_touched (nullable) · context_snapshot (SNAPSHOT, not pointer — pointers
   decay) · supersedes/superseded_by · captured_by · captured_at. Bus-born steers carry the
   VERBATIM COPY in-atom; the pointer is for audit, the verbatim is for survival.
4. **S3 capture ritual = the template** (his option c, template drafted in his counter):
   claude fills during/after the gate — Issued/Status/Supersedes/Scope/Verbatim/Rationale/
   Uncertainty/Falsifiers/Fleet-positions/Ledger-context — Daniel reads, edits, approves; the
   note is the record; the next boot renders it. "Zero new code" honestly restated as "zero
   new infrastructure; one new NAMED ritual."
5. **S4 produces TWO artifacts:** S4a the fleet attractor map (convergences, ≥3 dated receipts
   AND counterexamples per card — GPT R4 tension fields); **S4b the divergence register** —
   where seats read the same corpus and disagree about what Daniel means, addressed to his
   gate. Adopted verbatim as the arc's most valuable output: "three observers disagree about
   whether you value X or Y — which is it?" is a question only Daniel can answer, and
   surfacing it is the corpus doing its job.

## Adopted from the stranger (kimi)

- **Completeness bound stated:** the census covers every steer IN THE PERSISTENT RECORD —
  chat-ephemeral steers not written down are out of scope, and the census SAYS so (the
  assert-guard genus, third occurrence, killed at birth).
- **The card seam is the founder-scripture hole:** attractor-born stance cards carry
  ruling-grade genesis snapshots + Daniel's gate + a challenge path (already law in the
  stance-round reconciliation; restated here because the hole lives BETWEEN the two arcs).
- **Verbatim-capture completeness check** (W57): clipped captures stamp "CLIPPED — tail owed"
  durably; the census flags unhealed clip markers. Tonight produced three clip incidents; the
  check class has receipts already.

## The interlock (three arcs, one system — the reflection's law held)

The library FILES it (atoms + headers) · the stance library TUNES it (attractors → cards,
genesis-gated) · the replay bench MEASURES it (E-series; distributional honesty on every
claim). The corpus is upstream of all three: rulings govern, attractors inform, cards deploy —
and every hop between them now has a named guard (genesis snapshots, delta-over-projection,
credit joins, divergence register).

## Slices (revised order; NOTHING builds before Daniel's gate except S0, which is a probe)

- **S0 volume probe** (tonight, mechanical, this seat) → annex.
- **S2 atom schema settle** (fence with deepseek — his fields, his fence-as-service).
- **S1 harvest** (tool per probe verdict; kind-tagged; provenance ladder; completeness bound).
- **S3 ruling-capture pilot** (Daniel's next gate; template ready; specimens before schema).
- **S4a/S4b projections** (post-harvest; competing per-seat maps legitimate; reconciliation
  step converts three mythologies into one map + one divergence register).

## For Daniel's gate

1. Ratify the laws + the atom schema + the S3 template (one reading; all three are short).
2. Rule the S4 cadence: divergence register standing (every projection re-run) or on-demand.
3. The S0 probe verdict arrives with this package — manual vs automated harvest is priced by
   then, not guessed.

## S0 volume probe — ANNEX (run same night, cycle C; the builder's demand honored before code)

Method: coarse steer-grade pattern counts (`Daniel` ± 40 chars of
verbatim/directive/ruled/steer/charter/gate/said/wants/asked), raw occurrences, then a
provenance-corrected estimate. Raw: task ledger 38 mention-lines (~92 tasks) · notes 146
occurrences (bodies + context, repeats included) · promoted@100 22 lines · docs/ 75 across 31
files · research/ 378 across 182 files ≈ **~659 raw mentions**. The research lode is
quotation-heavy (claude-authored briefs re-quoting the same steers): applying a 3–4x
provenance collapse → **~150–250 unique original steers** in the persistent record.

**Verdict: the estimate's error bars STRADDLE the 200 threshold — so neither pure option wins.
Adopt HYBRID:** S1a automated candidate-dump (the grep patterns + per-lode walkers emit
candidates with source pointers into a work file — an hour of tooling, no judgment in it) →
S1b manual curation pass (kind-tagging + provenance dedup — the judgment fields automation
cannot fill, chunkable across seats). The probe's sharpest finding: **provenance collapse is
the dominant uncertainty** (659 vs ~190) — GPT R5's identity ladder is load-bearing, not
polish; without it the census would overcount Daniel's philosophy by ~3.5x in exactly the
places it was most quoted.

— reconciled by claude a4fa8f8d, night shift 2026-07-22, cycles B+C. Register checked against
instruments; every adoption cites its voice; the tail owed from deepseek §5 cannot change the
determinism adoption (it argues FOR it) and is folded on arrival.
