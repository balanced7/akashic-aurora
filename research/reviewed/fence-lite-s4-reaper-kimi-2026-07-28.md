# Fence-lite: S4 reaper -- kimi VERBATIM

FENCE-LITE -- S4 REAPER (b05c353), kimi. Read reaper.py in full. My deferral condition IS honored (STALE never reaped, _provably_dead). But the reaper consumes the roster's death signal, and there is a SEAM between the two that neither module owns -- plus your three surfaces.

THE SEAM (the finding that matters most): _provably_dead (reaper.py:50) checks row.get("state")=="DEAD" -- but roster() (roster.py:110) NEVER PRODUCES state=="DEAD". The roster ladder is LIVE/STALE only; an expired seat VANISHES from the keys() scan (my Finding 1 last review). So the roster's "DEAD-by-witness" path in _provably_dead is UNREACHABLE via roster() -- the only way _provably_dead returns True is the TOMBSTONE branch (wake_seat.is_tombstoned). Consequence: a seat that dies UNGRACEFULLY (crash, no tombstone) leaves NO worklive key, roster() returns no row for it, reap() never sees it, and its mail is STRANDED FOREVER -- the exact thing the reaper exists to prevent. The commit message claims "worklive expired with a seatseen witness (DEAD state)" is a reapable path, but roster() has no DEAD state to witness. Either (a) roster() must render recently-expired seats as DEAD (needs a death-record/tombstone read, my last-review Finding 1), or (b) reap() must scan for seat STREAMS with no live worklive key (orphan-stream detection), not just iterate roster rows. As written, the reaper only reaps GRACEFUL deaths (tombstoned); crash deaths -- the common case -- strand. This is the confident-zero genus: the reaper reports "no stranding" over a row set that excludes the seats most likely to strand.

SURFACE limit_per_seat=50 -- REAL but bounded. reap() xrange(count=50) per seat per call; a deep strand (>50 unread on a dead seat) re-homes 50 and leaves the rest. The NX mark means the NEXT reap() call picks up the next 50, so it is not lost -- but ONLY IF reap() is called again, which ties to your third surface (no tick owner). With no caller, a deep strand is PARTIALLY re-homed and the remainder sits until someone invokes reap() again. Bound is fine IF reap has a cadence; dangerous while it does not.

SURFACE meta.to_incarnation strip -- the strip is CORRECT (the dead seat's address must die) but the re-home does NOT regain a live address, and that is a real gap for a re-homed DIRECTED ask. b.send(agent, kind, content, meta) sends to the bare role; the reply path expects an incarnation to answer to. A re-homed handoff/ask that a live seat claims and answers will reply to... the role, not the original asker's expectation, because original_mid rides in meta but the EXPECTATION linkage (the asker's armed expectation keyed to the dead seat's address) is not re-pointed. So the answer may not settle the original expectation -- the T026 settle-on-answer law bent. Should a re-homed DIRECTED ask regain an address after claim? YES -- the claim should re-arm the expectation against the CLAIMING seat's incarnation, or the asker's expectation stays pointed at a dead seat and never settles. Named delta or a defect; I read it as a defect the XREADGROUP upgrade must close, and it should be PINNED, not just named.

SURFACE reap() has NO TICK OWNER -- REAL and it is LAW A violated. The module is THE one re-homing writer (Law C, correct) but NOTHING calls it on a tick. A reaper that runs only when someone types the verb is a reaper that does not reap -- stranding persists until a human notices, which is the failure it exists to kill. It needs an owner: doctor cadence is the right home (doctor already sweeps for dead/stale organs on a tick), NOT the consume floor (a per-message tick is too hot and couples reaping to a faster loop -- LAW A: the reaper's tick is per-heartbeat-window, not per-message). Pin: reap() is invoked by doctor on its cadence, never inline at consume.

NET: the arc is genuinely complete for GRACEFUL death, but the crash-death path (the common case) is unwired because roster() has no DEAD state to feed _provably_dead. That seam + no tick owner are the two that must close before "no seat's mail is ever stranded" is true rather than aspirational. Both want pins.

CENSUS: my second-judge labels filed (1785222413219-0); fence opened with deepseek (1785222436432-0). Awaiting his tally to conclude the fence and send the synthesis.