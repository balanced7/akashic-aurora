# T342 evidence sweep C (dead modules 145-216, alphabetical) — Explore subagent, 2026-08-18
# Contract: CAPABILITY from source, LIVE verdict with file:symbol evidence or honest NO/UNKNOWN.
# Preserved verbatim from the agent's return; judgment deferred to synthesis.

fast_agent_comm.py | CAPABILITY: FastAgentComm singleton over a Redis Stream (agent_comm:stream) with a consumer group — send_direct/send_broadcast, send_request+respond_to correlation-id round trips, XREADGROUP receive, pending count, XTRIM cleanup | LIVE: YES E:\AI-Setup\core\comm\bus.py:Bus (send/broadcast/send_reply/_emit over Redis streams, per-agent inbox + broadcast keys)
gemini_bridge.py | CAPABILITY: attaches to an already-running browser on debug port 9222 (raw Chrome DevTools Protocol, plus Selenium debuggerAddress variants), types a prompt into gemini.google.com's role=textbox and scrapes the model response — API-key-free Gemini querying | LIVE: YES E:\AI-Setup\scripts\gemini_web.py:ask_web_message/_ask_gemini_chat (Playwright/Patchright against gemini.google.com with a persisted profile), exposed as E:\AI-Setup\ai_setup_mcp.py:ask_gemini_web
gemini_bridge_monitor.py | CAPABILITY: Selenium watchdog that connects to port 9222, finds-or-creates the Gemini tab, verifies readyState/input presence, queries with a timeout, and on failure grabs a screenshot through vision_engine.get_screen_context_for_analyst to distinguish a crash from a slow page | LIVE: YES E:\AI-Setup\scripts\gemini_web.py:_looks_logged_out (browser-session health gate before the ask); the vision-feedback crash-diagnosis half has no live equivalent
harness_enforcer.py | CAPABILITY: EscapeDetector+HarnessEnforcer singleton that journals every agent action to a state file and blocks/warns on 10 named "escape conditions" (skip reprime, skip verify, skip logging, skip KB search, skip testing, impatient exit, session-id mismatch, phase-skip) at pre-action, phase-transition and session-close, emitting a compliance report | LIVE: NO — live policy is only two vetoes (E:\AI-Setup\agent\harness\guards.py:git_veto/lock_veto); no EscapeCondition, compliance_report or action-journal enforcement anywhere in the live tree
infrastructure/__init__.py | CAPABILITY: package docstring declaring the Redis/Docker/WSL orchestration plan plus a single re-export of check_infrastructure_health | LIVE: YES E:\AI-Setup\core\infrastructure\__init__.py (identical docstring and re-export)
infrastructure/health_check.py | CAPABILITY: StartupDiagnostics records per-phase startup success+duration_ms, sums total startup time, derives slow-phase/failure recommendations, prints and persists a JSON report to session_logs; StartupTimer context manager wraps a phase; check_infrastructure_health() returns the status dict | LIVE: YES E:\AI-Setup\core\infrastructure\health_check.py:StartupDiagnostics / check_infrastructure_health (same class, methods and line layout)
init_ai.py | CAPABILITY: reads Redis KB status, walks every registered model's learnings, lists stored docs, and prints a hardcoded system-state summary as a pre-work context brief | LIVE: YES E:\AI-Setup\agent_cli.py:cmd_boot and E:\AI-Setup\agent\initializer.py:derive_agent_context_from_startup_sources
init_session.py | CAPABILITY: 6-step session bootstrap that calls session_manager.check_and_reprime with the logger's SESSION_ID/SESSION_UNIQUE, flags a new session as needing re-prime, and reports each subsystem's result | LIVE: YES E:\AI-Setup\agent_cli.py:cmd_boot plus E:\AI-Setup\agent\harness\hooks\claude_sessionstart.py (session-start identity + autoboot whisper)
keepalive_ollama.py | CAPABILITY: 30s loop that TCP-probes port 11434, checks/restarts the Ollama container inside WSL Ubuntu-24.04, re-adds the netsh v4tov4 portproxy to the current WSL IP, and pings /api/tags | LIVE: NO — no 11434 restart, container control or portproxy code live; E:\AI-Setup\core\fleet\model_roster.py:probe_availability only reads availability and never starts or repairs anything
knowledge_base.py | CAPABILITY: KnowledgeBase over a pooled Redis — per-model learning registry (kb:model:*, kb:learning:* with optional TTL), markdown doc store (kb:docs), global context keys, keyword search, dual-write into a vector store with vector_search, sync_to_vector_store and backup() | LIVE: YES E:\AI-Setup\core\foundation\store.py:Store/RedisStore/HybridStore (the KV substrate), E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning/search_learnings_by_keyword (the learning half), E:\AI-Setup\core\primitives\embedder.py:Embedder (the vector half)
launch.py | CAPABILITY: interactive menu launcher — waits for Redis, prints agents from the registry, runs role initialization, then spawns the opencode binary in a new console as single agent / generator+analyst pair / custom role / helper request | LIVE: YES E:\AI-Setup\core\comm\launcher.py:Launcher.launch (registry-driven agent spawn with lifecycle tracking); the opencode-specific menu and role-pair options are absent
launch_brave_remote.py | CAPABILITY: starts brave.exe with --remote-debugging-port=9222 from a hardcoded install path and confirms the debug endpoint answers before returning | LIVE: YES E:\AI-Setup\scripts\gemini_web.py:_launch_context (brings up the automatable browser context with a persistent profile)
launch_dashboard.py | CAPABILITY: full local-stack bring-up — starts Docker Desktop, the Redis container, resolves the WSL IP and installs the Ollama netsh portproxy, starts Ollama, launches the Streamlit dashboard on 8501, opens the browser and a privileged terminal, and arms the keepalive | LIVE: NO — no streamlit, docker, WSL or portproxy code anywhere in the live tree
launch_opencode_crash_safe.py | CAPABILITY: reuses-or-creates a Redis session key, mirrors every launch action to both Redis and a session_logs file for crash recovery, prints a primed intro from recovered state, then spawns opencode and confirms it via launch_verifier | LIVE: YES E:\AI-Setup\core\comm\launcher.py:Launcher.launch/_classify_exit (spawn + exit-reason classification) and E:\AI-Setup\core\state\session_checkpoint.py:SessionState (crash-recovery state)
launch_opencode_primed.py | CAPABILITY: tkinter GUI launcher that pulls learning keys from Redis and dashboard status over HTTP, greets the user by name in a scrolled text pane, logs to ~/launcher_primed.log, and spawns opencode from a button | LIVE: NO — no tkinter or GUI launcher in the live tree
launch_opencode_zen.py | CAPABILITY: console launcher that counts Redis keys as a context measure, HTTP-probes the dashboard on 8501, prints a calm greeting plus recent actions, and prompts before launching | LIVE: NO
launch_paired_agents.py | CAPABILITY: launches a generator and an analyst as two opencode processes that collaborate through blackboard proposal.json/verdict.json plus the vector message bus, waiting for Redis and reporting both PIDs and blackboard state | LIVE: YES E:\AI-Setup\core\comm\launcher.py:Launcher.launch (multi-agent spawn + status); the generator/analyst blackboard protocol itself is absent
launch_verifier.py | CAPABILITY: post-launch confirmation — polls at 0.5s then 1s intervals checking tasklist for a process name, PowerShell for a window title, or OCR'd screen text, driven by a LaunchConfig | LIVE: YES E:\AI-Setup\core\comm\runner_lock.py:_pid_alive_default (tasklist PID probe) and E:\AI-Setup\core\comm\launcher.py:_classify_exit; the window-title and screen-OCR verification paths are absent
list_windows.py | CAPABILITY: enumerates every titled top-level window with pygetwindow and prints title, position and size | LIVE: NO — no pygetwindow or window enumeration in the live tree
log_gpu_fix.py | CAPABILITY: one-shot script that files a single ErrorDoc record (system=vision, type=gpu_detection_fix) describing DirectML detecting the AMD 9070 XT while inference stayed on CPU | LIVE: YES E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning (the live door for filing a diagnosed failure); the ErrorDoc class itself is gone
log_oversight.py | CAPABILITY: one-shot script filing a single ErrorDoc record (system=gemini_bridge, type=wrong_error_message) noting a misdiagnosed "connection failed" that was really a screen capture | LIVE: YES E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning + tag_anti_pattern; ErrorDoc absent
master.py | CAPABILITY: polling state machine over the Blackboard that drives PLANNING→REVIEW→EXECUTING→VERIFYING→DONE/ERROR, reads VRAM and forces gc/deload under pressure, pivots after 3 consecutive fails, resolves intent priority from prompt metadata, escalates via the escalation manager and captures vision context on error | LIVE: YES E:\AI-Setup\core\coord\conductor.py (impure orchestration shell over core/coord/task_ledger.py — real timestamps, gated transitions, bus RESOLVED marker); the VRAM gating and vision-on-error halves have no live equivalent
mcp_servers/agent_comm/__init__.py | CAPABILITY: package docstring listing the 8 exposed agent-comm MCP tools plus a re-export of mcp and serve from .server | LIVE: YES E:\AI-Setup\ai_setup_mcp.py (single-module MCP server; the agent_comm package wrapper no longer exists)
mcp_servers/agent_comm/server.py | CAPABILITY: FastMCP server exposing send_message, check_messages (with mark_read), get_active_agents, get_my_status, declare_operation (manifest+alert), complete_operation and semantic search_messages over the Redis agent-comm layer | LIVE: YES E:\AI-Setup\ai_setup_mcp.py:bifrost_send / bifrost_inbox / bifrost_presence / status (same tool surface); declare_operation and complete_operation have no live twin
model_lifecycle.py | CAPABILITY: ModelLifecycleManager that tracks a per-model VRAM budget, decides can_load by priority, picks victims via get_models_to_unload, loads/unloads the vision model, wraps a callable in execute_with_vision to swap around it, and suggests swaps | LIVE: NO — E:\AI-Setup\core\fleet\model_roster.py:select/probe_availability is a read-only spec-and-availability query that explicitly never drives a process; no VRAM accounting or load/unload code live
multi_agent.py | CAPABILITY: three Redis+vector subsystems — AgentRegistry (register/heartbeat/presence TTL, role lookup, detect_conflicts on a resource), MessageBus (direct/broadcast send, read marks, unread count, semantic search_messages), SharedWorkspace (put/get/delete, TTL lock/unlock, per-key history, named spaces) — plus help-request creation | LIVE: YES E:\AI-Setup\core\comm\bus.py:Bus.register/presence/send/broadcast (registry+bus) and E:\AI-Setup\core\comm\locks.py:LockManager.acquire/release/path_conflict (workspace locking); the help-request flow has no live equivalent
multi_instance_launcher.py | CAPABILITY: CLI that waits for Redis, runs role-specific initialization, spawns one opencode process per role (generator/analyst/master/orchestrator/general) with a session id, and can list active agents as a table | LIVE: YES E:\AI-Setup\core\comm\launcher.py:Launcher.launch/registry and E:\AI-Setup\core\comm\roster.py:render_by_agent (the agent table)
quick_launch.py | CAPABILITY: runs a shell command then polls `python -m naturo list windows` up to N times to confirm a named window appeared, reporting OK/FAIL | LIVE: NO — no window-presence verification loop in the live tree
redis_backup.py | CAPABILITY: RedisBackup scans include-pattern keys, exports each with its type (string/list/hash/set), writes a timestamped JSON plus a _latest alias under blackboard_data/redis_backups, supports a continuous 5-minute loop and restore_from_backup | LIVE: YES E:\AI-Setup\scripts\ops\snapshot_knowledge.py (type-aware redis_db0.json dump + store_state.json + learnings.jsonl into backups/snapshots/, with list/restore/verify and retention pruning)
redis_backup_fast.py | CAPABILITY: dumps Redis by shelling redis-cli inside the WSL Ubuntu-24.04 container one key at a time (no pipelining) and hashes the result into a JSON backup file | LIVE: YES E:\AI-Setup\scripts\ops\snapshot_knowledge.py (same dump product, in-process client instead of WSL shell-out)
reprime.py | CAPABILITY: clears the session state file and writes a reprime_trigger.json to force the next session to re-prime; --check just prints current session state; also prints the re-prime instruction block | LIVE: YES E:\AI-Setup\agent_cli.py:cmd_boot (on-demand reprint of startup context supersedes the trigger-file mechanism)
restore_redis.py | CAPABILITY: reads redis_backup_latest.json and re-writes every key back into Redis by type (set / rpush / hset mapping / sadd), deleting collections first, counting successes | LIVE: YES E:\AI-Setup\scripts\ops\snapshot_knowledge.py (restore subcommand, auto-snapshots current state before restoring)
robust_catchup.py | CAPABILITY: one function that connects to Redis and prints a full new-session catch-up — active sessions, then the stored context keys — as the first thing a fresh instance runs | LIVE: YES E:\AI-Setup\agent_cli.py:cmd_boot (ranked startup context, siblings, mailbox, save-line)
save_dashboard_fix.py | CAPABILITY: one-shot KB.write recording an AttributeError root cause in app.py render_header and the four isinstance guards applied | LIVE: YES E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning (live door for the same record); knowledge_base.KB is gone
save_diagnostic.py | CAPABILITY: one-shot script that registers a "system_diagnostic" model in the KB and writes a hardcoded service/window/container status snapshot plus a diagnostic-history markdown doc | LIVE: YES E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning and the note/doc verbs in E:\AI-Setup\agent_cli.py; the KB model registry has no live twin
save_gpu_fix.py | CAPABILITY: one-shot KB write of the working Ollama ROCm container config (volume mounts, HSA_OVERRIDE_GFX_VERSION, /dev/dxg device, portproxy command) plus a markdown doc explaining the GPU-discovery timeout fix | LIVE: YES E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning; the ROCm/Ollama content itself has no live counterpart
save_gpu_status.py | CAPABILITY: one-shot KB write recording that gfx1201/RDNA4 is unsupported by ROCm 7.2.1 so Ollama falls back to CPU, plus a status markdown doc | LIVE: YES E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning
save_keepalive.py | CAPABILITY: one-shot KB write registering a "keepalive_system" model with its 30s interval, root cause and file list, plus a usage markdown doc | LIVE: YES E:\AI-Setup\core\learning\learning_store.py:LearningStore.record_learning
screen_monitor.py | CAPABILITY: prints open windows via PowerShell Get-Process MainWindowTitle, checks for a docker* process, lists ollama containers through WSL, then grabs the primary monitor with mss and writes the PNG to the Desktop | LIVE: NO — no mss, no MainWindowTitle enumeration, no docker/WSL probing in the live tree
screen_ocr.py | CAPABILITY: captures the primary monitor with mss and OCRs it by driving Windows.Media.Ocr through an inline PowerShell script, falling back to pytesseract, and dumps the text to Desktop\ocr_output.txt | LIVE: NO — no OCR path and no desktop capture live
screenshot_logger.py | CAPABILITY: session-tagged desktop capture — writes {session_id}_{tag}_{timestamp}.png into session_screenshots and records the capture reason to Redis in parallel, with capture_ocr/capture_debug convenience wrappers and recent-screenshot listing | LIVE: NO — E:\AI-Setup\scripts\ui_shot.py screenshots a Playwright browser page only, never the desktop, and keeps no Redis-side capture log
scripts/_wait_reply.py | CAPABILITY: throwaway poller that xrevranges bifrost:inbox:cursor every 5s for up to N rounds looking for entries newer than a given stream id whose frm=="claude", pretty-prints them, and dumps the last 5 inbox entries on timeout | LIVE: YES E:\AI-Setup\ai_setup_mcp.py:bifrost_inbox/bifrost_sync (inbox read since cursor) and E:\AI-Setup\core\comm\expectations.py (awaited-reply tracking)
scripts/heimdall.py | CAPABILITY: background wake sentinel — blocks on an agent's bus inbox plus broadcast in 2-minute inner blocks up to a 30-minute deadline, skipping trace/reply/steer noise, exiting only on real mail so the harness re-invokes the idle agent; writes and clears a PID heartbeat file in tempdir | LIVE: YES E:\AI-Setup\scripts\bifrost_wake.py (same blocking wake listener, same noise-skipping and PID seat file, extended with per-session seats and operator-facing exit codes)
scripts/hooks/__init__.py | EMPTY | LIVE: N/A
scripts/hooks/birth_guard.py | CAPABILITY: pre-commit rule-13 guard whose pure classify() returns allow/refuse/warn for each newly-added .md path (allowing docs/library, docs/_archive, README, docs/UPPERCASE crown, charters/<agent>/CHARTER.md; refusing other docs/, research/ and non-projection chronicles/), then exits 1 on any refusal | LIVE: YES E:\AI-Setup\scripts\githooks\birth_guard.py:classify (same docstring and rule table; staged_added gained an optional paths argument)
scripts/hooks/cursor_beforeshell.py | CAPABILITY: Cursor beforeShellExecution adapter — captures the payload to the akashic_recall temp dir, extracts command (with tool_input.command fallback), asks agent/harness/guards.git_veto, and prints allow or a deny carrying agentMessage+userMessage; fails open | LIVE: YES E:\AI-Setup\agent\harness\hooks\cursor_beforeshell.py (same file, moved)
scripts/hooks/cursor_posttooluse.py | CAPABILITY: Cursor postToolUse/postToolUseFailure adapter — resolves the event from argv, calls resolve_action_outcome per normalized target, injects recall on failure and on unseen targets, and on a FAIL→SUCCESS flip emits a durable flip event plus the rate-limited JIT learn nudge | LIVE: YES E:\AI-Setup\agent\harness\hooks\cursor_posttooluse.py (same file, moved)
scripts/hooks/cursor_pretooluse.py | CAPABILITY: Cursor preToolUse adapter — deny-only; captures payload, defensively reads command/file_path, applies git_veto and, for in-scope paths, lock_veto against a peer's advisory lock, printing allow or deny | LIVE: YES E:\AI-Setup\agent\harness\hooks\cursor_pretooluse.py (same file, moved)
scripts/hooks/cursor_sessionend.py | CAPABILITY: Cursor sessionEnd adapter — captures payload then distills the session's last-24h commits, recent lessons, day's decisions and FAIL→SUCCESS flips into chronicles/last-session-draft.md via agent_cli.write_last_session_draft; silent and fail-open | LIVE: YES E:\AI-Setup\agent\harness\hooks\cursor_sessionend.py (same file, moved)
scripts/hooks/cursor_sessionstart.py | CAPABILITY: Cursor sessionStart adapter — captures payload, warms the recall cache and prunes state, then returns env AKASHIC_AGENT_ID (default "composer", propagating to later hooks) plus build_autoboot_context() as additional_context | LIVE: YES E:\AI-Setup\agent\harness\hooks\cursor_sessionstart.py (same file, moved)
scripts/hooks/install_git_hooks.py | CAPABILITY: runs `git config core.hooksPath scripts/githooks` at repo root so the tracked pre-commit backstop is active per clone/worktree, and prints the AKASHIC_AGENT_ID reminder | LIVE: YES E:\AI-Setup\scripts\githooks\install_git_hooks.py:main (same command, same output)
scripts/hooks/mojibake_signatures.py | CAPABILITY: byte-level encoding-corruption detector for .md — flags U+FFFD replacement chars, lone UTF-16 surrogates, cp1252 double-encoded smart quotes, truncated multibyte sequences at end-of-line and NUL bytes; runs over named files or --staged and exits 1 to refuse the commit | LIVE: YES E:\AI-Setup\scripts\githooks\mojibake_signatures.py:check_file/staged_md_files (same file, moved)
scripts/hooks/pre_commit.py | CAPABILITY: git pre-commit backstop that reads staged files and aborts the commit when a peer holds an advisory lock on one (core.comm.locks.path_conflict), failing open for a human but closed when the agent id is unset; plus a fast comprehensibility check | LIVE: YES E:\AI-Setup\scripts\githooks\pre_commit.py:check_staged (same function, extended live with guardrail_counts/ratchet_ok/regenerate_derived)
session_context.py | CAPABILITY: Redis-backed singleton SessionContextManager persisting a SessionContext dataclass — current task, phase, decisions with rationale/outcome, files modified, discoveries, blockers add/remove, next steps complete — and rendering a work summary and recent-work list | LIVE: YES E:\AI-Setup\core\state\session_checkpoint.py:SessionState (task/progress/blockers checkpointing) plus E:\AI-Setup\core\learning\agent_memory.py get_decisions
start_comm_service.py | CAPABILITY: starts the agent_comm_service NotificationServer on port 5555 in a background thread to wake sleeping agents when messages arrive, and can register/unregister itself in the Windows startup registry | LIVE: YES E:\AI-Setup\scripts\bifrost_wake.py and E:\AI-Setup\scripts\bifrost_daemon.py (bus-driven wake); the Windows autostart registration has no live equivalent
system_diagnostic.py | CAPABILITY: probes service ports, enumerates visible windows via an inline Win32 EnumWindows P/Invoke in PowerShell, and captures the screen into a RAM-backed context_cache instead of disk | LIVE: NO — no EnumWindows, no port-sweep diagnostic and no RAM screenshot cache in the live tree
test_gpu_pytorch.py | CAPABILITY: sets ROCM_PATH/HSA_ENABLE_DXG_DETECTION/LD_LIBRARY_PATH then asserts torch.cuda availability, device name/capability and a 1000x1000 matmul on the GPU, exiting 1 if unavailable | LIVE: NO — no torch import anywhere in the live tree
test_js_gemini.py | CAPABILITY: Selenium attach to 127.0.0.1:9222, switch to the Gemini tab, reload gemini.google.com, and probe via execute_script whether div[role='textbox'] can be focused and have innerText set | LIVE: YES E:\AI-Setup\scripts\gemini_web.py:_find_input (same input-locating problem, solved in Playwright)
test_js_gemini2.py | CAPABILITY: same CDP attach, plus find_response() which walks 8 candidate CSS selectors (div.model-response-text, gc-response-text, article, …) looking for a >20-char non-"typing" block — selector discovery for scraping Gemini's answer | LIVE: YES E:\AI-Setup\scripts\gemini_web.py:_extract_latest_model_text
test_js_gemini3.py | CAPABILITY: same attach, but instruments the round trip — reads the textbox innerText before and after JS injection to prove the typed prompt actually landed before submitting | LIVE: YES E:\AI-Setup\scripts\gemini_web.py:_ask_gemini_chat (fill-then-verify-then-submit path)
test_qwen_ocr.py | CAPABILITY: grabs a fixed 800x600 screen region with pyautogui, base64s it, POSTs to Ollama /api/generate with qwen2.5vl:7b asking for full text extraction, and logs elapsed time and character count | LIVE: NO — no pyautogui and no VLM OCR call live; E:\AI-Setup\scripts\ask_gemini_vision.py sends an existing image file to remote Gemini but never captures and uses no local model
test_qwen_taskmgr.py | CAPABILITY: locates the "Task Manager" window with pygetwindow, screenshots its exact bbox (falling back to full screen), and OCRs it through Ollama qwen2.5vl:7b at temperature 0, timing the run | LIVE: NO — no window-bbox capture and no local VLM OCR in the live tree
test_torch.py | CAPABILITY: near-duplicate of the ROCm probe — prints ROCM_PATH/LD_LIBRARY_PATH, asserts torch.cuda, runs a GPU matmul, and additionally reports memory_reserved vs memory_allocated | LIVE: NO — no torch in the live tree
test_video_keyframes.py | CAPABILITY: exercises vision_engine's video path — extract_keyframes over a video file via OpenCV, analyze_video_keyframes captioning, a screenshot-series variant and a screen-recording variant, reporting per-frame timings | LIVE: NO — no keyframe extraction, OpenCV or video analysis in the live tree
test_vision_timing.py | CAPABILITY: timing harness that instantiates VisionEngine, prints the resolved device, times model load, times capture_active_window, then times analyze_screen(task='caption') and prints the caption | LIVE: NO — VisionEngine has no live counterpart
tests/test_coordinator_foundation.py | CAPABILITY: 6-part integration test of the Week-1 coordinator — agents emitting signals through coordinator_api, the coordinator service monitoring and synthesizing, decision caching and retrieval, blocker escalation, agent handoff with briefing generation, memory efficiency and the learning system — isolated onto Redis db 15 via isolate_canonical | LIVE: NO — it imports core.signals.coordinator_service, which no longer exists (only coordinator_api.py survives); tests/test_session_signals.py covers a different contract
tests/test_fixes_quick.py | CAPABILITY: five no-Redis smoke checks — LearningStore(redis_client=None) file fallback, DecisionCache add/get/get_relevant_decisions, briefing_loader import, and hasattr probes for CoordinatorAPI/CoordinatorService startup-context methods | LIVE: NO — the coordinator_service/DecisionCache half is gone; only E:\AI-Setup\core\context\briefing_loader.py:load_briefing_from_previous_handoff still exists, and no live test replaces this smoke script
unified_agent.py | CAPABILITY: UnifiedAgentLayer facade that picks the Redis multi_agent backend when available and silently falls back to the filesystem agent_coordinator when not, keeping register/heartbeat/send/broadcast/search, workspace put-get-search, claim_task/release_task locking, learning sharing, help requests and a sync_to_redis reconciliation | LIVE: YES E:\AI-Setup\core\foundation\store.py:HybridStore (dual-write Redis + File with Redis-first read and File fallback — the same degradation contract) plus E:\AI-Setup\core\comm\bus.py:Bus for the messaging half
universal_cache.py | CAPABILITY: AssetsManager central download cache — a JSON manifest keyed by name+version over per-source-type dirs, has/get/add/remove, download() with update checking, get_or_download(), an inventory report, plus concrete fetchers for chromedriver, HuggingFace models and Ollama pulls and a @cached_download decorator | LIVE: NO — no chromedriver, huggingface, assets manifest or download-cache code anywhere in the live tree
vector_store.py | CAPABILITY: dependency-free similarity search — deterministic hash-based embeddings (no ML model), optional FAISS index with a brute-force fallback_search, add_entry/embed_learning/embed_log, top-k search with model filtering, get_by_key, get_recent, stats, and sync_from_knowledge_base | LIVE: YES E:\AI-Setup\core\primitives\embedder.py:Embedder (embed/embed_many/similarity/relevance with a store-backed cache — real sentence embeddings replacing the hash trick)
vision_engine.py | CAPABILITY: Florence-2 wrapper — load/unload the model on a resolved device, analyze_screen dispatching CAPTION / DETAILED_CAPTION / OCR / OCR_WITH_REGION / OD / phrase-grounding task tokens, full_analysis, active-window and region capture with PyAutoGUI/MSS/PIL fallbacks, screenshot saving, base64 encoding, quick_error_detection, an analyst-facing screen context payload, and video keyframe extraction | LIVE: NO — no Florence-2, no local vision model and no desktop capture live; E:\AI-Setup\scripts\ask_gemini_vision.py only forwards an existing image file to remote Gemini
window_monitor.py | CAPABILITY: verifies a launched window actually appeared by polling PowerShell's Get-Process MainWindowTitle JSON for a title pattern, returns success/reason, and can Stop-Process the matching window or sweep failed launcher windows | LIVE: NO — live liveness checking is process-level only (E:\AI-Setup\core\comm\runner_lock.py:_pid_alive_default tasklist PID probe, E:\AI-Setup\core\comm\launcher.py:Launcher.kill); nothing matches or closes windows by title
