# T342 evidence sweep A (dead modules 1-72, alphabetical) — Explore subagent, 2026-08-18
# Contract: CAPABILITY from source, LIVE verdict with file:symbol evidence or honest NO/UNKNOWN.
# Preserved verbatim from the agent's return; judgment deferred to synthesis.

_archive/legacy/agent_logger.py | CAPABILITY: singleton that wrote agent work-context/actions/results to Redis keys `agent:work`/`agent:actions`/`agent:history` and minted auto-versioned patch entries (`_gen_patch_id`, `_load_version`/`_save_version`, `patch()`/`feat()`/`fix()`) with by-system/by-result secondary indexes | LIVE: YES core/narrative/beat_log.py:BeatLog.emit + agent_cli.py:cmd_log (work/action beats); patch+version half at scripts/ship.py:build_plan
_archive/legacy/gemma_realtime/audio_handler.py | CAPABILITY: Silero-VAD speech detection over raw PCM chunks plus a rolling AudioBuffer and a WebSocket fan-out handler with barge-in `interrupt()` | LIVE: NO
_archive/legacy/gemma_realtime/document_implementation.py | CAPABILITY: one-shot script writing a JSON description of the voice stack (components, ports, ws URL) into Redis via `connect_to_redis_with_fail_fast` | LIVE: YES agent_cli.py:cmd_note (durable note store; connector still live at core/foundation/redis_connection.py:connect_to_redis_with_fail_fast)
_archive/legacy/gemma_realtime/document_state.py | CAPABILITY: one-shot script recording ROCm/WSL2 GPU-detection failure history (attempts, dates, results) as a JSON blob in Redis | LIVE: YES agent_cli.py:cmd_note (durable note store)
_archive/legacy/gemma_realtime/file_analyzer.py | CAPABILITY: dispatched uploaded bytes by extension to per-format analyzers — code, image (PIL/torch), PDF, spreadsheet, sqlite DB, audio, video — returning a structured summary dict | LIVE: NO
_archive/legacy/gemma_realtime/llm_wrapper.py | CAPABILITY: streaming Ollama chat client with mid-generation `interrupt()`, interrupt-intent classification, Redis persistence of the truncated response, and `resume()` | LIVE: YES core/fleet/caller.py:call (one-shot Ollama /api/generate); interrupt/resume half at core/comm/nudge.py:nudge/is_nudged
_archive/legacy/gemma_realtime/server.py | CAPABILITY: FastAPI+uvicorn app wiring audio/STT/TTS/LLM/file-analyzer behind `/ws`, `/health`, `/voices`, `/upload`, `/chat` and an inline HTML GUI | LIVE: NO
_archive/legacy/gemma_realtime/stt_processor.py | CAPABILITY: Faster-Whisper speech-to-text with a fast/accurate model-size switch and streaming-chunk transcription | LIVE: NO
_archive/legacy/gemma_realtime/test_components.py | CAPABILITY: async smoke test that calls `init_stt`/`init_tts`/`init_llm` and exits nonzero on load failure | LIVE: NO
_archive/legacy/gemma_realtime/test_gpu.py | CAPABILITY: torch probe printing CUDA/ROCm availability, GPU name and total VRAM, then selecting a device | LIVE: NO
_archive/legacy/gemma_realtime/tts_processor.py | CAPABILITY: text-to-speech with a Kokoro engine (voice catalog, per-sentence streaming) and an espeak-ng subprocess fallback behind one `TTSProcessor` facade | LIVE: NO
_archive/legacy/project_context.py | CAPABILITY: deprecated back-compat shim re-exporting `ProjectContextManager`, `Milestone`, `Task`, `Blocker`, `get_project_context_manager_instance` from `context.project_context` so old `from project_context import ...` kept working | LIVE: YES core/context/project_context.py:ProjectContextManager / get_project_context_manager_instance
_archive/legacy/services/background_monitor.py | CAPABILITY: daemon polling Redis Streams every 100ms, raising Windows toast notifications and maintaining a per-agent on-disk message inbox (`add_message`/`get_messages`/`get_unread_count`/`mark_read`) plus a heartbeat loop | LIVE: YES agent/bifrost_pull.py:peek_inbox/consume_inbox + core/comm/mailbox.py:query (inbox state); human-notification half at core/comm/pager.py:page
_archive/legacy/services/port_manager.py | CAPABILITY: declared the Akashic port map, allocated/released named ports with socket-level in-use probing and auto-reassignment, detected conflicts, and mirrored the registry into Redis `port:*` hashes | LIVE: YES config.py:PORT_REGISTRY + scripts/checkers/check_ports.py:report/gate (declared vs in-code vs listening; conflict gate)
_archive/legacy/services/redis_ha_manager.py | CAPABILITY: drove a 1-master/2-replica/3-Sentinel Redis topology over WSL — queried Sentinel for the current master, checked replication and instance health, generated the HA docker-compose and sentinel.conf, and blocked on `wait_for_failover` | LIVE: NO
_archive/legacy/services/redis_manager.py | CAPABILITY: full Redis backup lifecycle — type-aware key export, SHA-256 checksummed backups with a rotating catalog, integrity verification, restore, health scoring, container start, and a monitor loop | LIVE: YES scripts/ops/snapshot_knowledge.py:snapshot/_dump_redis/_restore_redis/verify
_archive/legacy/services/redis_sync.py | CAPABILITY: low-CPU background poller that tailed session log files from a checkpointed line offset, deduped entries by content key, and pushed only new ones into Redis, with reconnect and SIGTERM-safe shutdown | LIVE: YES core/foundation/store.py:HybridStore (Redis authority + FileStore mirror) and core/foundation/durable_reconcile.py:plan/apply
_archive/legacy/services/session_monitor.py | CAPABILITY: reconciled OpenCode sessions from Redis `session:*:log` and from files to classify each as logging vs silent, then nudged silent sessions with primer text and ran as a daemon | LIVE: YES core/comm/doctor.py:examine (wedge/stall grading) + core/comm/nudge.py:nudge
_archive/legacy/session_compressor.py | CAPABILITY: watched three signals (Redis string log, list log, `session:events` stream via XREAD) and summarized session text with Gemma into a RediSearch text index, exposing `search_context` | LIVE: YES core/narrative/chronicler.py:Chronicler (rolls beats into summarized chapters) + core/primitives/distiller.py:Distiller
_archive/legacy/session_logger.py | CAPABILITY: singleton JSONL session logger with auto-tagging, typed helpers (`action`/`decision`/`error`/`learning`/`system`), Redis mirroring, session digest generation, an index update, and a failsafe write path | LIVE: YES agent_cli.py:cmd_log -> core/narrative/beat_log.py:BeatLog.emit + core/events/event_log.py:capture_event
_archive/legacy/session_summarizer.py | CAPABILITY: watched Redis for new session logs, summarized each via a Gemma HTTP endpoint, computed MiniLM embeddings and stored summary+vector into RediSearch indexes on both WSL and Windows Redis | LIVE: YES core/primitives/embedder.py:Embedder/get_embedder + core/primitives/distiller.py:Distiller (used by core/narrative/theme_discovery.py)
_archive/legacy/stack_manager/__init__.py | CAPABILITY: package facade re-exporting SERVICES, `resolve_tiers`, `check_health`, launcher/port/routing/resource/memory objects plus shared `c()`/`log()` console formatters | LIVE: NO
_archive/legacy/stack_manager/__main__.py | CAPABILITY: `python -m stack_manager` entrypoint that imports and calls `stack_manager.cli.main` | LIVE: NO
_archive/legacy/stack_manager/cli.py | CAPABILITY: operator CLI for the stack — start (parallel per DAG tier), status, ps, kill (with WSL/Windows force paths), recover, stop, restart, and a live monitor screen | LIVE: YES core/comm/launcher.py:Launcher (spawn/monitor/kill/status of agent processes) — DAG-tiered start and ps/recover subcommands have no live counterpart
_archive/legacy/stack_manager/config.py | CAPABILITY: the SERVICES table — per-service command, ports, `depends` edges, health-check spec and resource limits, with paths resolved to session_compressor/ai_watchdog/stack_gui/ai_setup_mcp | LIVE: NO
_archive/legacy/stack_manager/dag.py | CAPABILITY: Kahn's-algorithm topological sort of the service `depends` graph into parallel-safe launch tiers | LIVE: NO
_archive/legacy/stack_manager/health.py | CAPABILITY: health-check engine dispatching per service kind — TCP connect, HTTP GET, Redis PING, process-exists, WSL-alive — via subprocess wrappers | LIVE: YES core/infrastructure/health_check.py:check_infrastructure_health + scripts/checkers/check_ports.py:listening_ports (TCP-listening probe)
_archive/legacy/stack_manager/launcher.py | CAPABILITY: started a service as a Windows, WSL or Docker subprocess and blocked in `wait_for_healthy` until its health check passed, registering the route | LIVE: YES core/comm/launcher.py:Launcher.start/_classify_exit
_archive/legacy/stack_manager/memory.py | CAPABILITY: sampled per-service RSS/VMS across Windows (PowerShell), WSL and Docker and flagged services over their configured memory limit | LIVE: NO
_archive/legacy/stack_manager/ports.py | CAPABILITY: extracted port assignments from the SERVICES table, detected duplicate/occupied ports, scanned the host for listeners and synced the map into Redis | LIVE: YES scripts/checkers/check_ports.py:code_ports/listening_ports/gate
_archive/legacy/stack_manager/redis_util.py | CAPABILITY: returned a pinged Redis client built from `config.get_redis_config()`, or None on any failure, for the orchestrator sidecars | LIVE: YES core/foundation/redis_connection.py:connect_to_redis_with_fail_fast
_archive/legacy/stack_manager/resources.py | CAPABILITY: sampled host CPU/RAM via psutil, WSL memory via /proc, GPU VRAM, and RAM-disk usage, and checked whether declared service resource needs fit capacity | LIVE: NO
_archive/legacy/stack_manager/routing.py | CAPABILITY: Redis-backed service discovery — register/update/unregister a service endpoint under `service:<name>:endpoint`, `discover()`, `list_all()`, and bulk sync from the SERVICES table | LIVE: YES core/comm/roster.py:heartbeat/roster (per-seat reachability directory) — host:port endpoint registry itself is not reproduced
_archive/python_old/add_milestone.py | CAPABILITY: one-shot script constructing a milestone dict (id, status, confidence, tags, component map) for the voice stack and persisting it | LIVE: YES core/context/project_context.py:record_milestone_marking_progress / mark_milestone_as_completed
_archive/python_old/agentic_automation.py | CAPABILITY: desktop automation facade combining Florence-2 screen understanding with PyAutoGUI input — `click_on_text`, `type`, `press_key`, `wait_for`, screen-change detection — plus a CDP-attached BrowserAutomation | LIVE: NO
_archive/python_old/ai_helper.py | CAPABILITY: grab-bag REPL helper — screen OCR (pytesseract+mss), dashboard/service status, learn/recall, UI-tree inspect/click/type/scroll via a Naturo service, screenshots, and Windows z-order capture/restore | LIVE: NO
_archive/python_old/ai_setup_mcp.py | CAPABILITY: FastMCP server exposing ~45 tools over the pre-refactor pipeline — session identity/append/flush, canonical stream search, project milestones/blockers, watchdog report, screenshot/window-capture/OCR, and mouse/keyboard control | LIVE: YES ai_setup_mcp.py (live rewrite; its own header states it replaces this one, now wrapping agent_cli `cmd_*` verbs) — the screenspace/OCR/input tools are not carried over
_archive/python_old/ai_watchdog.py | CAPABILITY: one-shot/daemon infra report joining a port section (from stack_manager PortManager), a `session:events` stream + JSONL snapshot, legacy log hints and compressor-process detection, persisted as JSON | LIVE: YES scripts/checkers/check_ports.py:report (port plane) + agent_cli.py:cmd_doctor / core/comm/doctor.py:examine (liveness plane) — the session-stream snapshot half is absent
_archive/python_old/architecture.py | CAPABILITY: hardcoded architecture-status document (Redis/vLLM/Ollama/vision state, GPU limitation notes) with `save_architecture()` persisting it | LIVE: YES scripts/generators/gen_arch_index.py + core/context/arch_loader.py:load_arch_slice (projects docs/ARCHITECTURE.md into boot context)
_archive/python_old/auto_capture.py | CAPABILITY: task-bracket recorder — `before_task`/`after_task` with a success score, plus `when_decided`/`when_approach_works`/`when_approach_fails`, writing through `learning.store.learn` and Redis and returning prior context for a task | LIVE: YES agent_cli.py:cmd_learn -> core/learning/learning_store.py; retrieval half at core/recall/at_action.py
_archive/python_old/auto_logger.py | CAPABILITY: scraped session JSONL/history to infer file operations and decisions with regexes and back-fill log entries without the agent calling a logger (`capture_session`, `quick_capture`) | LIVE: YES scripts/hooks/claude_posttooluse.py + agent/harness/capture.py:capture (harness-level automatic capture)
_archive/python_old/bulk_compress.py | CAPABILITY: batch pass over historical `session:*:log` / `session:*:actions` keys that summarized each via Gemma into `session:summary:*` in a text index and deleted the raw logs to reclaim space | LIVE: YES core/learning/consolidation.py + core/narrative/chronicler.py:Chronicler (retrospective rollup); the delete-raw-after-summarize step is not reproduced (core/primitives/distiller.py keeps a lossless source pointer instead)
_archive/python_old/catchup.py | CAPABILITY: resume briefing printer — recent sessions with efficacy, chronicles, learning-store decisions/experiences/insights, `context:*` keys, canonical stream tail, learned tags, action-type patterns, changelog, and FT.SEARCH over summaries | LIVE: YES agent_cli.py:cmd_boot (ranked startup context: lessons, handoff, project state)
_archive/python_old/check_active.py | CAPABILITY: ad-hoc script printing every entry (type, sequence, timestamp, tags) of the newest `session:session_*` Redis list log | LIVE: YES agent_cli.py:cmd_events -> core/events/event_query.py:EventQuery (the read surface over the current event log)
_archive/python_old/check_new.py | CAPABILITY: ad-hoc script counting all `session:session_*` log keys and dumping the newest one's entries to check whether new entries are arriving | LIVE: YES agent_cli.py:cmd_events -> core/events/event_query.py:EventQuery
_archive/python_old/chronicle.py | CAPABILITY: skeptical auto-chronicle — pattern-detected entries with a status ladder (prototype→alpha→beta→stable→verified), a dynamically learned tag vocabulary, ADR/failure capture, and `suggest_verification()` for unverified completion claims | LIVE: YES core/narrative/chronicler.py:Chronicler + core/narrative/tagging.py / tag_governance.py (learned vocabulary); the verification/status ladder survives as core/codex/lifecycle.py:supersede and core/learning/learning_store.py:tag_anti_pattern
_archive/python_old/diagnosis.py | CAPABILITY: throwaway script comparing entry counts and last timestamps of two hardcoded Redis session keys (`session:opencode_...:actions` vs `session:session_...:log`) | LIVE: NO
_archive/python_old/escalation.py | CAPABILITY: tiered review router — packaged a `ReviewRequest` and escalated it to OpenCode (best-practice review) or Gemini (strategy/security), with SOS-template generation and an architect path | LIVE: YES agent_cli.py:cmd_ask -> core/comm/ask.py (synchronous peer/model help call) + scripts/ask_gemini.py
_archive/python_old/fast_ocr.py | CAPABILITY: captured the screen with mss and ran PaddleOCR, with EasyOCR and Tesseract fallbacks, timing each backend | LIVE: NO
_archive/python_old/florence_vision_setup.py | CAPABILITY: bootstrap that pip-installs torch/PIL if missing, loads Florence-2 and exposes `analyze_screenshot(path, task)` for open-vocabulary detection on screenshots | LIVE: NO
_archive/python_old/gemma_voice_service.py | CAPABILITY: Flask+CORS HTTP service exposing /chat, /voice_input, /voice_output, /tts, /memory (Redis save+search), /code_execute and /models over Ollama Gemma and faster-whisper | LIVE: NO
_archive/python_old/health_check_session_pipeline.py | CAPABILITY: pipeline health report — compressor PIDs, docker ps/inspect device passthrough, ollama ps, Redis key checks, summary parity, persisted session id, and an inference-GPU hint section, with `--json` | LIVE: YES core/infrastructure/health_check.py:check_infrastructure_health + agent_cli.py:cmd_doctor
_archive/python_old/init_agent.py | CAPABILITY: entry script calling `initialize_and_load_context('claude-code-session')` and printing counts of loaded startup decisions, learnings and briefing | LIVE: YES agent/initializer.py:initialize_and_load_context (same symbol, still exported)
_archive/python_old/journey_dump.py | CAPABILITY: ad-hoc dump of the whole Redis knowledge surface into one dict — current task, milestones, decisions registry, approaches, experience, learnings keys and session logs | LIVE: YES agent_cli.py:cmd_knowledge_map -> core/recall/knowledge_map.py; raw export at scripts/ops/snapshot_knowledge.py:_dump_redis
_archive/python_old/launch_ai_stack.py | CAPABILITY: Tkinter launcher GUI that shells out to stack_manager commands, streams subprocess output into a scrolling Text widget via a queue pump, and opens service URLs; PyInstaller-aware root resolution | LIVE: YES core/comm/launcher.py:Launcher + scripts/bifrost_ui.py (browser console replaces the Tk window)
_archive/python_old/log.py | CAPABILITY: argparse CLI writing one typed entry (action/decision/error/learning) with tags and key=value data to both Redis and a JSONL file | LIVE: YES agent_cli.py:cmd_log
_archive/python_old/log_analysis.py | CAPABILITY: ad-hoc script listing every `session:session_*` log's entries with type/timestamp/tags and then aggregating a tag-frequency search across all items | LIVE: YES core/events/event_query.py:EventQuery (filter/window/rank over the event log)
_archive/python_old/migrate_logs.py | CAPABILITY: converted legacy `{unique_id, action, description, source, checksum}` entries to the `{type, content, tags, data}` shape — noise filtering, significance test, auto-tagging, per-session grouping and summary generation, with `--dry-run` | LIVE: NO
_archive/python_old/migrate_sessions.py | CAPABILITY: moved session JSONL and summary .md files into a `sessions/YYYY-MM-DD/` archive with a master `sessions/index.json` and generated per-session markdown digests | LIVE: NO
_archive/python_old/monitor_turbo_launch.py | CAPABILITY: watched a stack launch by polling a cached window list (fast_cache TTL) and calling capture_window/OCR/screenshot from the old MCP module to verify windows appeared | LIVE: NO
_archive/python_old/operational_alerts.py | CAPABILITY: tiered multi-agent alert bus — FILE_WRITE_IN_PROGRESS / DEPLOY / TEST / INTENT_DECLARED / SCOPE_CLAIMED alerts at CRITICAL..LOW, with `check_file_conflict` and `is_file_busy` to detect two agents touching the same path | LIVE: YES core/comm/locks.py:path_conflict/guard_write (advisory path claims with fencing token + TTL); page-grade alerting at core/comm/pager.py:page
_archive/python_old/origin_story.py | CAPABILITY: ad-hoc script walking all `session:*:actions` keys to print each session's first entry timestamp, plus the approaches registry, to reconstruct project chronology | LIVE: YES agent_cli.py:cmd_timeline + core/coord/timeline.py
_archive/python_old/patch_log.py | CAPABILITY: git-commit-styled change log — `feat`/`fix`/`refactor`/`docs`/`config`/`test` entries with auto-detected system and change type, semantic version load/bump/save, and `show_changelog` | LIVE: YES scripts/ship.py:build_plan/main (the one commit+push+lesson+snapshot ritual)
_archive/python_old/persist_semantic_learnings.py | CAPABILITY: one-shot seeding script emitting the relationship-types/semantic-naming lessons through `SignalEmitter` and `persist_learning_to_store` | LIVE: YES agent_cli.py:cmd_learn -> core/learning/learning_store.py + core/signals/coordinator_api.py:SignalEmitter (both still live)
_archive/python_old/populate_chronicle.py | CAPABILITY: one-shot backfill calling `Chronicle.milestone(...)` repeatedly to reconstruct project history into the chronicle | LIVE: YES scripts/seed_narrative.py (seeds core/narrative/chronicler.py:Chronicler)
_archive/python_old/port_registry.py | CAPABILITY: read-only console report printing every Redis `port:*` hash (port/protocol/description) and a hardcoded service-status list | LIVE: YES scripts/checkers/check_ports.py:report + scripts/generators/gen_ports.py:render (docs/PORTS.md from config.PORT_REGISTRY)
_archive/python_old/primer.py | CAPABILITY: printed a fixed ISSUE/CAUSE/COMPONENT/FIX/TEST scaffold for an agent to fill in at session start | LIVE: YES agent/harness/context.py:build_autoboot_context (the whisper that replaced the primer)
_archive/python_old/redis_failover_sync.py | CAPABILITY: dual-write handler keeping Redis and JSONL backup files consistent — checksummed sync metadata, drift detection with automatic resync, and a CrashRecovery class that rebuilt Redis from files | LIVE: YES core/foundation/store.py:HybridStore (Redis authority mirrored to FileStore) + core/foundation/durable_reconcile.py:plan/apply; rebuild path at scripts/ops/snapshot_knowledge.py:restore
_archive/python_old/redis_helper.py | CAPABILITY: argparse CLI over the learning store — `brief`, `lookup <query>`, `similar <task>`, `record <task> --success`, `insights` | LIVE: YES agent_cli.py:cmd_recall + agent_cli.py:cmd_learn
_archive/python_old/redis_sync_admin.py | CAPABILITY: admin surface over `RedisSyncCoordinator` — sync-status verification, health dashboard, manual resync triggers, data migration and historical analysis | LIVE: YES core/foundation/durable_reconcile.py:plan/apply + scripts/ops/snapshot_knowledge.py:verify
_archive/python_old/session_canonical.py | CAPABILITY: the single canonical write path — built versioned JSON envelopes, XADDed them to the `session:events` Redis stream, mirrored to `session_events_canonical.jsonl`, persisted the session id, and offered `xrange_tail`/`xrevrange_scan_for_session`/`aggregated_text_for_session` readers | LIVE: YES core/foundation/ledger.py + core/events/event_log.py:capture_event / core/events/event_query.py:EventQuery (config.py:SESSION_EVENTS_STREAM constant survives but has no live writer)
_archive/python_old/session_compare.py | CAPABILITY: ad-hoc script counting entries under `session:opencode_*:actions` vs `session:session_*` keys to compare the two competing log formats | LIVE: NO
