# c-map-design — RECONCILIATION (Vandor, 2026-08-23)

Inputs: half_a (Heimdall, mechanism — sealed), half_b (Vandor, spec,
window-fallback with disclosure — sealed; Simon's late input folds with
credit when it arrives), the prior-art mining (q1-prior-art doc: every
directive verbatim with transcript addresses), and Daniil's morning arc —
the Q1 breakdown, the prior-art commission, and the gate word verbatim:
"Lets build it."

## THE RECONCILED DESIGN — one substrate, two headsets

1. **Q1 (ratified by the morning arc):** primary viewer = the human headset.
   Front page: THE DECK AS TERRAIN dominant (active tasks, fences, open bets
   as landmarks lit by activity), whispers = event-family badge strip + last
   -24h trail heat. Kernel (unscrollable): doctor page-grade count + OVERDUE
   bets. RED takes the top banner unconditionally. Composition law verbatim:
   "assembled, not composed" is the named failure; ONE dominant layer.
2. **Trust before beauty (e696354a:965 made law):** the STAMP BLOCK ships in
   the first render — generation ts, HEAD sha, per-plane source cursors
   (ledger seq, newest stream ids, forecast count). A render without its
   stamp fails the fence's own pin.
3. **The generator is a pure seam** (T375's fold lesson applied to
   rendering): `build_map(data) -> html` pure; `gather_map_data() -> data`
   is the only I/O; CLI regenerates on demand and at gates. Static v1
   (state/map/index.html, repo-local, no auth — nothing leaves the machine);
   SSE realtime badges are v2 under the One UI 60fps contract; fuma-docs +
   better-auth ride Simon's fold or the multi-viewer future.
4. **Half_a's mechanism laws bind the generator:** it calls ONLY the Eye's
   read verbs — NEVER position.go/back/inherit/since (a render that moves a
   seat's cursor poisons since= — C3, load-bearing); the Eye's events index
   and routes.jsonl are journaled-AUTHORED, never treated as regenerable
   (C1); trails v1 = the authored routes layer, the T378 sensed overlay is
   a declared gap that arrives with the sensor (C4).
5. **The AsyncAPI x-convention** (half_b §1-2) commits as the census
   instrument seed: every x-claim is wire-testable; the drift check (spec
   vs wire) lands as a doctor line in slice M3 — instrument, not brochure.

## SLICES

M1 (NOW): generator + stamp block + landmarks + red banner — pins RED-first.
M2: badge strip + authored-trail whisper.
M3: asyncapi spec file committed + the x-claim drift check as a doctor line.
M4 (v2): SSE realtime badges under the One UI contract; Simon's deployment
    shape when multi-viewer arrives.

## Verdict lines

V1. One substrate two headsets: human front page and AI sensorium draw from the same planes with the same stamps, or the view plane grows the dual-store disease [CERTAIN]
V2. Front page = deck-as-terrain dominant + two whispers, kernel = page-grades + overdue, red takes the banner [DESIGN]
V3. The stamp block lands in the FIRST render -- trust before beauty, per the operator's own trust-first law [CERTAIN]
V4. The generator is a pure fold; its I/O half touches only read verbs and never the Eye position family [CERTAIN]
V5. AsyncAPI + the x-convention is the census instrument; the drift check is a doctor line, else it is a brochure [DESIGN]

## PV acknowledgment (M1-PV, section-scoped)

MISSING per M1-PV: half_b cites state/map/index.html -- a BUILD TARGET, not
evidence (the file exists after M1, by construction). Forward reference
acknowledged; no section invalidated; the pin that proves it lands with M1.

**Gate:** Daniil's word, verbatim, 2026-08-23: "Lets build it" — recorded as
the ratification of this reconciled design; M1 opens immediately.
