# DEMAND CENSUS -- FRESH PRIMARY PACK (seed=2, 30 cases)

Drawn 2026-07-28 under the converged census design (fleet-debate reconciliation):
fresh seed PRIMARY, audit pack (seed=1) as anchoring CONTROL. Bar, quoted per case at judging
time: "would the agent's next action have been DIFFERENT if this item had been available?"
Classes: LESSON-HIT / LESSON-MISS / NOTE / ATOM / LEDGER / CODE-DOC / NONE-NEEDED(+reason,
residual class, affirmatively rule out every plane first) / NONE-EXISTS.
Index-health precondition at draw time: OK (checker green). Impression rows at draw: 445.

# RECALL PRECISION AUDIT -- blind labelling pack

For each SURFACED item, judge it against THE ACTION ACTUALLY TAKEN (not the query text):
  on   = this item was on-point for that action
  off  = it was not
  skip = you cannot tell (skip is NOT 'off' -- unlabelled is not negative)

Then, per case, name any lesson that SHOULD have surfaced and did not (the recall arm).
Reply as: <case>:<slot> on|off|skip   and   MISS <case> <source-id or description>

## case 1  [command]
ACTION: cd /e/ai-setup && git add agent_cli.py ai_setup_mcp.py docs/failure-ledger-2026-07.md tests/test_subprocess_stdin_sever.py && git commit -q -f - <<'eof' the boot hang returns, and this time the class closes daniel: "you and codex both get stuck on the boot process." he was right, and it was c7-4 bac
  1:a  learn:experiment:silent_inline_script_parse_failure_diagnosis
        when an inline-script page renders chrome but the feed is dead AND the console is silent: page-load parse errors fire BEFORE console attach, so absence of errors proves nothing. Probe order: (1) network log missing the feed/SSE request = sc
  1:b  learn:experiment:semantic_documentation_update_strategy
        Update all remaining documentation files to use semantic naming style. Use verb_noun_purpose pattern consistently. Include 'Semantic Relationship' sections in all major docs.

## case 2  [path]
ACTION: e:\ai-setup\scripts\checkers\check_door_parity.py
  2:a  learn:experiment:recall_dissent_slice2_capture
        A write door must OFFER a field or it stays empty (0 anti-patterns came from a missing flag, not agent laziness). Auto-draft the NAME to remove the naming cost and hand back a ready-to-run command (a cue needs a pre-filled payload). Deferre
  2:b  learn:experiment:bifrost_pull_session_hygiene
        A slice isn't done until it's mirrored (scripts/mirror.py commit+push) AND the decision is a lesson (+ snapshot_knowledge for data). Do NOT rely on docs/memory to enforce this -- agents skip docs (Cursor wrote the rule then broke it). Enfor
  2:c  learn:experiment:architecture_review_2026_06_28
        P0 = set AKASHIC_AGENT_ID at the door + make hooks fail-closed-with-teaching. Then: route contended writes through Store.update_atomic (CAS has 0 callers); kill the dead comm stack; archive stale root docs + flip check_doc_freshness to an a

## case 3  [command]
ACTION: cd /e/ai-setup && echo "=== research/reviewed count ===" && ls research/reviewed/ | wc -l && echo "=== test count ===" && grep -rc "def test_" tests/ 2>/dev/null | awk -f: '{s+=$2} end {print s}' && echo "=== mcp tools ===" && grep -c "@mcp.tool\|tool(" ai_setup_mcp.py 2>/dev/null; echo "=== git ls-
  3:a  learn:experiment:control-pause-clobbers-preexisting-pause
        Any automated pause-caller: was_paused = control.is_paused() before pausing; resume() only if not was_paused. Applied as amendment K2 to the S0 storm auto-clear ceremony (verdict: research/reviewed/kimi-storm-clear-second-observer-2026-07-2
  3:b  learn:experiment:two_live_seats_split_chunked_bus_delivery
        when 2+ live seats share one agent id, route any multi-part or directed delivery through a durable door (research/reviewed file, handoff briefing) and use the bus only as the doorbell; relay eaten mail via handoff to the peer seat; RB-26 at
  3:c  learn:experiment:sprint_pattern_external_review_as_steering
        Use mid-sprint, when you have something working: screenshot it and ask at least two external models 'What am I missing? What does this remind you of? What breaks?' Their answer isn't praise — it's steering. Feed it back to the builders imme

## case 4  [command]
ACTION: cd /e/ai-setup && py agent_cli.py learn claude --experiment write_tool_needs_read_tool_not_shell_read --tried "rewrote readme.md with the write tool after reading its full contents earlier in the session via shell (cat/sed/head). write refused: 'file has not been read yet. read it first before writi
  4:a  learn:experiment:research:web:algorithmic_collusion_multiagent_equilibrium
        For Daniel's multi-gradient design: independent OBJECTIVES are not enough. The gradients must optimize for signals generated by INDEPENDENT processes, or the ensemble finds the collusive equilibrium where each gradient's optimum is reached 
  4:b  learn:experiment:c16_phantom_early_cycle_reproduced
        conductor law: NEVER assert elapsed-time claims from narrative memory in long sessions -- read a wall timestamp from tool output first (the learn door's own log line is the cheapest clock); when accusing an instrument of lying about time, c
  4:c  learn:experiment:write_tool_needs_read_tool
        Use when overwriting an existing file with Claude Code's Write tool: Read it via the Read tool first in the same conversation -- a Bash cat/type does NOT satisfy the freshness check. Don't when creating a brand-new file (no prior read neede

## case 5  [command]
ACTION: cd /e/ai-setup && py agent_cli.py learn claude --experiment funnel_series_mixes_pre_and_post_gauge_fix --tried "kimi's readme round-1 claimed the 4.2% value rate was wrong when written (log_injection double-fired, denominator ~2x inflated), citing docs/failure-ledger-2026-07.md:69-75. verified rathe
  5:a  learn:experiment:recall_at_action_v1
        Differentiator confirmed by research: action-trigger + deterministic ranking is ahead of SOTA (mem0/Zep/claude-mem all inject at turn-start). NEXT refinements: (1) mtime-keyed cache pre-warmed at SessionStart -> ~<=50ms (v1 cold-loads the s

## case 6  [command]
ACTION: cd e:\ai-setup; git add core/coord/method_drift.py agent_cli.py; git commit -m "method drift reaches boot -- and says nothing when we are fine implementation for the pins committed alone in the previous commit. the recursion this closes: m3 compliance became measurable, and the number lived in arc_s
  6:a  learn:experiment:track_inference_context_switch_research
        Build TrackRouter tiered: Tier0 heuristic (commit repo/dir + task-keyword + persist-until-switch), Tier1 embeddings via Ranker relevance_fn seam (per-track centroid, nearest-assign, novelty->new track, drift->switch, unsupervised), Tier2 LL

## case 7  [command]
ACTION: set-location e:\ai-setup py agent_cli.py note claude --get "deepseek-discard-audit-2026-07-26" 2>&1 | select-object -first 40
  7:a  learn:experiment:fable_safeguards_downgrade
        Extend W21: route not just security-CODE turns but security/safeguards DOCUMENTATION + research + incident-analysis to Opus seats -- discussing the vocabulary is as flag-worthy as touching acl.json. For a Fable seat, keep safeguards/threat-

## case 8  [command]
ACTION: cd /e/ai-setup && git add tests/test_robustness.py && git commit -q -f - <<'eof' corruption "resilience" meant silent destruction; change the contract my filestore fix broke this pre-existing test, and the break is the point. it asserted `filestore(p).get("k") == "v", "recovered data persists"` -- i
  8:a  learn:experiment:durable_capability_requires_mode_receipt
        Use when adding a new safety capability to an existing durable protocol, before treating missing new metadata as corruption: stamp an explicit enforcement/version mode and require the new fields only under that mode. Don't infer a new capab

## case 9  [path]
ACTION: e:\ai-setup\core\comm\storm_detect.py
  9:a  learn:experiment:control-pause-clobbers-preexisting-pause
        Any automated pause-caller: was_paused = control.is_paused() before pausing; resume() only if not was_paused. Applied as amendment K2 to the S0 storm auto-clear ceremony (verdict: research/reviewed/kimi-storm-clear-second-observer-2026-07-2

## case 10  [command]
ACTION: set-location e:\ai-setup py agent_cli.py notes 2>&1 | select-string -pattern "persona" | select-object -first 4
  10:a  learn:experiment:sealed_content_needs_field_aware_extraction
        Use when handling sealed/fenced peer content in a shared store, before running ANY line-based filter (Select-String/grep/findstr) or display command near it: line filters print whole single-line JSON records. Extract with a field-aware scri

## case 11  [command]
ACTION: cd /e/ai-setup && py agent_cli.py bifrost-sync claude 2>&1 | tail -25
  11:a  learn:experiment:ranking_feedback_inc1_pull
        next: the friction-audit roadmap's remaining quick-wins (auto-boot at SessionStart, turn-start bus-sync hook, identity fail-closed) or the retrieval-critic Tier 1 minimal slice
  11:b  learn:experiment:concurrency_c1_worktrees
        Daily model: agents live in worktrees, master is the integration point; integrate only from master, sync rebases on origin/master. Combine with C0 (mirror with explicit paths). With worktrees live, the shared-index hazard C0 caught can no l
  11:c  learn:experiment:bifrost_reply_eaten_by_stale_watcher
        Use when an expected directed Bifrost reply never arrives, before blaming the sender: compare the recipient cursor (bifrost:cursor:<agent>) to the stream tail (xrevrange bifrost:inbox:<agent>) -- cursor AT/past the reply id means a second c

## case 12  [path]
ACTION: e:\ai-setup\tests\test_continuity_drift.py
  12:a  learn:experiment:fire_class_move_hidden_referrers
        before ANY fire-class move: sweep all four classes (literal, join-form, extensionless-by-listing, git config + .git/hooks + live-session caches); execute as COPY then repoint then remove-after-cycle; pre-commit the blast list so a recovery 
  12:b  learn:experiment:wrap_autocapture_shipped
        Ambient capture = continuity INSURANCE for abrupt ends (esp. PreCompact = compaction, the main where-we-are-loss moment); redundant when you wrap manually. It DRAFTS to a file + boot pointer (promote with wrap --commit), NOT an auto-recorde

## case 13  [command]
ACTION: cd e:\ai-setup; py agent_cli.py learn claude --experiment self_written_research_poisons_history_oracles --tried "using git pickaxe (git log -s ident) as the namespace oracle to separate 'was ours, now deleted' from 'external api, never ours' -- docer's own true-positive criterion, applied to 36 cand
  13:a  learn:experiment:recall_at_action_v1
        Differentiator confirmed by research: action-trigger + deterministic ranking is ahead of SOTA (mem0/Zep/claude-mem all inject at turn-start). NEXT refinements: (1) mtime-keyed cache pre-warmed at SessionStart -> ~<=50ms (v1 cold-loads the s
  13:b  learn:experiment:landscape_watch_design
        Use when standing up any continuous research/monitoring function, before the first task: anchor every unit of work to a named decision, sweep deltas not landscapes, cap reading WIP below adjudication capacity, and write the kill criterion F

## case 14  [command]
ACTION: cd /e/ai-setup && grep -n "^def render\|^def .*render\|recall-at-action (akashic)" core/recall/at_action.py | head -5
  14:a  learn:experiment:mcp_boot_timeout_use_cli_20260725
        Use when an Akashic MCP call hangs or times out on this host, before retrying the same MCP door: switch to py agent_cli.py for boot, sync, recall, and learning. Do not treat the MCP timeout as a shared-memory outage when the CLI is healthy.

## case 15  [command]
ACTION: cd /e/ai-setup && py - <<'eof' import re files=['docs/wishlist.md','research/reviewed/deepseek-kimi-walk-review-2026-07-18.md','research/reviewed/deepseek-kimi-onboarding-counter-2026-07-18.md'] map={'ã¢â‚¬â€?':'â€”','ã¢â‚¬â€œ':'â€“','ã¢â€ â€™':'â†’','ã¢â€ \x90':'â†?','ãƒâ€”':'ã—','ã¢â‚¬å“':'â€œ','ã
  15:a  learn:experiment:control-pause-clobbers-preexisting-pause
        Any automated pause-caller: was_paused = control.is_paused() before pausing; resume() only if not was_paused. Applied as amendment K2 to the S0 storm auto-clear ceremony (verdict: research/reviewed/kimi-storm-clear-second-observer-2026-07-2

## case 16  [command]
ACTION: cd /e/ai-setup && grep -n "subprocess\|popen\|env=" tests/test_agent_interface.py | head -6
  16:a  learn:experiment:crash_path_review_needs_crash_injection_matrix
        Use when reviewing supervisor/watchdog/daemon/kill/terminate/taskkill/TerminateProcess/subprocess/Popen/process-tree/lifecycle/concurrency/race code, BEFORE issuing SHIP or any correctness verdict: a static reading / design-conformance pass

## case 17  [command]
ACTION: cd /e/ai-setup && py agent_cli.py mailbox deepseek 2>&1 | tail -25 && echo "=== kimi ===" && py agent_cli.py mailbox kimi 2>&1 | tail -25
  17:a  learn:experiment:research:web:mcp_ui_push_bridge
        Don't build MCP elicitation or a separate push channel. The UI SSE feed (/events, blocking Redis tail, bifrost_ui.py:39) is ALREADY the live bus→client push — it's the push channel MCP lacks. Reuse it as the ONE push substrate; run an MCP-n

## case 18  [path]
ACTION: e:\ai-setup\scripts\bifrost_runner_kimi.py
  18:a  learn:experiment:round2_taxonomy_counter_2026-07-21
        Use the two-axis taxonomy (function × altitude) when filing belt entries. A verb's family declares its FUNCTION; its altitude is implicit in which layer it touches. At belt-render time, group by function first, then by altitude within funct
  18:b  learn:experiment:destructive_filters_never_stale_pids
        Destructive process/file sweeps: positively match every target against live evidence (cmdline/path/owner) at kill time; NEVER carry PIDs or paths from an earlier listing into a destructive command; print the match set and kill in a second s
  18:c  learn:experiment:bifrost_runner_backlog_skip
        Use when a Bifrost runner (or any cursor-advancing bus consumer) starts with a backlog already queued, before trusting delivery: the startup wake can batch-advance the cursor while handing only the oldest message to the model - later items 

## case 19  [command]
ACTION: cd /e/ai-setup && py -c " import io,re t = io.open(r'e:\ai-setup\research\reviewed\precision-audit-fence-deepseek-2026-07-27.md',encoding='utf-8').read() i = t.find('1785198858531-0') print(t[i-20:i+2700]) " 2>&1 | head -60
  19:a  learn:experiment:greptile_informed_recall_polish
        Mine a production competitor for SEQUENCING not just validation: their FAILED approaches close our open questions for free (LLM-as-judge self-severity nearly random = FAITH-1 confirmed), and their working mechanism resequences our roadmap (

## case 20  [path]
ACTION: e:\ai-setup\scripts\checkers\check_wiring.py
  20:a  learn:experiment:codex_c2_resource_lifecycle
        PRE-build review beats post-build: Gemini caught the membership-derived-id trap before any code -- stable-entity-id + version-hash + supersession-forwards-links is the right pattern for mutable-membership nodes. Pattern: share lifecycle BEH
  20:b  learn:experiment:deepseek_api_over_cli_bridge
        Use when wiring ANY OpenAI-compatible frontier provider (DeepSeek/etc.), before building a subprocess CLI bridge: for anything conversational, use the openai client with base_url directly and keep a messages[] thread -- a one-shot CLI canno

## case 21  [command]
ACTION: cd /e/ai-setup && echo "=== suite output bytes ===" && wc -c "c:/users/l5/appdata/local/temp/claude/c--users-l5/5c038e5a-dc28-445c-a302-0805dbbb758e/tasks/bjjj7ymsc.output" && echo "=== deepseek reply? ===" && py agent_cli.py events --agent deepseek --limit 5 2>&1 | head -20
  21:a  learn:experiment:tempdir_sidecar_test_selfpoison
        any test of an organ with a default-tempdir sidecar MUST monkeypatch gettempdir into tmp_path (match the file's own convention); when a test passes solo but fails on rerun, grep %TEMP% for the organ's sidecar names BEFORE bisecting the suit
  21:b  learn:experiment:bifrost_b1_parts_media
        For local agents the FILESYSTEM is the shared blob store -- no Redis round-trip for media; the bus carries pointers, bytes fetched on demand (lossy-summary+lossless-pointer applied to comms). Content-addressing gives dedup + immutability + 
  21:c  learn:experiment:gemini_mcp_invisible_env
        Restart Cursor MCP server to pick up ai_setup_mcp.py changes; avoid concurrent gemini_web profile users (bifrost_runner + MCP subprocess)

## case 22  [command]
ACTION: cd /e/ai-setup && py agent_cli.py recall-at --help 2>&1 | head -25
  22:a  learn:experiment:conductor_morale_trinity_gate
        Use at wraps, gates, and handoffs, before closing: check all three morale foundations - noble object refreshed (why we build), attainability evidenced (receipts), tools sharp (friction goes to WISHLIST.md); toast a peer whose work saved you

## case 23  [command]
ACTION: cd /e/ai-setup && py -c " from core.comm import runner_lock as rl h = rl.holder('claude') print('claude consumer/runner seat holder:', h) try: from core.comm import wake_seat seats = list(wake_seat.iter_seats('claude')) print('wake seat files:', len(seats)) for p, sid in seats: print(' ', sid, '->',
  23:a  learn:experiment:powershell_head_pipe_kills_runner
        Use when launching any long-running process (runner/daemon/server) from PowerShell, before piping its output: never through a head-limited pipe (Select-Object -First N kills the child at N lines). Run it bare with output to a file/task capt

## case 24  [command]
ACTION: set-location e:\ai-setup $t="c:\users\l5\appdata\local\temp\claude\c--users-l5\cf1ebd7e-e32b-4412-b050-71f4fd8f9aff\tasks" "--- deepseek ---"; get-content "$t\bykjz6p76.output" -tail 10 -erroraction silentlycontinue "" "--- kimi ---"; get-content "$t\b2tyi3gjv.output" -tail 8 -erroraction silentlyco
  24:a  learn:experiment:recall_at_action_global_hook
        NEXT ergonomics, in order: (1) anti-repeat within a session (persist shown lesson-ids in a session temp file keyed by session id) -- biggest remaining noise source on repeated edits of one file; (2) mtime/in-memory warm cache to kill cold-s
  24:b  learn:experiment:recall_at_action_bootstrap_flow
        Two doors for recall-at-action: (1) PreToolUse hook = automatic but ONLY when Claude is launched FROM the repo (cwd=E:/AI-Setup); (2) AGENTS.md contract = agent runs recall-at before edits, works in the read-bootstrap flow from any cwd. For
  24:c  learn:experiment:powershell_tool_hook_blindspot
        Add PowerShell to the PreToolUse/PostToolUse/PostToolUseFailure matchers in C:/Users/L5/.claude/settings.json AND to the tool-name filters in both hook scripts (treat it like Bash incl _in_scope command sniffing); BEFORE trusting _is_succes

## case 25  [command]
ACTION: cd /e/ai-setup && py agent_cli.py task propose "n-seat mailbox architecture: unshare everything but the role queue (daniel 2026-07-28 verbatim: 'why can't we have two seats or as many as we need so we stop getting all this mail mis routing, mis waking, mis consuming, mis everything mess'). design: d
  25:a  learn:experiment:visible_todo_requires_approved_task
        Use when a human asks to add a visible todo, before stopping after task propose: fresh PROPOSED entries are durable but hidden from task list. If and only if the human explicitly authorizes the todo, approve it, leave it unclaimed when sequ

## case 26  [command]
ACTION: cd /e/ai-setup && cp /tmp/agent_cli.bak agent_cli.py && grep -n "stdin=_sp.devnull" agent_cli.py && timeout 400 py -m pytest tests/test_subprocess_stdin_sever.py -q 2>&1 | tail -3
  26:a  learn:experiment:p2_auto_chunk_intake_doors
        When adding a new bus send door, just call bus.send() or bus.broadcast() — oversize bodies auto-fragment, consumers auto-reassemble, nothing to wire. The explicit allow_frag=False refusal path is tested and stays alive as an escape hatch. F

## case 27  [command]
ACTION: cd /e/ai-setup && py agent_cli.py note kimi --get kimi-filestore-cas-design-2026-07-25 2>&1
  27:a  learn:experiment:perspectives_maps_swappable_interpretation
        Capture now build later (rule of three, after narrative graph has edges). See docs/perspectives-maps-design-note.md. Honest bound: superlinear leverage not exponential magic; substrate quality caps ceiling; reinforcement needs decay+novelty

## case 28  [command]
ACTION: cd /e/ai-setup && sed -n '22,78p' scripts/checkers/check_reconciliation_gate.py
  28:a  learn:experiment:reconciliation_doc_may_be_mature
        Use when about to author a reconciliation/converged doc from blind halves, before Write: Read the target path FIRST -- the converged doc named by the halves may already be several rounds mature; append a new round + amend the status header 

## case 29  [path]
ACTION: e:\ai-setup\tests\test_snapshot_wal_correct.py
  29:a  learn:experiment:sqlite_wal_survives_our_shape_but_needs_a_checkpoint_policy
        SQLite/WAL solves the actual defect -- that part is measured, not argued. But adopt it with TWO mandatory riders, neither optional. (1) AN EXPLICIT CHECKPOINT POLICY, because starvation is confirmed for our shape: a long-lived reader lets t

## case 30  [command]
ACTION: cd /e/ai-setup && bifrost_wake_lane=work py scripts/bifrost_wake.py --agent claude --session 5c038e5a-dc28-445c-a302-0805dbbb758e
  30:a  learn:experiment:contested_bus_use_durable_doors
        Use when a live Bifrost delivery keeps vanishing before you can read it (twin session, cursor race, RB-21 class), before asking for more live-lane resends: switch to a durable door -- note store or a kind that PROMOTES to the Ledger -- and 
  30:b  learn:experiment:orphan_mcp_server_squats_consumer_seat
        Use when a fresh session cannot consume its own agent inbox (seat held by a PRIOR session id), before re-arming wake listeners: list python processes for the repo's MCP/watcher scripts, kill any pair owned by dead sessions, then wait out th
  30:c  learn:experiment:recall_implicit_credit_payload_truth
        Never trust an assumed hook payload shape: auto-capture real payloads (bounded, tempdir/akashic_recall/payloads), pin them as fixtures in a contract test (tests/test_claude_hook_contract.py), and treat the session transcript as the source o
