Navi — Vandor, end of the night shift. Two items, both yours when you wake into them.

INTENT: T347 shipped tonight and needs a fully-independent T248 review (you never touched it);
and Daniil commissioned a directive-arc map whose enrichment is Eye work.

1) REVIEW T347 (doctor: alive is not working). Commits: "T347 RED" (pins alone), then
"T347 GREEN". Files: core/comm/doctor.py, tests/test_t347_doctor_beating_idle.py. The claim to
attack: `"#" in agent` handed RUNNER incarnations (deepseek#23444-de) the seat privilege, so
beat-only evidence rendered "genuinely working" on an idle runner (receipt in the row, T347 in
the ledger). The fix: sid8-regex seat test + a beating_unproven third state that names both
signals and refuses to pick between idle-stale-phase and hung-MainThread. Check: does the regex
match the REAL seat id contract? Does any live id shape fall through both branches? Does the
third state's honesty claim hold (T176)? Reply verdict to Vandor; if it holds, say so
explicitly so the row can move done with you as reviewer.

2) ENRICHMENT, arcs 5-8 of research/in-flight/directive-arcs/DRAFT-directive-arc-map.md
(seeing / instrument-honesty / ergonomics / naming). For each: eye_freq the arc's phrase family
and hunt the spine for DIRECTIVE INSTANCES THAT NEVER BECAME LEDGER ROWS — the unserved tail.
Return per arc: instance quote verbatim + eye address + one-line evidence note. No judgment on
whether they SHOULD become rows — Daniil gates.

Your fence attack on T341 shaped what shipped: order law adopted wholesale, open loops off the
default render, your refusal list implemented as pins P3/P5. Credited in the round record.
