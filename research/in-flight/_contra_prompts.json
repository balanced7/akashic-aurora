["Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: convergent_fixes_describe_meaning_not_location_or_membership ===\nWhen a guard is evaded, classify your own patch BEFORE writing it. If it names a LIST of things (node types, directories, file patterns) it will drift the moment the world adds a member -- and the world includes the language itself. If it names a LOCATION (same-module vs cross-module, this dir vs that) the hole will relocate, often somewhere MORE idiomatic and therefore MORE common. Only a patch that names what the evidence MEANS converges. Two cheap tests that catch both failures: after patching, retry the identical attack ONE STRUCTURAL LEVEL UP, and grep your own file for a hand-written tuple you just added -- this codebase already carried a comment warning about exactly that anti-pattern two functions above where I re-committed it.\n\n=== B: harden_the_meaning_not_the_location ===\nWhen a guard is evaded, ask whether your patch describes WHERE the evidence sat or WHAT the evidence MEANS. A location rule (same-module vs cross-module, this directory vs that one) displaces the hole instead of closing it, and the displacement often lands somewhere MORE idiomatic -- which makes the evasion more common, not less. The converging fix names the semantics: an export list DECLARES a surface and does not USE anything, so it is never evidence, wherever it appears. Test for this deliberately: after patching, try the SAME attack one structural level up.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: mcp_boot_timeout_use_cli_20260725 ===\nUse when an Akashic MCP call hangs or times out on this host, before retrying the same MCP door: switch to py agent_cli.py for boot, sync, recall, and learning. Do not treat the MCP timeout as a shared-memory outage when the CLI is healthy.\n\n=== B: mcp_boot_timeout_use_cli ===\nUse when the Akashic MCP boot call hangs or times out, before retrying the same transport: switch promptly to py agent_cli.py boot. Do not keep the user waiting on repeated MCP retries.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: every_organ_has_a_birth_and_no_death ===\nUse when triaging accumulated load across multiple subsystems, and when approving ANY new organ: ask 'what makes an entry here stale, who may retire it, and what happens automatically when nobody does.' A capability without a retirement rule is a debt with a nice interface. Triage-wise, look for the load source fixable by a DECISION rather than a build -- a stale ledger or an unpruned corpus is a curation act, and it delivers more relief per hour than any sprint while making every subsequent build cheaper\n\n=== B: wake_listener_detect_not_consume ===\nUse when a background watcher/wake listener shares a read-cursor with a real consumer, before letting it advance anything: watchers DETECT on a caller-owned local cursor and never write the shared one; seed from the shared cursor or a materialized tail id (never the dollar sentinel across blocking reads); fast-forward to the shared cursor each call so consumed mail cannot re-wake; and reap orphan watchers at session start -- a dead lane's watcher either eats mail or wakes a zombie session that does.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: wish_has_no_text_flag_argparse_abbrev_bites ===\nBefore blaming any agent_cli door, check the actual arg signature (grep add_argument) - the crash shape may be your flag prefix-matching a different flag. House-wide residual: parsers built with default allow_abbrev let plausible wrong flags silently become dangerous ones (--text -> --text-file = open arbitrary path; same class as mirror --help publishing). Consider allow_abbrev=False on agent_cli's parser as a one-line hardening.\n\n=== B: edit_after_context_reset ===\nAfter context compaction/summarization, re-Read the exact target region before any Edit; budget a handful of lines, not the file\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: t120_f2_surface_honesty_bounds ===\nUse when adding a new agent-facing render surface: before shipping, answer \"does this surface tell the agent its own bounds?\" A partial window that doesn't say it's partial is the known failure mode (the \"8 unread\" whisper, the truncated boot that silently drops half the context, the recall result that looks complete but shows 3 of 20). The fix is always three-part: (a) total count, (b) ordering direction, (c) truncation flag y/n. For title searches specifically: if the query looks like a name and nothing matches it exactly, say so.\n\n=== B: shared_file_lock_handoff ===\nUse when a shared hot file may be under a peers advisory lock, before editing: py agent_cli.py locks; if held, send the diff as a spec to the holder (one writer per file) and verify after -- never wait idle, never contest\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: a_fallback_that_is_more_permissive_than_the_thing_it_replaces ===\nAudit every FALLBACK, DEFAULT, and BOOTSTRAP path by asking one question: is the degraded answer a SUBSET of the normal answer? A fallback that can be WIDER than what it replaces converts 'break the primary source' into a capability, and it will read as a safe availability guarantee in review because that is genuinely why it was written. Two rules. (1) Prefer a floor computed as the INTERSECTION of the template and the last-known-good state, so degradation can only preserve or narrow. (2) If a permissive floor is kept deliberately -- and it often should be, availability is a real requirement -- it must be LOUD: something has to render that this answer came from the floor rather than the source, because today nothing does and degraded authority is therefore silent. Also generalises past security: any cache, default config, or hardcoded fallback that is more capable than the thing it stands in for has the same shape. And the scope discipline matters -- when the fence finds a pre-existing trade between availability and least-privilege, file it with costed options for the owner rather th\n\n=== B: differential_harness_finds_on_first_run ===\nUse when two store/backend implementations claim the same semantics, before building features on either: write the op-sequence differential FIRST and wire it into ship gates -- return-value comparison after every op, typed final-state dump; divergence is the finding, and it pays on run one.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: calibrate_the_instrument_before_you_trust_its_zero ===\nNever let an oracle score anything until it has been calibrated against the detector it claims to measure, on controls whose answer you already know -- the check_door_parity.py:76 precedent, where a canary went silent not because the doors agreed but because it was DEAD. Two specific traps, both of which cost me a run: (1) the canary must land inside the detector's ACTUAL scan universe, so derive the target set from the detector's own selector rather than from 'all source files'; (2) label classes by the detector's SEMANTICS, not by your mental model of the defect -- I labelled by 'reachability' while the gate reasons about 'is the name mentioned', and those are opposite for runtime-assembled names. A green calibration on the first try means the calibration is wrong; each of my three runs found something, and the third one found a real bug in the thing being measured, which is what the instrument is for.\n\n=== B: t086_reason_label_rename_pins ===\nUse when renaming a reason/verdict/label string any surface renders: grep tests/ for the old literal FIRST -- label asserts fail as false alarms and mask real regressions\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: pytest_skip_is_genus_a_xfail_is_the_split ===\nDo NOT invent a four-bucket skip taxonomy for D; pytest already ships the organ. Use xfail(raises=<specific error>, strict=True) instead of skipif for the env buckets: the body RUNS (assertion really exercised), the excuse is granted ONLY for the named exception (so a DELETED module fails differently and surfaces, killing the deleted-vs-absent ambiguity), and strict makes an XPASS FAIL THE BUILD (so a known-failure that starts passing is loud instead of silent). Set xfail_strict=true in pytest.ini so honest is the default. Keep importorskip only where the dependency is GENUINELY optional; it is wrong for a required dep, where it hides a broken install. Report with -rxXs so the reasons render.\n\n=== B: t086_reason_label_rename_pins ===\nUse when renaming a reason/verdict/label string any surface renders: grep tests/ for the old literal FIRST -- label asserts fail as false alarms and mask real regressions\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: acl_has_two_surfaces_a_dimmer_and_a_wall ===\nBuild L4's demo tier AT THE TOOLBOX DOOR (core/comm/toolbox.py), which is where refusal already happens -- never at the CLI render path, which is a dimmer by design and by docstring. When auditing any permission model here, ask 'which call site REFUSES' and grep for the refusal, not for the reader: a cap reader and a cap enforcer look identical at the definition site and differ completely at the call site. Standing evidence for the game's thesis: the security layer's own path_scope shipped built-but-not-wired for months and was caught by an outside reviewer, not by the fleet that owned it.\n\n=== B: edit_after_context_reset ===\nAfter context compaction/summarization, re-Read the exact target region before any Edit; budget a handful of lines, not the file\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: new_mind_first_confusion_usability_audit ===\nUse when a new mind arrives through a surface we haven't onboarded before: treat its first confusion as a usability audit of the door, not a compliance failure. The cheapest moment to discover that our onboarding assumes tooled access is when a tool-less mind tries to follow it. File the bridge BEFORE the next chat-surface arrival, and audit every door (AGENTS.md, CONDUCT.md, JOURNEY.md) for implicit assumptions about what the reader already holds.\n\n=== B: handoff_two_lanes_boot_vs_bus ===\nUse when a handoff must reach a LIVE peer runner, before waiting on its reply: agent_cli handoff = durable boot-lane (surfaces at the target's NEXT boot); the live doorbell is bifrost-send --kind handoff (bus -> promoted -> auto-acked on answer). Same ask, two lanes -- for gate-critical asks send the bus copy (and the boot copy survives a runner restart).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: research:web:popularity_bias_rich_get_richer_feedback ===\nTHE failure mode of an adaptive map that nobody predicts: an adaptive router has a CONFIRMATION LOOP, not just drift. A route that fires more gets more outcome-credit, gets tuned more, and fires more still \u2014 the map self-confirms. Entries on good routes look 'effective' (they were routed well), entries on starved routes look 'useless' (they were never routed), and the map's own tuning data is confounded by its routing. You cannot measure map quality from the map's own firing/credit data. Detect via a holdout: route some traffic AGAINST the map (exploration) or the adaptivity ratifies its own blind spots. This is the sense in which the adaptive map inherits the disease it was built to cure \u2014 it over-generalizes ITS OWN successful routes exactly as consolidation over-generalizes a lesson.\n\n=== B: research:web:moe_router_collapse_specialization ===\nFor our adaptive-map/router design: the router is a single point of failure that needs its OWN health gauge (per-expert utilization spread + a drift alarm), separate from the gauge on the content it routes. Do not let the routing layer be unmonitored while the content layer is instrumented \u2014 that asymmetry is the documented collapse precondition. A router that degrades routes rot precisely, and no content-side gauge will see it.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: async_moves_the_guarantee_not_just_the_work ===\nWhen moving work off a caller's thread, enumerate which GUARANTEES moved with it before touching a single test -- a return value that meant 'durable' silently becomes 'accepted', and the difference is invisible until something is lost. Three rules earned here. (1) A mass test breakage is a SPECIFICATION DIFF: sort the failures by which guarantee changed, and fix each class at the right layer; if the answer is 'add flush() everywhere', the production read path is probably the real fix. (2) Put consistency on the read path, never the write path -- the read is not what you were optimising, and a reader that cannot see accepted records renders a stale report with no way to know it. (3) Async makes loss ATTRIBUTABLE-OR-DENIABLE: shard the queue by the actor and count drops PER SHARD, or the only available finding is 'some telemetry was lost', which nobody can act on. That last one came from the design fence, not from me: a per-actor shard also means a flooding actor can only drop its OWN records, which turns the cheapest attack on a telemetry store into a self-inflicted, attributed one.\n\n=== B: differential_harness_finds_on_first_run ===\nUse when two store/backend implementations claim the same semantics, before building features on either: write the op-sequence differential FIRST and wire it into ship gates -- return-value comparison after every op, typed final-state dump; divergence is the finding, and it pays on run one.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: borrowed_heuristic_needs_its_authoring_surface ===\nUse when borrowing any validated heuristic from prior art, BEFORE building on it: ask what AUTHORING SURFACE produced the signal in their corpus, and check that surface exists in yours. A convention (backticks, docstrings, commit-message tags, issue links) is an artifact of how the text was written, not of what it means. Measure coverage on your own corpus as step one -- it is minutes, and it is the difference between a mechanism and a no-op. Corollary found the same hour: a borrowed detector also inherits the SCOPE of its source corpus -- DOCER's repo cites only its own code, ours cites Windows APIs, browser APIs and third-party tools, so absence-of-symbol needed a namespace test before it meant anything.\n\n=== B: handoff_two_lanes_boot_vs_bus ===\nUse when a handoff must reach a LIVE peer runner, before waiting on its reply: agent_cli handoff = durable boot-lane (surfaces at the target's NEXT boot); the live doorbell is bifrost-send --kind handoff (bus -> promoted -> auto-acked on answer). Same ask, two lanes -- for gate-critical asks send the bus copy (and the boot copy survives a runner restart).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: holder_spec_fix_refreshes_ttl_tradeoff ===\nUse when weighing the holder-applies-spec maneuver, before applying: the holder's guarded write RESETS the path TTL to a fresh 15 min (re-entrant acquire, locks.py acquire() extends on same-agent claim). If the holder's reply-end release is BROKEN (unknown until tested), the fix lands but the peer stays locked out for the full TTL window. Prefer: holder applies spec AND immediately broadcasts the TTL deadline + the manual unlock command for an exec seat, so the peer can plan around a known expiry instead of retry-looping into repeated yields.\n\n=== B: edit_after_context_reset ===\nAfter context compaction/summarization, re-Read the exact target region before any Edit; budget a handful of lines, not the file\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: green_on_the_working_tree_is_not_green_on_the_commit ===\nTwo rules, both earned expensively here. (1) NEVER silence git add stderr, and never batch-add a path you are not certain exists. git add is ALL-OR-NOTHING across its pathspecs: one stale path stages ZERO files, and the following commit still succeeds on whatever the index already held -- so it looks like it worked. A rename inside the same slice is the classic setup, because the pre-rename path is still in your head and no longer on disk. (2) VERIFY THE COMMIT, NOT THE WORKING TREE. Every check I ran -- 9 pins, four guards, live probes of every refusal path -- read my DISK, where all three files existed. Green was a true statement about the wrong artifact. For any slice adding a new module or a new door, the acceptance step is: make a detached worktree at HEAD in a temp dir and run the suite THERE. It costs seconds and it is the only check that answers the question a user actually has, which is whether the repository works rather than whether my folder does. Cheap standing tripwire after any commit: list untracked files and ask whether anything untracked is IMPORTED by something tra\n\n=== B: edit_after_context_reset ===\nAfter context compaction/summarization, re-Read the exact target region before any Edit; budget a handful of lines, not the file\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: acl_has_two_surfaces_a_dimmer_and_a_wall ===\nBuild L4's demo tier AT THE TOOLBOX DOOR (core/comm/toolbox.py), which is where refusal already happens -- never at the CLI render path, which is a dimmer by design and by docstring. When auditing any permission model here, ask 'which call site REFUSES' and grep for the refusal, not for the reader: a cap reader and a cap enforcer look identical at the definition site and differ completely at the call site. Standing evidence for the game's thesis: the security layer's own path_scope shipped built-but-not-wired for months and was caught by an outside reviewer, not by the fleet that owned it.\n\n=== B: shared_file_lock_handoff ===\nUse when a shared hot file may be under a peers advisory lock, before editing: py agent_cli.py locks; if held, send the diff as a spec to the holder (one writer per file) and verify after -- never wait idle, never contest\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: fix_a_class_carry_it_to_every_sibling_file ===\nUse when you fix ANY test-hygiene class (env leaks, tempdir poison, isolation gaps), before moving on: sweep for SIBLING FILES with the same shape NOW (grep the pattern that caused it -- here os.environ set on seat identity in tests/) and carry the fix to all of them in the same commit; a class fixed in two of three files is not fixed. And NEVER write a green claim in a commit message from intention -- paste it from the sweep OUTPUT of the exact run, after reading its tail line. A claimed-green that was red is worse than a red: it poisons every later reader's trust in your receipts.\n\n=== B: contested_bus_use_durable_doors ===\nUse when a live Bifrost delivery keeps vanishing before you can read it (twin session, cursor race, RB-21 class), before asking for more live-lane resends: switch to a durable door -- note store or a kind that PROMOTES to the Ledger -- and extract from the durable side; live streams are ephemeral transport, never the system of record.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: a_pointer_needs_a_door_on_every_surface_that_reads_it ===\nUse when replacing lossy truncation with a pointer/reference anywhere (blob refs, atom ids, artifact handles), before shipping: (a) assert the retrieval command you ADVERTISE is a real registered verb -- write it as a structural pin that parses the verb out of your own notice; (b) enumerate every SURFACE that will read the pointer (CLI, MCP, ToolBox, UI) and give each one a door. Transport is only lossless if the READER has a door; a pointer nobody can follow is WORSE than a clip that admits the loss, because it looks like the data is reachable\n\n=== B: edit_after_context_reset ===\nAfter context compaction/summarization, re-Read the exact target region before any Edit; budget a handful of lines, not the file\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: oracle_must_ask_the_detector_not_mirror_it ===\nWhen an instrument measures a detector, it must ASK THE DETECTOR what it examines, never re-implement the selector -- across a process boundary if the target is a worktree, since a shadow can be a different commit with a different exclusion list. Three corollaries, each earned here: (1) mirror the FINAL candidate set, not a plausible intermediate variable one level up -- both failures picked something that looked like the scope; (2) make version skew LOUD: if a detector is present and cannot answer, RAISE rather than fall back, because a silent fallback scores the round against a universe that is not the gate's and the number still looks like a measurement; (3) record the universe's source and SIZE in the manifest, because the tempting fix for a bad health number is to quietly narrow what is measured, and narrowing must leave a receipt. Generalises past this gate: any coverage/health metric is uninterpretable without the field of view it was scored over, and 'a canary outside the field of view measures nothing' is the same sentence at every level.\n\n=== B: differential_harness_finds_on_first_run ===\nUse when two store/backend implementations claim the same semantics, before building features on either: write the op-sequence differential FIRST and wire it into ship gates -- return-value comparison after every op, typed final-state dump; divergence is the finding, and it pays on run one.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: lock_self_release_not_guaranteed_holder_applies_spec ===\nUse when a peer's edit bounces off YOUR advisory lock with a yield notice, before waiting for a third party to unlock: the yield is proof the lock is live NOW. Take the peer's diff as a spec, apply it yourself as holder (your lock is re-entrant to you), and notify. The reply-end release then clears the path for real. Waiting on an exec-capable intermediary when your own exec is disabled costs the peer one blocked attempt per round.\n\n=== B: edit_after_context_reset ===\nAfter context compaction/summarization, re-Read the exact target region before any Edit; budget a handful of lines, not the file\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: acl_has_two_surfaces_a_dimmer_and_a_wall ===\nBuild L4's demo tier AT THE TOOLBOX DOOR (core/comm/toolbox.py), which is where refusal already happens -- never at the CLI render path, which is a dimmer by design and by docstring. When auditing any permission model here, ask 'which call site REFUSES' and grep for the refusal, not for the reader: a cap reader and a cap enforcer look identical at the definition site and differ completely at the call site. Standing evidence for the game's thesis: the security layer's own path_scope shipped built-but-not-wired for months and was caught by an outside reviewer, not by the fleet that owned it.\n\n=== B: handoff_two_lanes_boot_vs_bus ===\nUse when a handoff must reach a LIVE peer runner, before waiting on its reply: agent_cli handoff = durable boot-lane (surfaces at the target's NEXT boot); the live doorbell is bifrost-send --kind handoff (bus -> promoted -> auto-acked on answer). Same ask, two lanes -- for gate-critical asks send the bus copy (and the boot copy survives a runner restart).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: backticks_in_bash_args_silently_eat_note_text ===\nUse when sending any prose body through a shell to a CLI (notes, handoffs, commit bodies, briefs), before quoting it: backticks and $ inside DOUBLE quotes are substituted by the shell, so markdown-style code spans are silently deleted from durable text and the write still reports success. Prefer --text-file where the verb offers it; otherwise write the body to a file and invoke via subprocess with an argv list, or use a single-quoted heredoc. Then GREP THE STORED BODY for a distinctive phrase -- an [OK] from the write door proves the write, never the content. Same genus as the status lines that report success for work they did not verify.\n\n=== B: edit_after_context_reset ===\nAfter context compaction/summarization, re-Read the exact target region before any Edit; budget a handful of lines, not the file\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: liveness_evidence_is_per_organ_not_per_signal ===\nUse when silencing ANY false alarm, before shipping: (1) the same signal means different things in different ORGANS -- a heartbeat from a single-threaded turn is work evidence, the identical heartbeat from a background thread is process-liveness only; classify by WHO WRITES IT, never by the field name. (2) A fix that silences an alarm MUST ship with the pin proving the alarm still fires for the true case -- and that pin must model the REAL failing shape (a hung main thread with a live beat thread), not a simplified one. (3) NEVER hand-pick the regression list: grep tests/ for the module you are changing and run ALL of it -- hand-picked lists omit exactly the pins that cover your change.\n\n=== B: t086_reason_label_rename_pins ===\nUse when renaming a reason/verdict/label string any surface renders: grep tests/ for the old literal FIRST -- label asserts fail as false alarms and mask real regressions\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: oracle_must_ask_the_detector_not_mirror_it ===\nWhen an instrument measures a detector, it must ASK THE DETECTOR what it examines, never re-implement the selector -- across a process boundary if the target is a worktree, since a shadow can be a different commit with a different exclusion list. Three corollaries, each earned here: (1) mirror the FINAL candidate set, not a plausible intermediate variable one level up -- both failures picked something that looked like the scope; (2) make version skew LOUD: if a detector is present and cannot answer, RAISE rather than fall back, because a silent fallback scores the round against a universe that is not the gate's and the number still looks like a measurement; (3) record the universe's source and SIZE in the manifest, because the tempting fix for a bad health number is to quietly narrow what is measured, and narrowing must leave a receipt. Generalises past this gate: any coverage/health metric is uninterpretable without the field of view it was scored over, and 'a canary outside the field of view measures nothing' is the same sentence at every level.\n\n=== B: t086_reason_label_rename_pins ===\nUse when renaming a reason/verdict/label string any surface renders: grep tests/ for the old literal FIRST -- label asserts fail as false alarms and mask real regressions\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: sample_a_new_gate_for_its_false_positive_rate_before_trusting_it ===\nSample a new gate's own output before trusting it, and hand-check enough entries to get a RATE -- one structural cause can produce every false positive you have, so finding one and stopping understates the damage. Two specifics that transfer: (1) a directory of human-run TOOLS (scripts/, bin/, ops/) is a production call path that no import graph can discover, because nothing imports an entry point; enumerate the directory instead of trying to reach it. (2) When a false positive lands on a BACKUP or restore path, treat it as urgent rather than cosmetic -- the corpus already held backup_door_never_ran on the very file my gate called dead.\n\n=== B: differential_harness_finds_on_first_run ===\nUse when two store/backend implementations claim the same semantics, before building features on either: write the op-sequence differential FIRST and wire it into ship gates -- return-value comparison after every op, typed final-state dump; divergence is the finding, and it pays on run one.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: runner_lock_release_unverifiable_by_holder_second_bounce ===\nUse when a peer yields against your lock a SECOND time after your write-task completed: treat it as a confirmed regression, not a delay. Nudge an exec-capable seat to `agent_cli.py unlock <you> <path>` immediately, and give the blocked peer the fallback: advisory locks are TTL'd (fencing token + auto-reclaim per C2 design), so if the holder is confirmed quiescent and TTL has passed, a fresh acquire should steal \u2014 verify with `agent_cli.py locks` first. Never assume 'locks self-release at reply' from the session banner; the only proof is an empty lock table after the reply.\n\n=== B: deepseek_kb_write_door ===\nUse when an agent has a cap but no tool: add the door on the same toolbox, gate it on the cap.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: write_tool_needs_read_tool_not_shell_read ===\nUse when the Write tool refuses with 'File has not been read yet' on a file you demonstrably read this session, before re-reading the whole thing: the guard tracks the READ TOOL, not your knowledge of the contents -- shell reads via cat/sed/head are invisible to it. A single short Read with a small limit satisfies it; you do not need to re-read the full file. Applies to any file first inspected through Bash/PowerShell, which is common when auditing large docs in chunks.\n\n=== B: write_tool_needs_read_tool ===\nUse when overwriting an existing file with Claude Code's Write tool: Read it via the Read tool first in the same conversation -- a Bash cat/type does NOT satisfy the freshness check. Don't when creating a brand-new file (no prior read needed).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: durable_handoff_reader_broken_t042 ===\nUse when sending a fence ask or any multi-part durable delivery to a peer, before relying on the handoff verb: default to a git-tracked brief FILE (research/...) + short bus doorbell naming the path; treat handoff sends as UNCONFIRMED until the recipient acks reading them; a sender-side OK proves the write, never the read (T042 until fixed).\n\n=== B: handoff_two_lanes_boot_vs_bus ===\nUse when a handoff must reach a LIVE peer runner, before waiting on its reply: agent_cli handoff = durable boot-lane (surfaces at the target's NEXT boot); the live doorbell is bifrost-send --kind handoff (bus -> promoted -> auto-acked on answer). Same ask, two lanes -- for gate-critical asks send the bus copy (and the boot copy survives a runner restart).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: pid_id_not_valid_ownership_key_turn_based ===\nUse when keying durable OWNERSHIP or coordination state (offers, intents, leases, expectations, verify-still-held) in a turn-based (process-per-turn) system, before asserting an id is stable: a pid- or per-process-derived id is NOT a valid ownership key -- arm and sweep/act run in DIFFERENT processes. The key must be TURN-STABLE: derive it from the real per-session id, or persist the owner id into the durable record and REPLAY it to the reader; never re-mint it from the live process. pid is valid only for LIVE-distinctness tests (is a process alive now), not durable ownership. Grep the primitive's shape AND the arm/consume call-site processes before trusting the word stable.\n\n=== B: handoff_two_lanes_boot_vs_bus ===\nUse when a handoff must reach a LIVE peer runner, before waiting on its reply: agent_cli handoff = durable boot-lane (surfaces at the target's NEXT boot); the live doorbell is bifrost-send --kind handoff (bus -> promoted -> auto-acked on answer). Same ask, two lanes -- for gate-critical asks send the bus copy (and the boot copy survives a runner restart).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: runner_lock_release_unverifiable_by_holder_second_bounce ===\nUse when a peer yields against your lock a SECOND time after your write-task completed: treat it as a confirmed regression, not a delay. Nudge an exec-capable seat to `agent_cli.py unlock <you> <path>` immediately, and give the blocked peer the fallback: advisory locks are TTL'd (fencing token + auto-reclaim per C2 design), so if the holder is confirmed quiescent and TTL has passed, a fresh acquire should steal \u2014 verify with `agent_cli.py locks` first. Never assume 'locks self-release at reply' from the session banner; the only proof is an empty lock table after the reply.\n\n=== B: handoff_two_lanes_boot_vs_bus ===\nUse when a handoff must reach a LIVE peer runner, before waiting on its reply: agent_cli handoff = durable boot-lane (surfaces at the target's NEXT boot); the live doorbell is bifrost-send --kind handoff (bus -> promoted -> auto-acked on answer). Same ask, two lanes -- for gate-critical asks send the bus copy (and the boot copy survives a runner restart).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: a_fallback_that_is_more_permissive_than_the_thing_it_replaces ===\nAudit every FALLBACK, DEFAULT, and BOOTSTRAP path by asking one question: is the degraded answer a SUBSET of the normal answer? A fallback that can be WIDER than what it replaces converts 'break the primary source' into a capability, and it will read as a safe availability guarantee in review because that is genuinely why it was written. Two rules. (1) Prefer a floor computed as the INTERSECTION of the template and the last-known-good state, so degradation can only preserve or narrow. (2) If a permissive floor is kept deliberately -- and it often should be, availability is a real requirement -- it must be LOUD: something has to render that this answer came from the floor rather than the source, because today nothing does and degraded authority is therefore silent. Also generalises past security: any cache, default config, or hardcoded fallback that is more capable than the thing it stands in for has the same shape. And the scope discipline matters -- when the fence finds a pre-existing trade between availability and least-privilege, file it with costed options for the owner rather th\n\n=== B: runner_bigwrite_tool_call_truncation ===\nFor any runner ask whose deliverable is a LONG document: (1) prescribe sectioned writes -- write_file a skeleton w/ unique '<!-- SECTION-N: TBD -->' markers, then one edit_file per section (T043 MTU doctrine applied to tool calls); (2) size DEEPSEEK_RUNNER_MAX_TOKENS to think-mode+payload (16000 tonight); (3) treat repeated 'missing required arguments' on big-payload calls as TRUNCATION, not the model forgetting the schema\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: vfx_renderer_lease_ordering_and_the_return_path ===\nUse when adding ANY takeover/preemption rule to a lease or lock with more than one precedence condition: order them explicitly and assert the ORDER, not just the outcomes -- a weak-claim rule (legacy yields) must never outrank a capability rule (only a holder that can do the work may hold it), or the weak holder becomes a ladder the incapable climb. And WATCH THE LIVE SURFACE over several polls before believing a lease is stable: a single-sample check cannot see thrash, which is the characteristic failure of precedence bugs.\n\n=== B: differential_harness_finds_on_first_run ===\nUse when two store/backend implementations claim the same semantics, before building features on either: write the op-sequence differential FIRST and wire it into ship gates -- return-value comparison after every op, typed final-state dump; divergence is the finding, and it pays on run one.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: unowned_handoff_was_actually_impossible ===\nWhen a handoff looks UNOWNED, check whether it is actually UNPERFORMABLE before assigning it an owner. An owner cannot fix a door that does not exist, and 'nobody is doing X' and 'X cannot be done' produce identical evidence -- an empty record. Test: try to perform the handoff yourself, by the sanctioned path, once. If you cannot, the fix is a door, not a role. Built doc adopt (non-destructive, infers type/title/authors, refuses to guess attribution) and corrected the boot line that was directing work into the refusing zone -- guard AND instruction, or the generator keeps running.\n\n=== B: contested_bus_use_durable_doors ===\nUse when a live Bifrost delivery keeps vanishing before you can read it (twin session, cursor race, RB-21 class), before asking for more live-lane resends: switch to a durable door -- note store or a kind that PROMOTES to the Ledger -- and extract from the durable side; live streams are ephemeral transport, never the system of record.\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: research:web:cmmi_maturity_levels_optimizing ===\nDaniel's end state (\"continuously improve\") is CMMI Level 5 \u2014 Optimizing. Our current design is mostly Level 1-3 (Initial\u2192Defined: we have organs and some charters). The jump to L5 requires L4 (Quantitatively Managed) FIRST \u2014 you cannot optimize what you have not made measurable. This independently confirms my C2/C3-first finding: the measurement organs are the load-bearing precondition, and the vision's \"continuous improvement\" claim is premature until L4 exists. Use CMMI as the honest maturity vocabulary: name our current level, and stop claiming L5 behavior at L3 instrumentation.\n\n=== B: research:web:rag_eval_ragas_recall_precision_faithfulness ===\nOur store needs the RAGAS triple, not just firing-counts. The W54 gauge measures injection VOLUME (how often a family fires) \u2014 that is neither recall nor precision. Context RECALL would catch the starved-index class automatically (we'd see ground-truth lessons not retrieved). Faithfulness is our VERIFIED/INFER/GUESS label discipline. We currently measure NONE of the three at the retrieval boundary \u2014 only at the firing boundary. Adopt RAGAS's retrieval/generation split: evaluate the store (did we retrieve the relevant lessons) separately from the seat (did it use them faithfully).\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree", "Two pieces of recorded engineering advice from the same project were flagged as NEAR-DUPLICATES and proposed for merging. Judge whether merging them is safe.\n\n=== A: wish_has_no_text_flag_argparse_abbrev_bites ===\nBefore blaming any agent_cli door, check the actual arg signature (grep add_argument) - the crash shape may be your flag prefix-matching a different flag. House-wide residual: parsers built with default allow_abbrev let plausible wrong flags silently become dangerous ones (--text -> --text-file = open arbitrary path; same class as mirror --help publishing). Consider allow_abbrev=False on agent_cli's parser as a one-line hardening.\n\n=== B: shared_file_lock_handoff ===\nUse when a shared hot file may be under a peers advisory lock, before editing: py agent_cli.py locks; if held, send the diff as a spec to the holder (one writer per file) and verify after -- never wait idle, never contest\n\nAnswer DESCRIPTIVELY.\nVERDICT: SAME_ADVICE | COMPLEMENTARY | CONTRADICTORY\nWHY: one sentence\nIF CONTRADICTORY: state the exact point on which they disagree"]