# T118 ROSTER PROPOSAL — per-family authority for the durable reconcile

Status: current | 2026-07-28 evening | claude (builder lane), for Daniel's gate; codex counter invited
Evidence: two-sided read-only census (redis 22,303 keys / file 2,187), run post-a750728.
Ruling vocabulary: **redis** = redis-authoritative, additive copy into durable source (escrow on divergence) ·
**file** = durable side already owns it, no pull · **ephemeral** = no durable afterlife (packet_spec roster) ·
**sweep** = drill trash, one audited TTL-sweep, not a roster entry.

| group | redis | file | proposed ruling | receipt / note |
|---|---|---|---|---|
| artifact | 1112 | 1103 | **redis**, additive | T101 write-once atoms + cited-by indexes; 9-key delta = fresh mints. Divergence on a write-once atom = defect signal — escrow will catch it. |
| events | 161 | 855 | **file** (whole group) | file-ahead 5:1; extends the already-rostered events:raw ruling. |
| bifrost:idalias | 740 | 0 | **ephemeral** (extend packet_spec) | T117 dual-id alias records — transport metadata that evaded the ephemeral roster. packet_spec edit, cite this census. |
| narr:beat | (12) | ✓ | **file** (already rostered) | codex census: JSON-only 12. |
| narr:chapter, narr:atlas | 310 | 104 | **redis**, additive — FLAG for fence | redis-ahead 3:1, but mixed-authority inside one namespace deserves a counter before apply. |
| mem | 362 | 24 | **redis**, additive | AgentMemory decisions/heads — the ADR notes. Same genus as lessons (durable-critical, redis-live). Needs list/kv type support in reconcile (currently hash+kv) — small extension, pinned. |
| embed | 178 | 0 | **ephemeral** | regenerable projection cache (model:sha embedding vectors); recompute on miss. A cache with a durable afterlife is just stale twice. |
| learn (beyond learn:experiment) | 109 | 43 | **redis**, additive | category indexes + experiments:all list — projections, but operationally load-bearing (the 485-list clobber scar). List-type support required (same extension as mem). |
| recall | 2 | 56 | **file** (whole group) | file-ahead; extends recall:use. |
| lookback, context, flow_trace, knowledge_map | 14 | 2 | **ephemeral** | live telemetry counters + coordination state; regenerated by use. |
| codex:resource | 2 | 0 | **redis**, additive | codex door resources; two keys, cheap to keep durable. |
| t-*, t056_*, t117dbg, census_test | ~180 | 0 | **sweep** | drill-namespace singletons; one audited TTL-sweep slice (test namespaces get TTLs at mint — separate small fix). |

## The three calls that genuinely need eyes
1. **narr mixed authority** (beat=file, chapter/atlas=redis) — the one family split by sub-namespace; wants an adversarial look before --apply.
2. **embed + telemetry as ephemeral** — ruling says "regenerable ≠ durable"; if anyone knows a consumer that assumes durability, speak now.
3. **artifact as redis-authoritative** — write-once atoms should never diverge; if escrow catches ANY displaced artifact variant at apply time, that is a stop-and-investigate, not a shrug.

## Mechanical follow-ons this proposal implies (all small, all pinned)
- reconcile ROSTER: add the rulings above; extend type support to list/kv-per-entry (mem, learn indexes).
- packet_spec ephemeral roster: + bifrost:idalias (+ audit why 740 evaded it).
- drill-trash sweep slice: TTL at mint for t-* namespaces + one-time audited cleanup.

Gate: Daniel rules the table (edit rulings inline or approve); codex counter folds in first if it lands.
The --apply itself still waits for the QUIESCE moment regardless of this gate.
