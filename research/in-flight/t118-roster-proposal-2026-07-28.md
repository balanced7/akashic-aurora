# T118 ROSTER PROPOSAL — per-family authority for the durable reconcile

Status: current | 2026-07-28 evening | claude (builder lane), for Daniel's gate; codex counter invited
Evidence: two-sided read-only census (redis 22,303 keys / file 2,187), run post-a750728.
Ruling vocabulary: **redis** = redis-authoritative, additive copy into durable source (escrow on divergence) ·
**file** = durable side already owns it, no pull · **ephemeral** = no durable afterlife (packet_spec roster) ·
**sweep** = drill trash, one audited TTL-sweep, not a roster entry.

| group | redis | file | proposed ruling | receipt / note |
|---|---|---|---|---|
| artifact | 1112 | 1103 | **redis**, additive | T101 write-once atoms + cited-by indexes; 9-key delta = fresh mints. Divergence on a write-once atom = defect signal — escrow will catch it. |
| events | 161 | 855 | **file** (whole group) | file-ahead 5:1; extends the already-rostered events:raw ruling. |
| bifrost:idalias | 740 | 0 | **ephemeral** (extend packet_spec) | T117 dual-id alias records — transport metadata that evaded the ephemeral roster. packet_spec edit, cite this census. |
| narr (whole family) | 310 | 104+12 | **union-additive**, tie-break to redis w/ escrow (REFRAMED at Daniel's steer 2026-07-28: "Can we address the shortcomings of both or split the responsibility rather than deciding which to trust") | The additive reconcile IS a union: file keeps everything it holds (immune to redis trims), redis extras get pulled in -- which RESCUES chapters the old FileStore clobbered and lost. Authority only breaks ties on same-key-different-bytes twins (live side wins, file variant escrowed, nothing destroyed). Uniform with every other redis-additive family -- the sub-namespace split this row previously proposed was needless caution. Supersedes the separate narr:beat row. |
| mem | 362 | 24 | **redis**, additive | AgentMemory decisions/heads — the ADR notes. Same genus as lessons (durable-critical, redis-live). Needs list/kv type support in reconcile (currently hash+kv) — small extension, pinned. |
| embed | 178 | 0 | **durable content-addressed cache** (AMENDED at Daniel's steer 2026-07-28: "Can we think of some lightweight way of compressing and saving embeddings? I feel it will be useful for our re-analysis walks down memory lane") | Keys are model:content_sha -- write-once by construction, can never go stale (changed text = new key). Archive as fp16-packed blobs (~halves size, similarity-neutral); quality pin: packed-vs-original cosine >= 0.999. Kills recompute-thrash on memory-lane walks. Follow-up slice: pack + migrate + pin. |
| learn (beyond learn:experiment) | 109 | 43 | **redis**, additive | category indexes + experiments:all list — projections, but operationally load-bearing (the 485-list clobber scar). List-type support required (same extension as mem). |
| recall | 2 | 56 | **file** (whole group) | file-ahead; extends recall:use. |
| lookback, context, flow_trace, knowledge_map | 14 | 2 | **ephemeral** | live telemetry counters + coordination state; regenerated by use. |
| codex:resource | 2 | 0 | **redis**, additive | codex door resources; two keys, cheap to keep durable. |
| t-*, t056_*, t117dbg, census_test | ~180 | 0 | **sweep** | drill-namespace singletons; one audited TTL-sweep slice (test namespaces get TTLs at mint — separate small fix). |

## The flagged calls — state after Daniel's second pass (2026-07-28 evening)
1. **narr** — RESOLVED by reframe at Daniel's steer (see table row): union-additive with live-side tie-break + escrow, uniform with the other redis-additive families. Background kept for the record: the two backends fail differently (FileStore loses concurrent writes -- measured 65.6%; Redis trims/expires); union-additive rescues each side's survivors from the other side's failure mode. Codex's counter still welcome as a check on the reframe. FORWARD LAW (Daniel's framing, answered): post-cutover the responsibility split is permanent and by FUNCTION -- one durable archive that never forgets (WAL, verified), one fast cache that is allowed to (trims harmless once durable-first writes hold).
2. **embed** — RESOLVED per Daniel's steer: durable content-addressed compressed cache (see table row). Telemetry counters stay ephemeral (unchanged).
3. **artifact escrow-hit = stop-and-investigate** — APPROVED by Daniel verbatim: "I like your approach with the artifacts."

Daniel's framing question, answered into the record ("Can we make both be trustworthy with verification
and instant powerloss recovery mechanisms?"): that IS the destination state the cutover builds -- WAL
crash-safety at the single authority, one write door, bidirectional verifier runnable any time, the
dual-authority checker watching continuously. The roster is the one-time triage of the divergence that
already happened: verification proves the sides differ; only per-family history says which is right.
Long-run law: many verified COPIES, exactly one AUTHORITY per record.

## Mechanical follow-ons this proposal implies (all small, all pinned)
- reconcile ROSTER: add the rulings above; extend type support to list/kv-per-entry (mem, learn indexes).
- packet_spec ephemeral roster: + bifrost:idalias (+ audit why 740 evaded it).
- drill-trash sweep slice: TTL at mint for t-* namespaces + one-time audited cleanup.

Gate: Daniel rules the table (edit rulings inline or approve); codex counter folds in first if it lands.
The --apply itself still waits for the QUIESCE moment regardless of this gate.
