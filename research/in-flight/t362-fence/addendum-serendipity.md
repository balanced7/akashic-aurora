ADDENDUM TO ALL THREE BRIEFS — landed mid-fence-prep, 2026-08-18, DANIIL VERBATIM:

  "One of the things I am aiming for is for our query results to be so powerful and useful that
   you can tangially uncover something you may have been looking for or that would be useful /
   rich without directly searching for it. I think this is achievable with our existing tooling
   and can be enhanced by further design."

This is now a FIRST-CLASS DESIGN AXIS of the fence. Resolution is not just dereference: a
resolved token should carry its RICH NEIGHBORHOOD. The house already holds shards of this law
unnamed: recall-at fires unsearched lessons at the point of action; eye get names utterance
siblings (T361); eye look renders neighbors-as-silhouettes + exits; routes save traversals;
reentry surfaces the open door. The fence should decide whether T362's resolve() envelope carries
a neighborhood section as CONTRACT (siblings, co-citations, same-arc rows, same-store adjacents)
— and each seat should answer ITS half:

  Heimdall: what does the neighborhood section COST at the store layer, and which adjacencies are
  mechanical (no meaning-judgment) so the envelope stays deterministic?
  Navi: attack it — serendipity vs noise. When does the neighborhood become spam that trains
  readers to skip it (the recall-silence lesson cuts both ways)? What is the noise guard?
  Sol: prior art — faceted search, "people also viewed", citation graphs, spreading activation.
  Which precedent fits a DETERMINISTIC house (no ranking model), and which failure do we copy
  by accident?

--- SEEDS FROM DANIIL, off-to-work 2026-08-18, VERBATIM ---

  "The few ideas I have for the search is perhaps we can automate part of the eye search for
   specific words by auto searching for synonyms and related words including partial matches.
   A way of searching what else was touched during a walk on a particular day, perhaps the
   frequency and direction of reference when working on a problem. I need to run to work right
   now but hopefully that helps some ^__^!!"

MAPPED TO EXISTING SUBSTRATES (search the right plane before designing — all three have organs):
  SEED 1 (auto synonym/partial expansion) -> a query-expansion layer over eye freq's existing
    multi-phrasing OR; freq already accepts hand-built families, this automates building them.
  SEED 2 (what else was touched during a walk, by day) -> a cross-plane JOIN that only became
    possible YESTERDAY: route walks are journaled with depth since T335 (his own ruling), plus
    git commits-by-day, plus eye events-by-day. Day -> walks + files + utterances.
  SEED 3 (frequency + direction of reference during a problem) -> the connectome: eye trace
    already walks upstream/downstream with edge provenance; his ask aggregates it over a
    problem's SPAN. Direction exists per-edge; nobody has ever summed it.
Each seat: cost/attack/precedent these three specifically, same halves as above.
