# Live sheet music: plan amendments

| | |
|---|---|
| Date | 2026-09-14 |
| Status | Binding for builders. **Where this file and `live-sheet-music-plan.md` disagree, this file wins.** The plan and `critic.md` are not edited. |
| Built from | `critic.md` corrections C1-C20 and section 3 (internal errors), checked against the plan, the scratch `quant.mjs` (metric and alphabet as actually run) and `arsenal/performance.py` (`KINDS`, `SOUND_END_BY`). |
| Labels | Plan labels (LS, LR, LQ) keep their meaning. New receipts continue the plan's numbering (LR2g, LR3d, LR4i-LR4k, LR8b-LR8d, LR11f-LR11g). "Start value" = not measured yet; the first dated run sets or confirms it. |
| Privacy | Benches over S1..Sn run locally; outputs go to `state/arsenal/score/` (git-ignored). Tracked files carry S-numbers and aggregates only. Fixtures are synthetic. |

---

## 0. Standing rules for LS0-LS5 (added)

1. **No VexFlow yet (LQ1 unanswered).** `score/engrave.js` v1 is our own drawer (the plan's `paintBarReduced` set, moved out of `paint.js`) behind an interface a VexFlow adapter can later implement unchanged:
   ```js
   // score/engrave.js
   export const ENGRAVER_API = "arsenal.piano.score.engrave/v0";
   export function createHouseEngraver(opts) -> {
     name: "house",
     ready(fonts) -> boolean,                     // false until the SMuFL face is loaded; engraveBar draws nothing while false
     measureBar(bar, geom) -> { minWidth, beatWidths: [] },   // pure; constant glyph boxes in staff spaces, no measureText
     engraveBar(ctx, bar, geom) -> { width, boxes: [{ kind, staff, x, y, w, h }], ms },
   }
   ```
   A later `score/engrave_vexflow.js` implements the same three methods (C9 rules apply there). LR11b and LR11e run on the house engraver now.
2. **Spelling is injected, never copied.** `spellForKey` stays in `piano.js` until LS6. Node tests build it read-only from `piano.js` source the way `tests/nashville_js.test.mjs` extracts the THEORY block (with a `theoryUi` stub for `minor`). The lab page uses `nashville.js` `spellInKey` for every tone and labels that as a lab approximation. No twin copy in score code.
3. **Voices before `hands.js`.** In LS2, `measures.js` takes an injected `voiceOf(note)`: truth voices on fixtures, a split at MIDI 60 on local benches. From LS3 it takes `hands.js` output.
4. **Lab page is replay-first (critic M8).** `piano-lab-score.html` takes `events.jsonl` replay as its primary input; live Web MIDI is a bonus. No LR11 receipt may depend on Web MIDI sharing across tabs.

---

## 1. Design corrections

### C1. Written durations under the pedal
**Changes:** plan 6.4 "Durations"; plan 12 "Decided by default" item 1 (pedalled notes as played, now filled under the pedal; mention to Daniel with LQ2, not a new question).

**Rule.** For each note: onset `t_on`, key release `t_off`, sound end `t_se` (the `sound_end` event; if missing, `t_off` when the pedal is up at release, else the next pedal up-crossing), next onset in the same voice `t_next` (chord members share `t_on`), `tol` = one 16th at the local beat (P/4 simple, P/6 compound).

1. **Pedal up from `t_on` to `t_next`:** the plan's rule is unchanged (release snapped forward within the division; legato fill when `t_next − t_off < tol`; staccato when held < 40% of the slot).
2. **Pedal down at release, or pressed between release and `t_next`:**
   - if `t_se ≥ t_next − tol`: written end = `min(t_next, bar end)`;
   - if `t_se < t_next − tol`: written end = `t_se` snapped to the nearest slot of the beat's division (never before onset + one slot), then a rest to `t_next`.
   - A pedal lift before `t_next` ends the sound, so the lift cap follows from `t_se`.
3. **Lowest voice of the lower staff:** C7 overrides.
4. **Shortest written value** = one slot of the beat's chosen division (was: a 16th).
5. **Staccato** only in case 1.

**Receipts.**
- LR4h (durations exact on fixtures): the LS0 generator gains the pedal model in section 7 LS0. The threshold is still set at the first measurement.
- **LR8b (new):** S1..Sn clean copies, pedalled bars (pedal down ≥ 50% of the bar): median rests per voice per bar **≤ 1**. Fixtures: rests per voice per bar reported against truth.

### C2. Rhythm alphabet
**Changes:** plan 6.4 cost formula, section 3 method, LS0 generator, LR4; plan 11 item 6 (sextuplets move from v2 into v1).

**Rule.** Per-beat divisions and simplicity costs (start values, tuned on LS0 fixtures in LS2 without changing the order):

| Beat | Set | λ |
|---|---|---|
| Simple meter (quarter = 24 ticks), feel straight | {1, 2, 3, 4, 6} | λ1 = 0, λ2 = 0.5, λ4 = 1.5, **λ6 = 1.75**, λ3 = 2.0 |
| Simple meter, feel triplet (C11) | {1, 2, 3, 4, 6} | λ1 = 0, λ3 = 0.5, λ6 = 1.5, λ2 = 1.75, λ4 = 2.0 |
| Compound meter (dotted quarter = 36 ticks) | {1, 3, 6, 2} | λ1 = 0, λ3 = 0.5, λ6 = 1.5, λ2 = 1.75 |

- **d = 8 (32nds, 3 ticks):** simple meters only (36/8 is not an integer). Behind option `div8`, **off** by default. LS2 turns it on for one suite run and keeps it on only if LR4a and LR4d each drop by ≤ 0.5 points and the 32nd fixture's onsets exact improves. Record the decision in section 5.
- Everything else in the cost is unchanged: fit term, 0.8 continuity, 0.8 bar-back, 6 per collision, σ₀ 40 ms, EMA 0.05, clamp 15-60 ms, f > 0.875 goes to the next beat, loose ceiling 12.
- **Loose beats** (5 or 7 groups that fit no division, or cost > 12) are drawn proportionally live, as before. Export: TPQ 24 cannot hold 5 or 7 tuplets, so the clean copy places groups in time order on the beat's d = 6 grid, minimizing total displacement. Distinct groups merge into `<chord/>` only when there are more than 6, closest pair first. Each such beat is flagged `loose: true` in `score.json`. Quintuplets wait for a TPQ change (v2).
- **Check on the critic's own caveat (LS1 bench, report only):** the share of S1..Sn beats with 5+ groups that sit where the tracker's ×2 family level is live. If it is high, the ×2 button is the fix, not d = 6.

**Receipts.**
- LR1: section 3 tables reproduce in `s3Compat` mode (DIVS {1, 2, 4, 3} for every meter, including 12/8, as `quant.mjs` ran).
- LR4a-e: thresholds unchanged, run with the amended alphabet on the section 3 suite. A drop below threshold is a failure, not a retune.
- **LR4j (new):** sextuplet-arpeggio suite. Onsets exact and bars onset-exact are set at the first measurement. False d = 6 on s3-suite beats whose truth is d ∈ {1, 2, 3, 4}, oracle beats: **≤ 1%** of non-empty beats (start value).
- LR5: threshold unchanged.

### C6. Tape collision push
**Changes:** plan 2.1 tape, 7.3 geometry, LR11a. Lands in LS5 (`layout.js`, `paint.js`).

**Rule.** One time axis for both staves; each onset group (a T1 chord or roll) is one column. Units: s = 15 px, v = 120 px/s.
- **Column width** w = **1.4 sp** (21 px), + **1.0 sp** (15 px) if any head in it has an accidental, + **1.18 sp** (17.7 px) if it holds a second. Across the two staves, the wider staff sets w.
- **Placement**, per new column i, with debt D (px right of nominal time):
  ```
  want = t_i·v + D_prev ;  min = x_prev + w_prev
  if want < min:  x_i = min                                           // push
  else:           x_i = want − min(D_prev, 0.5·(want − min))          // reclaim half the spare space
  D_i = x_i − t_i·v, clamped to [0, D_max]; if the clamp binds, x_i = t_i·v + D_max (overlap allowed and counted)
  ```
- **D_max** = min(**180 px** (1.5 s), right band edge − playhead − 2 columns). In 9:16 (band x 110-940, playhead at 70% = x 691), D_max = 180.
- **Placed columns never move.** Live and review use the same causal pass, so they match.
- **Ticks:** a faint tick every **1 s** at x = T·v + D(T), with D interpolated linearly between the neighbouring columns. Duration lines end at the warped x of their sound end.

**Receipts.**
- LR11a timings: unchanged.
- **LR11f (new):** overlapping noteheads per minute of tape. An overlap is two heads on the same staff in different columns whose boxes (1.18 × 1.0 sp, shrunk 0.1 sp per side) intersect.
  - Fixture: a 1.5 s run at 10 single-note groups/s gives **0**. A 4 s run at 10/s is reported.
  - S1..Sn replays: reported per session (S12 is the dense case); threshold set at the first measurement.

### C7. Held bass
**Changes:** plan 6.4 "held bass". Option `heldBass: true` stays the default.

**Rule.** For notes in the lowest voice of the lower staff:
- written end = min(`t_se`, next onset in that voice, end of the note's bar);
- **at most one bar-line tie**, only when the next bar has no onset in that voice before its midpoint (beat 3 in 4/4, dotted-quarter beat 3 in 12/8 (the 7th eighth), the second half-bar in 3/4 and 6/8);
- with the tie, the end is min(`t_se`, the next onset in that voice, the end of the next bar);
- the pedal line carries any remaining ring;
- ties inside a bar from the 6.5 beat-3 split do not count toward the one tie.

**Receipts.**
- LR4h fixtures include long pedalled bass (section 7 LS0).
- **LR8c (new):** bar-line ties per held-bass note ≤ 1 on every S1..Sn clean copy (100%, a construction check). LR8's "≤ 2 ties per bar" median is unchanged.

### C8. Accents
**Changes:** plan 6.5 "Dynamics", accents.

**Rule.**
- **v1 default `accents: "off"`**: no accent marks in live, clean or export.
- `accents: "voice"` (option, not the default): +**25** over the running median velocity of the last **16** notes in the **same voice**, at least **2** notes between accents in that voice, and never the top note of a chord unless the whole group clears +25.
- Turning `"voice"` on by default is a later decision, recorded in section 5 with its LR8d numbers.

**Receipts.** **LR8d (new):** accents per minute on S1..Sn: **0** with defaults; reported with `"voice"`.

### C11. Triplet feel under a simple meter
**Changes:** plan 6.3 (suggestions), 6.4 (λ table), LQ3 wording, section 3 (compound set).

**Rule.**
- **Feel is his setting:** `setOptions({ feel: "straight" | "triplet" })`, default `straight`, remembered per session. It swaps the simple-meter λ table (C2 table). It is ignored in compound meters.
- **Subdivision chip** in `meter.js`, from the tempo lane's grid test (`beat.js` exposes `subdivision: 1 | 2 | 3`): over the last **32** beats, grid 3 when thirds beat halves and exceed **25%** of on-beat salience.
  - "sounds like triplets?" when feel is straight and grid 3 holds for **8** consecutive decisions (one per second);
  - "sounds straight?" is the mirror;
  - no `steady` gate: the band must be `steady` or `loose`, not `free` or `hold`;
  - a chip only suggests; nothing changes until he accepts.
- **Clean pass:** `clean(events, { feel })` and CLI `--feel straight|triplet`. Default: the take's setting.

**Receipts.**
- **LR4k (new):**
  - 12/8 fixtures run under meter 4/4 with oracle beats at the dotted quarter: onsets exact and bars onset-exact reported for straight and for triplet. Triplet must beat straight; the margin is set at the first measurement.
  - 12/8 fixtures under 12/8 with the compound set {1, 3, 6, 2}: bars onset-exact **≥ 0.94**, the LR4a bar (section 3 reached 0.971 with the simple set).
- **LR3d (new, LS3):** the "triplets?" chip appears within 32 beats in **≥ 90%** of 12/8-under-4/4 fixtures, and in **≤ 5%** of straight 4/4 and 3/4 fixtures (start values).

---

## 2. Internal errors, restated

**TPQ rationale (plan 1, tick unit; C13, corrected again).**
- At TPQ 12, a 12/8 duplet eighth is 18 / 2 = 9 ticks, and a sextuplet 16th in a quarter is 12 / 6 = 2 ticks. **12 holds both.** The critic's line that 12 cannot hold sextuplets is itself wrong.
- 12 fails on 32nds (12 / 8 = 1.5) and on compound quadruplet 16ths (18 / 4 = 4.5).
- TPQ 24 holds every v1 value:
  - simple beat 24 → d 1, 2, 3, 4, 6, 8 = 24, 12, 8, 6, 4, 3;
  - compound beat 36 → d 1, 2, 3, 6 = 36, 18, 12, 6 (and quadruplet 9, 32nds 3).
- **Decision unchanged: TPQ 24.** The plan's "(9)" is the 12/8 quadruplet 16th, not the duplet.

**Timing spread to jitter rows (plan 3 point 2, 11 item 4, 13 point 2; C14).**
- The lanes' 35-55 ms is the spread of a **gap**. With independent per-note jitter, gap spread = √2·σ, so per-note σ ≈ **25-39 ms** (an upper bound: drift included).
- Interpolating the live, oracle-beat sweep (15 ms 99.7%, 30 ms 92.7%, 45 ms 65.0%) gives **≈ 95% at 25 ms and ≈ 76% at 39 ms** bars onset-exact against a click: about 1 wrong bar in 20 to 1 in 4, not "1 in 3 to 1 in 14" or "65-93%".
- This is a registered prediction for LR15.
- **LR15 (changed):** record the quantizer's adaptive σ per take. It is already per note, so no √2. Any comparison with a lane gap spread divides that spread by √2.

**What "bars exact" counts (plan 0, 3, LR4; C10).**
- `quant.mjs` `score()` marks a bar right when every onset **position** is right. Durations, rests, ties, voices, hands and spelling are not scored.
- Metric names from LS0 on:
  - `onsetsExact`;
  - `beatsExact` (same positions and same normalized division);
  - **`barsOnsetExact`** (the old "bars exact");
  - **`barsFullyExact`** (onsets, written durations, rests, ties and voice assignment all equal truth; reported from LR4h on, no threshold in LS2).
- LR4a-e read "bars onset-exact". Their numbers are unchanged.

**LR4f conditioning (C10).**
- **LR4f (restated):** *conditional* onset accuracy of the inferred rung, counted only inside tracker intervals whose ends match consecutive true beats within 70 ms. ≥ 0.95 [0.974]; coverage reported [0.443 all, 0.242 rub2]. It says nothing about wrong levels or phases.
- **LR4i (new): precision of shown rung-4 bars.**
  - Setup: fixtures with only rung 4 available and the meter set to truth.
  - Count: every bar the live view draws metric (LR4g gate on) that reaches settled.
  - A bar is correct only if both bar lines lie within 70 ms of true bar lines (so level ×2 / ×1.5 and phase errors fail) and every onset in it is exact.
  - Report precision (correct / shown) and the shown share (shown / all bars).
  - **Target ≥ 0.80.** If missed, raise the rung-4 metric gate (periodicity strength floor, now 4.5) in 0.5 steps until it holds, and record the new floor in section 5. Do not lower the target.

**Other wording fixes (no receipt change).**
- Plan 0: "inferred downbeats are right only 17% of the time" becomes "downbeat F-measure 0.17".
- Plan 0 (C12): add "without a jam loop, the song listener or taps, the live panel is mostly tape (steady 0-12% of his time)".
- Plan 1 (C18): 2604.22290's 97.3% used ground-truth beats **and downbeats**, with no noisy-beat test. It supports rung 1 only.
- **Taps model (plan 3).** The ±30 ms row is iid error per beat, which models the listener rung. **LR4c is relabelled "listener rung (iid beat error)".** Taps get LR2g (C4 below).
- **Section 3's 12/8 run** used the simple set, not the stated compound set. This is covered by the LR1 `s3Compat` note and LR4k.

---

## 3. Deferred items (outside LS0-LS5)

| # | What | Lands in | Done now, inside LS0-LS5 |
|---|---|---|---|
| **C3** | Free takes: Save take offers **tap-along replay** plus a note click for "1"; forced bars only if skipped. LQ2 reworded per critic "suggested shape" item 2, with the recommended default "tape live; tap-along for the saved copy". LR15's free-take clause becomes "tape live; saved copy tapped along, or flagged unverified if skipped". | **LS7** (Save take, Clean last take UI); LR15 in LS9 | LS2: `clean(events, { taps: [t_ms], one: t_ms })` treats external taps as rung 3 with full hindsight (agents anchored at taps). LS4: CLI `--taps <file.json>`. `score.json` top-level `rhythm: "jam" \| "song" \| "tapped" \| "inferred" \| "unverified"`. A span with no rung 1-3: `score.json` keeps tape measures; MusicXML gets forced bars at the rough BPM with **every** measure marked `freely`, and `rhythm: "unverified"`. |
| **C4** | Hands-free tap: a left-foot switch on the KeyLab 88 mk3 Aux input (short press = tap, press ≥ 500 ms = "This is 1", start value). **A question for Daniel** (possible purchase), asked with LQ2. | After the jam gate, **LS6 or later** (`onMidiMessage` mapping in `piano.js`) | LS0 count-in fixture and **LR2g** (below). Rung 3 from count-in taps stays valid for **K = 4 bars** after the last tap (start value, set from LR2g); after that, rung-4 rules apply (metric only while `steady`). |
| **C5** | Continuous CC64 (half-pedal) logging. | With the **`log.js` change after the jam gate** (alongside the LR16 timestamp decision), with Daniel's OK, since it changes new sessions | LS4 `midi.js`: `performance.mid` (SMF **type 0**, PPQ 500, tempo 500,000 µs) writes CC64 **127 at each down-crossing and 0 at each up-crossing** at the logged times. Drop "half-pedal kept" (plan 6.5, 7.4). **LR9b (restated):** every on, off and CC64-crossing time equals the log's `t_ms` with 0 ms error; CC64 values ∈ {0, 127}. |
| **C9** | VexFlow fonts: no unpinned `loadFonts`. Either `setFonts` against the page's loaded `Bravura` face, or pin `VexFlow.Font.HOST_URL` to versioned `@vexflow-fonts/...@x.y.z/` paths first. Plan a substitute for Academico. Gate on `fontState.smufl`. LQ1 wording: "pinned, fonts included". | The **VexFlow adapter** (`engrave_vexflow.js`) after **LQ1** = yes: LS5 lab if the answer comes before LS5 closes, else LS6 | The house engraver's `ready(fonts)` gate. Layout uses constant glyph boxes, so metrics never depend on font load. The lab page registers `FontFace("Bravura")` from the same pinned URL string `piano.js` uses. No new host. |

**Other critic items: where they go.**

| # | Placement |
|---|---|
| C10, C13, C14, C18 | Section 2 above |
| C12 | Plan 0 wording (section 2) |
| **C15** | **LS5.** A settled beat widens to its content: min width = Σ over columns of (1.18 head + 1.0 if accidental + 1.18 if second + 0.4 padding) sp, never below 96 px. **LR11g (new):** overlapping glyph boxes per settled bar on the dense fixtures (12, 24, 43, 67 onsets; 10-head chords, accidentals, seconds): **0** with widening on; reported with it off. **LR11e (restated):** mean head x shift at settle ≤ 0.5 sp on beats that did not widen (0 by construction for the slot-placed house engraver); widened beats reported separately. |
| **C16** | **LS5** `layout.js`: 9:16 score band right edge ≤ **940 px**, bottom ≤ **1436 px** (asserted in a layout test). 16:9 placement stays with LQ4. LR12 (LS6) also checks that the band sits inside those bounds in the MP4. |
| C17 | **LS6.** Ask Daniel first: hide the "now" staff while the score is on, or give `drawStaff` the hand split. Nothing in LS0-LS5. |
| C19 | Lane errata only. Settled for builders: `performance.mid` is SMF type 0; the pedal line is painted by time by us (no `PedalMarking`). |
| C20 / M7 | **LS6:** the transcriber feed sits beside `logged()`, not inside it, and copies the demo and cue exclusions. |
| M6 | Lead-sheet view: v2, offered to Daniel as the "readable on a phone" mode. Not in LS0-LS5. |
| M9 | The same as C5 plus LR16. |

---

## 4. Receipt changes at a glance

| Receipt | Slice | Change | Threshold |
|---|---|---|---|
| LR1 | LS0 | Reproduce section 3 in `s3Compat` (DIVS {1, 2, 4, 3} everywhere) | exact seeds; ±1 point |
| **LR2g** | LS1 | New. Count-in: 4 taps (±30 ms iid), then no taps for N = 1, 2, 4, 8 bars under rub1 and rub2; bars onset-exact against N | reported; sets K (C4) |
| **LR3d** | LS3 | New. Subdivision chip (C11) | ≥ 90% hit / ≤ 5% false (start) |
| LR4a-e | LS2 | Read "bars onset-exact"; run with the C2 alphabet; LR4c relabelled listener rung | unchanged |
| LR4f | LS2 | Restated as conditional accuracy | unchanged |
| LR4h | LS2 | Uses the C1 and C7 rules and the pedal-model fixtures; `barsFullyExact` reported | set at first measurement |
| **LR4i** | LS2 | New. Precision of shown rung-4 bars | ≥ 0.80 target; the gate floor moves, not the target |
| **LR4j** | LS2 | New. Sextuplet suite; false d = 6 | set at first / ≤ 1% (start) |
| **LR4k** | LS2 | New. 12/8 under 4/4 by feel; 12/8 compound set | triplet > straight; ≥ 0.94 |
| LR8 | LS3/LS4 | Plus **LR8b** rests per voice per bar in pedalled bars, **LR8c** held-bass bar-line ties, **LR8d** accents per minute | ≤ 1 median; ≤ 1 per note; 0 by default |
| LR9b | LS4 | Crossings only, CC64 ∈ {0, 127} | 0 ms |
| LR11b, LR11e | LS5 | Run on the house engraver; LR11e excludes widened beats | unchanged |
| **LR11f** | LS5 | New. Tape overlaps per minute | 0 on the 1.5 s fixture; S1..Sn set at first |
| **LR11g** | LS5 | New. Settled-bar glyph collisions | 0 with widening |
| LR15 | LS9 | Per-note σ, no √2; free-take clause per C3 | unchanged otherwise |

---

## 5. Threshold changes

Any loosened, raised or newly fixed threshold, and any gate floor moved to meet a target (LR4i), goes here **before** the receipt is claimed: date, receipt, old → new, measured value, why.

| Date | Receipt | Old → new | Measured | Why |
|---|---|---|---|---|
| 2026-09-15 | **LR3b** (ls1-rulings.md) | ≤ 1.0 ×1.5 flips/min and ≤ 0.5 ×2 flips/min on every S1..Sn replay → rates only on sessions with **≥ 2 min of locked playing** (readout state `lock`, the tempo lane's "locked"); shorter sessions report their flip count and fail only with **more than 2** ×1.5 + ×2 flips. Display = `bpmShown`, meter held at 4/4. | LS1fix bench: 11 of 15 sessions have ≥ 2 min locked: ×1.5 max **0.63**/min, ×2 max **0.21**/min. Short (S10, S11, S15, S17; 0.5-1.5 min locked): ×1.5 + ×2 flips 0, 0, 0, 1 (all flips 2, 1, 0, 1). Pass. Reading "2 minutes" as session length instead gives the same maxima. LR3c on the final-code run: S12 p95 0.150 ms per tick (0.165 on the first run). | S17 lasts 0.55 min and has one ×1.5 flip, which reads as 1.83/min. One event over half a minute is not a rate, and the original tracker reproduces it. |
| 2026-09-15 | **LR1** (ls1-rulings.md) | Configuration unnamed → LR1 measures the **tempo-lane configuration** `LANE_BEAT_PARAMS` (`ibiAcrossHolds: true`, `shownConfirmBars: 0`). The product defaults are reported beside it as a delta, not gated. Tolerances unchanged (±1 point). | Worst deviation 0.001 (tempoLane rub2 acc1s); `score_fixtures` 1885 passed, 0 failed. Product minus lane, 81 pieces, meter auto: Acc1 −0.002, Acc2 +0.000, flips −0.07/min, lock time −1.26 s, pieces never locked +1 (4 → 5). `bpmShown` Acc1 0.663 (lane 0.700), `bpmShown` flips 0.82/min (lane 1.22). | Keeps LR1 comparable with the tempo lane's published numbers while the product readout drops intervals across holds and confirms `bpmShown` for a bar (the S5 ×2 flips). |
| 2026-09-15 | **LS1-level** (new, ls1-rulings.md) | Builder's unrecorded gate (×2 only, 18 pieces, 6/8 filtered out) → recorded: **×2 and ×0.5** on rub0 release **≤ 2** pieces and Acc1 at the chosen level is **≥ 0.90**; rub1 and rub2 reported. The suite now includes the 9 held-out **6/8** pieces and a **simple-meter tactus-0.5** set (8 arp pieces at 63-64.5 bpm in 4/4 and 3/4, factor 0.5 at every rub0 press): 35 pieces per rubato level. The ruling's "2 of 18" is kept as a count over the 35. 12/8 is pinned in the unit tests (the tempo suite has no 12/8). | **FAIL on ×0.5.** rub0: ×2 released 1 of 35, Acc1 0.913 (pass); ×0.5 released 1 of 35, Acc1 **0.864** (fail). Acc1 ×2 / ×0.5 by group: simple 0.945 / 0.922, 6/8 0.771 / 0.691, tactus-0.5 1.000 / 0.930. **Breakdown (reported):** 2 of 9 6/8 pieces (ballad) show double tempo before any press (no-press Acc1 0), and the press multiplies that number, as the must-fix requires. Over ticks with a readout, Acc1 is 0.914 for both buttons (0.941 with no press). The rest of the ×0.5 gap is the ~8 s null readout after the press (the LS3/LS5 `hold` header). Button pins (6/8, 12/8, 4/4 at factor 0.5, all four ratios): readout within 3% of shown × ratio on ≥ 90% of ticks, median within 1.5%. rub1: the tactus-0.5 set releases 6 of 8 on both buttons (Acc1 0.30 / 0.22 against 0.94 with no press). **LS1fix r1 (2026-09-15): PASS, threshold unchanged.** The ×0.5 null window was a code defect, not a counting convention: a holding level's reading (shadow × q) waited for the level agent's own 4 intervals. It now reads from the tick after the press, and until the level agent has its own readout the state words come from the shadow. rub0: ×2 released 1 of 35, Acc1 **0.914**; ×0.5 released 1 of 35, Acc1 **0.915** (simple 0.945, 6/8 0.778, tactus-0.5 1.000 on both buttons; `acc1AtLevelRead` now equals `acc1AtLevel`). Without the 2 pieces wrong before the press: 0.970 on both. rub1 ×2 / ×0.5: released 11 / 12, Acc1 0.691 / 0.656 (was 0.689 / 0.606); rub2: released 22 / 21, Acc1 0.299 / 0.270 (was 0.298 / 0.249). With no press the code path is unchanged: LR2a-g, LR3a and all 17 S1..Sn bench rows are identical; LR3c S12 p95 0.156 ms per tick. | The builder set the threshold, and the amendments require recording it before it is claimed. The old receipt passed only because it filtered out 6/8. **Not re-tuned here:** whether the gate should count levels that were already wrong before the press, and how the ×0.5 null window counts, is left to the conductor. After LS1fix r1 only the first question is open, since the null window is gone. The gate passes with those 2 pieces (6/8 ballads, seeds 41 and 53, true tempo 53.0 and 48.5) counted as misses. The tempo lane's own tracker reads both at double tempo too (105.8 and 96.6, meter auto and 6/8), so the error comes from rung 4, not from the buttons. **Ruled 2026-09-15 (ls1-rulings.md LS2 rulings):** levels already wrong before the press count as misses (Acc1 0.914 / 0.915, gate 0.90). The two 6/8 ballads are a rung-4 level error that LR2a and LR2d already count, and they stay in the gate so the thin margin stays visible. |
| 2026-09-15 | **LR4i** (LS2, rung-4 metric gate) | Periodicity strength floor for drawing rung-4 bars 4.5 → **8.0**, raised in 0.5 steps per the LR4i rule. It is a new transcriber option, `rung4Floor` in `score/index.js`; the steady word in beat.js stays at 4.5 (LR2, LR3 unchanged). | Held-out tempo suite (81 pieces, meter set to truth), precision at 4.5, 5, 5.5, 6, 6.5, 7, 7.5: 0.657, 0.681, 0.697, 0.710, 0.709, 0.728, 0.772. **At 8.0: 0.856**, shown share 0.147 (382 of 2,592 bars; rub0 0.855, rub1 0.860, rub2 shows none). Tuning seeds 11 and 23: 0.694 at 4.5, 0.876 at 8.0. Failure classes at 4.5: 289 bars whose lines sit on no true beat, 101 at another level or partial, 63 with wrong onsets, 9 with a wrong phase. LR4g: 0 at 4.5 and at 8.0. | The rule. Phase is not the main loss (9 bars, with the LS2 stand-in phase scorer); beat-time and level errors are. So meter.js (LS3) is unlikely to lower the floor on its own. Consequence: with no jam, song or taps rung, the live panel draws rung-4 bars on about 15% of synthetic bars, and none under strong rubato. Re-measure when beat.js or meter.js changes. |
| 2026-09-15 | **LR4h** (LS2) | Set at the first measurement → **durations exact >= 0.95** (pedal and heldBass families, held-out seeds, oracle beats, truth voices, `clean()`). `barsFullyExact` is reported, with no threshold in LS2. | Durations exact 0.971 (pedal 0.974, heldBass 0.958; rub0 0.998, rub1 0.981, rub2 0.933); 0.985 when the onset is exact. `barsFullyExact` 0.886. LR8b on fixtures: median rests per voice per pedalled bar 0 (truth 0). | A few points under the first measurement (plan section 10). The misses gather in rub2 and in rolled chords that T1 leaves split: a spread of 40-50 ms is neither a 40 ms chord nor a roll (at least 50 ms). |
| 2026-09-15 | **LR4j** (LS2, sextuplet suite) | Set at the first measurement → **onsets exact >= 0.96, bars onset-exact >= 0.83** | Onsets exact 0.982, bars onset-exact 0.865 (C2 as written). The false d = 6 row keeps its start value (<= 1%) and measures **0.025, a FAIL**, not changed here. | A few points under the first measurement. |
| 2026-09-15 | **LR4k** (LS2, feel) | Margin set at the first measurement → **triplet minus straight >= +1.0 point bars onset-exact**, with triplet onsets exact not below straight | Straight 0.991 / 0.942, triplet 0.994 / 0.963 (onsets / bars onset-exact); margin +2.1 points. The compound set on the section 3 12/8 pieces: 0.990 bars onset-exact (>= 0.94, pass). | Half the measured margin. |
| 2026-09-15 | **div8** (LS2, the C2 decision) | Stays **off** | λ8 start value 2.5. Turned on, LR4a drops 1.0 points (onsets) and 3.9 points (bars), and LR4d drops 2.2 and 4.8; the 32nd family's onsets exact rises from 0.834 to 0.953. | C2 keeps div8 only if LR4a and LR4d each drop by at most 0.5 points. |
| 2026-09-15 | **Loose ceiling** (LS2; plan 6.4 says to set this start value on fixtures, so it is not a receipt threshold) | "Cost > 12" → cost > 12 **not counting the penalty of collisions T1 explains** (two groups within 120 ms, or a grace candidate). An unexplained collision still makes the beat loose. | Before the change, 66 of 6,967 beats of the section 3 suite were loose in the clean pass, nearly all at cost 12.0-13.1 with one explained collision at the 15 ms σ floor. The d = 6 export spread them out, and the clean copy fell to 0.981 / 0.913 (live 0.987 / 0.936). After: clean 0.987 / 0.939, and LR4h durations exact 0.963 → 0.971. | A chord or roll that T1 left split is not a rhythm the grid cannot hold. Counting its 6-point penalty flagged ordinary beats whenever σ was small. |
| 2026-09-15 | **LR4a-e and LR4j false d = 6** (LS2; no change, recorded as FAIL) | None. C2 as written fails, and "a drop below threshold is a failure, not a retune". | C2 as written, product onset groups, section 3 suite: LR4a 0.987 / **0.936**; LR4b **0.838**; LR4c **0.961 / 0.834**; LR4d **0.876 / 0.535**; LR4e **+0.110**; false d = 6 **0.025**. λ6 tuned inside the order on tuning seeds only (2.5-3.0, with λ3 = λ6 + 0.25) still fails LR4b, LR4c and LR4d. **Proposal for a ruling:** `d6MinGroups: 5` (d = 6 is tried only on beats with 5 or more onset groups; λ values and order unchanged) gives LR4a 0.994 / 0.971, LR4b 0.919, LR4c 0.981 / 0.912, LR4d 0.929 / 0.673, LR4e +0.186, false d = 6 0.0007, sextuplets 0.984 / 0.872, LR4k triplet 0.961 > straight 0.931. It was also the best variant on the tuning seeds. | Root cause: in the straight table λ6 < λ3, and the d = 6 grid holds every d = 3 slot, so d = 3 is never chosen. Triplets are priced at λ6, and the slots only d = 6 has (1/6, 5/6) pick up jittered 16ths and 8ths. The gate changes the design (C2 says the rest of the cost is unchanged), so it waits for the conductor. The quantize.js defaults stay C2 as written; `--params '{"d6MinGroups":5}'` runs the receipts under the proposal. **LS2 r1 (2026-09-15), still FAIL, no ruling yet:** re-measured after the measures.js spelling repair, which does not touch these receipts: LR4a 0.9866 / 0.9359, LR4b 0.8376, LR4c 0.9612 / 0.8343, LR4d 0.8759 / 0.5354, LR4e +0.1096, false d = 6 2.50%; under `d6MinGroups: 5` all 62 checks pass. The ruling's second option, measured as evidence: straight {1: 0, 2: 0.5, 4: 1.5, **3: 1.75, 6: 2.0**} (d = 3 can win) passes LR4a (0.9882 / 0.9424) but fails LR4b 0.8505, LR4c 0.9676 / 0.8540, LR4d 0.8846 / 0.5547, LR4e +0.1110, false d = 6 2.07%. Swapping λ3 and λ6 is not enough on its own. **LS2 r2 (2026-09-15), still FAIL, no ruling yet:** re-run after the held-bass tie repair (measures.js `capTick`, index.js segment caps), which does not touch these receipts: defaults 60 passed, 6 failed (LR4a 0.9866 / 0.9359, LR4b 0.8376, LR4c 0.9612 / 0.8343, LR4d 0.8759 / 0.5354, LR4e +0.1096, false d = 6 2.50%); `d6MinGroups: 5` 66 passed, 0 failed (LR4a 0.9943 / 0.9713, LR4b 0.9189, LR4c 0.9814 / 0.9115, LR4d 0.9286 / 0.6727, LR4e +0.1864, false d = 6 0.07%). The slice closes only after the conductor rules and the chosen default is re-run. **Closed 2026-09-15:** the conductor ruled for the gate; see the C2 row below. |
| 2026-09-15 | **C2 d = 6 gate** (ls1-rulings.md LS2 rulings; a **rule change**, no threshold moves) | C2 as written (d = 6 competes on every simple-meter beat) → **`d6MinGroups: 5` is the `quantize.js` default.** The alphabet stays {1, 2, 3, 4, 6}, and the λ tables and their order are unchanged. In simple meters (both feels), d = 6 is a candidate only on a beat holding at least 5 onset groups. The compound and `s3Compat` sets are not gated. | LS2 close, defaults (`score_quantize` 68 passed, 0 failed): LR4a 0.9943 / 0.9713 (clean 0.9938 / 0.9686), LR4b 0.9189, LR4c 0.9814 / 0.9115, LR4d 0.9286 / 0.6727, LR4e +0.1864 (0.9393 − 0.7530), false d = 6 0.07% (raw d = 6 labels on 0.16% of beats), LR4j sextuplets 0.9839 / 0.8715, LR4k triplet 0.9933 / 0.9606 against straight 0.9892 / 0.9306 (+3.0 points), compound 12/8 0.9903, LR4f 0.9771 (coverage 0.4505; rub2 0.9434). Knock-on effects under unchanged thresholds: LR4h durations exact 0.9791 (was 0.971), `barsFullyExact` 0.9066 (was 0.886). LR4i precision at `rung4Floor` 8.0 is 0.9058 (was 0.856), with 382 of 2,592 bars shown (0.147). LR4g 0. Loose beats 7 of 6,967. div8 stays off: turned on, LR4a drops 1.7 / 7.0 points and LR4d 7.2 / 15.8, while the 32nd family rises 0.846 → 0.959. C2 as written, reported beside it (d6MinGroups 0): LR4a 0.9866 / 0.9359, LR4b 0.8376, LR4c 0.9612 / 0.8343, LR4d 0.8759 / 0.5354, LR4e +0.1096, false d = 6 2.50%. | The critic's evidence for C2 was beats with 5 or more onsets, 0-22% of beats in Daniel's playing. Six even divisions are only plausible when about that many onsets are present, so the gate is the rule C2 meant. As written, the d = 6 grid holds every d = 3 slot at a lower straight λ: triplets were priced at λ6, and the slots only d = 6 has (1/6, 5/6) picked up jittered 16ths and 8ths. |
| 2026-09-15 | **Design note: phase-keep** (ls1-rulings.md LS2 rulings, ratified; no receipt change) | None (a construction convention) | When a phase correction would leave partial the bar after a settled bar with a bar-line tie, the new phase starts one bar later (`index.js` `setPhase`, `tieInto`). Re-counted on the LS2 close code: it fired 59 times on the 294 inferred-beat fixture takes (live) and 20 times on S1..Sn, the counts the ruling cites. | *Settled means never repainted.* Re-capping a frozen note's recorded duration would change a bar Daniel has already seen, which breaks the settle-then-freeze promise. |
| 2026-09-15 | **Design note: a live bar-line tie stops at the start of the beat the onset finality horizon is in** (LS2 close, repaired in LS2close round 1; the frozen-tie overlap edge in ls1-rulings.md; no receipt change) | None (a construction rule: `measures.js` `heardTick`, passed by `index.js` on live builds only) | Before the rule the ruled fixture overlapped (`tests/score_settle.test.mjs`: bar 0 settles on a 2 s pause at 4500 ms with the held bass tied; a bass onset at 4520 ms quantizes to bar 1 position 42). The continuation, snapped to 144 ticks, ran past it; with the rule it stops at 120. Fixed-beat sweep (3 tempos, 26,190 replays): 220 replays with an overlap before, 0 after; bar-line ties 34,536 → 33,794. A first cut capped at the last *detected* beat also gave 0 overlaps, but it dropped 54 of 718 live bar ties on the 294 inferred-beat fixture takes and 4 of 308 on S1..Sn. The kept rule drops 6 of 718 and 0 of 308. Construction audits (values, tiles, ties lead on, one bar line, overlaps) are 0 on the fixture suites and on S1..Sn, live and clean. `clean()` has no cap, so LR4h does not move. **Repair round 1:** the first rule took the beat from the tick time, but an onset group can still be open in `onsets.js` (chord, near-chord or roll window, up to 180 ms) when a bar settles. Verifier's case: beats every 400 ms from 1000; bar 0 settles by beats at 4250 ms with the bass tied to 192 ticks; the bass onset at 4140 ms is handed out at 4500 ms and quantizes to 186, inside the frozen continuation. The horizon is now min(tick time, the oldest note-on `onsets.js` has not handed out: `oldestPending()`), and the tie in that case stops at 168. The case and two variants (onset at MIDI 38; release 4120, onset 4150) are in the frozen-tie fixture; against the pre-fix code, 9 of their 12 checks fail. Verifier's sweep: 4 of 1,200 overlapped before, 0 after; bar ties 1,836 → 1,836. Denser sweep (6 tempos; later onset as a single note, a 40 ms chord or a roll): 100 of 19,926 before (30 single, 30 chord, 40 roll), 0 after; bar ties 49,262 → 49,262; 0 late changes, 0 refusals. Live bar ties on the 294 inferred-beat takes 712 → 711; fixed-beat family takes 90 → 90; S1..Sn live 308 → 308, clean 295 → 295. Every receipt is unchanged except timing fields. | Settled means never repainted (the phase-keep reason): a settled bar cannot re-cap its frozen tie, so the claim must never reach past the point where a later onset can still quantize. Every onset the build has not seen starts at or after the horizon (events up to the tick time are delivered before the tick, and `onsets.js` holds back only groups at or after its oldest open note-on). It quantizes at or after the beat that holds it: `assignBeats` puts an onset in its own beat or the next, never an earlier one. Exact on fixed beats; on tracker beats it holds while that beat's predicted time moves by less than 1/8 of a period (the next-beat rule, f > 0.875). **Superseded as the guarantee by the next row (repair round 2):** heardTick stays and keeps the claim short, and the pin enforces no overlap. |
| 2026-09-15 | **Design note: an open bar never places an onset inside a frozen continuation (the pin), and frozen notes come from the settled bar** (LS2close repair round 2; no receipt change) | None (construction rules in `index.js` build; the construction audit's overlap check now reads a continuation's full extent) | **Verifier's case, confirmed by probe.** Inferred beats, 3/4, a 600 ms beat, ×1.3 from bar 6, bass 38 at 0.95 of odd bars. Bar 3 settles by beats at 14500 ms with bass 36 (tick 228) frozen at 96 ticks: continuation [288, 324), and the 38 touches it at 324. At 15250 ms the tracker revises the grid and re-quantizes that already-seen 38 to 318. Bar 4's lower voice held 78 ticks, with 1 overlap. heardTick covers only onsets the build has not seen. **Rule 1 (pin):** a build collects the frozen continuations in the settled bar before the first rebuilt bar (end per voice). Any onset group with a note in such a voice that quantizes inside one is placed at the continuation's end: `stats().pinned` counts pinned groups, and a pinned note carries `pinnedFrom`. In the case above the 38 stays at 324. **Second defect, found by the tracker sweep:** 3 dangling ties and 3 untiled notes, the same before and after rule 1. Confirmed by a logged build on the round-1 code: 3/4, an 800 ms beat, ×0.9 from bar 5; bar 4 settles at 20500 ms holding a bass 38 played at 15052 ms, 108 ms before its first beat (15160), at position 0 and tied. At 20750 ms the build rebuilding from bar 5 opened its group window at 15070 ms (15160 minus 1/8 of 720), so the frozen 38 was left out and its continuation vanished. Earlier builds, on a 929 ms period, had held it. **Rule 2:** frozen notes come from the settled bar itself, not the time window (a tie crosses one bar line). **Audit:** the overlap check read only the continuation's piece at position 0. It now reads every piece of the continuing note in the bar. **Receipts (round-1 code → this code, full-extent audit unless noted).** Verifier's probe: 1 → 0 overlaps. Verifier's sweep (240 variants): 1 → 0, bar ties 1,281 → 1,282. Tracker sweep (15,840 variants: meters 4/4, 3/4, 6/8; beats 450-800 ms; tempo ×0.7-×1.5 from bar 5, 6 or 7, as a step or a 4-bar ramp; later bass at 0.5-0.98 of the bar, single or a 40 ms chord): overlaps 540 → 0 (1 → 0 with the old audit; 4/4 253, 3/4 281, 6/8 6), dangling 3 → 0, untiled 3 → 0, bar ties 105,279 → 105,280. It pinned 537 groups (804 notes), shifting them 2-6 ticks (median 4), with 0 late changes and 0 refusals. Dense fixed-beat sweep (19,926 variants): identical (0 overlaps, 49,262 bar ties). Fixtures, 294 inferred-beat takes, live: overlaps 7 → 0 (the old audit read 0), 7 pinned groups (19 notes, 2-6 ticks), bar ties 711 → 711. Fixed-beat family takes (213): 0 pins, bar ties 90 → 90. S1..Sn live: overlaps **5 → 0** (the old audit read 0, so the round-1 row's "construction audits are 0 on S1..Sn" was measured with the first-piece check and did not hold), 4 pinned groups (7 notes, 2-4 ticks), bar ties 308 → 308. S1..Sn clean: 295 → 295, 0 pins (`clean()` freezes nothing before a segment closes, so LR4h cannot move). Frozen-tie fixture: 2 new cases, 8 checks; against the round-1 code, 6 fail. **Suites on this code** (run alone, no jam timing process running): `score_fixtures` 1885 passed, `score_beat` 133 and `score_quantize` 68, all with 0 failed. Apart from timing fields they are identical to the round-1 outputs, except the inferred-beat live note pieces (69,039 → 69,033; audit 0). So LR1-LR4k do not move. `score_settle --sessions`: 41 passed, 0 failed. LR5 S12 p95 0.2192 ms per group (limit 0.5). LR6a 0 changes over 1,998 settled bars. LR6b lag 4 vs 8: 0.0002 (was 0; range 0-0.0013, limit 0.06), lag 2 0.0003 (was 0.0002), lag 1 0.0113 (was 0.0112). A pin depends on when a bar freezes, so churn between settle lags can now be nonzero. LR6c median 74.09 per min (was 74.19). S1..Sn five-property audit: 0 on live and clean copies. | Settled means never repainted, so the open bar gives way. Capping the claim at freeze time (the verifier's other option) needs a margin that no bound on tracker revisions supports, and it would shorten every tie that ends at a later onset. The pin moves an onset only when a revision would otherwise break the frozen claim: 11 groups over the 294 fixture takes and 16 sessions. |
| 2026-09-15 | **LR7** (LS3, hands; no threshold change, recorded as FAIL) | None. Staff accuracy >= 0.95 / voice F1 >= 0.90, gated on the clean copy (`clean()`, oracle beats, hands.js voices) over every LS0 family on held-out seeds (213 takes). Voice F1 = F1 of voice links (every pair at one onset, every pair across successive onsets of one voice). | **Staff 0.9658 (pass), voice F1 0.8588 (FAIL).** Live copy 0.9658 / 0.8585; hands.js alone (hindsight, truth tick order) 0.9658 / 0.8738; the MIDI 60 split 0.9632 / 0.6715. By texture (clean): ballad 0.9742 / 0.843, arp 0.918 / 0.8198, mixed 0.9981 / 0.915. Tuning seeds (5, 6) only: with truth staves the voice rule reaches 0.9380. Written durations on hands.js voices (C1, C7; reported): pedal 0.9395, heldBass 0.964 (LR4h on truth voices 0.9791). | Two losses. (1) Left-hand arpeggio tops at C4-A4, which the fixtures write on the lower staff, read as right hand: every distance cost prefers the idle right hand, and the arp staff accuracy is 0.918. (2) Triads released early under the pedal leave the lower voice. Tuned on tuning seeds, inside T5's "costs are guesses until SR7": `switchCost` 3 / `switchMs` 600 (a figure-continuity cost that T5's list does not have: a single note changing hand from the single note just before it), `clusterMs` 80 (60: 0.8646, 80: 0.8691, 120: 0.8681), and pedal sustain for a chord's inner notes. Tried and dropped: a chord's lower notes outlasting its top note as a second lower-voice rule (voice F1 with truth staves 0.9365 -> 0.9268). No proposal is adopted. Open for the conductor: group figures by beat after quantization, or accept the fixture convention. |
| 2026-09-15 | **LR3d** (LS3, C11 chip; no threshold change, recorded as FAIL) | None (start values: hit within 32 beats >= 90% on 12/8 under 4/4, false <= 5% on straight 4/4 and 3/4). C11 as written stays the default (`chipRule: "grid"`). | **grid: hit 0.111 (FAIL), anywhere 0.222; false within 32 beats 0.047 (pass), anywhere 0.094.** 27 compound takes; 213 straight takes (every LS0 family in 4/4 or 3/4 and the tempo suite's 4/4 and 3/4 pieces). Proposal measured, `chipRule: "tatum"`: on grids 2 and 3 the best compound template beats the best simple one by `chipMargin` 0.05 (or the grid is 3). It gives hit 0.481, anywhere 0.778; false 0.085, anywhere 0.211. Tuning seeds, margins 0 / 0.02 / 0.05 / 0.1 / 0.15: hit 11 / 11 / 10 / 8 / 5 of 18, false within 32 beats 13 / 10 / 10 / 9 / 8 of 94 (grid: 2 of 18, 5 of 94). | Root cause: under 4/4 the tracker locks a 12/8 at two eighths (×1.5 of the dotted quarter; readout median 67-101 for true 45-64), where beat.js's grid test sees halves, so subdivision reads 2 on every tatum. Neither rule meets the start values; the chip needs the level, which the ×2/3 button fixes. Left for a ruling. |
| 2026-09-15 | **LR8** (LS3, S1..Sn clean copies; no threshold change, recorded as FAIL) | None. Every session: median per bar tuplet brackets <= 1, ties <= 2, voices per staff <= 2; pedal marks <= 1 per beat; dynamics <= 3 per min; key signature changes <= 1 per 30 s of key area (each area's changes over its length; summary.json key areas). Meter 4/4, inferred beats, hands.js voices, con Ped. on, accents off. | 18 sessions used of 23 (5 under 100 onsets). **Worst session medians: tuplet brackets 4.5 (S18; S7 and S9 4, FAIL), ties 6 (S17, 13 bars; most sessions 3-4, FAIL), voices per staff 2 (pass).** Pedal marks per beat max 0.244 (pass; 34 con Ped. phrases on S12). **Dynamics per min: S18 4.41 (1.36 min), S13 3.15, S1 3.14 (FAIL); the rest 0.24-2.85; pooled about 1.7.** Key signatures: area max 0.97 per 30 s, session max 0.27 (pass). Before the 30 s minimum-area rule (next row) the area max was 2.33 (S12). Only 2 of 1,969 bars are metric at `rung4Floor` 8, so LR8 reads the unverified forced bars of C3. **LR8b** median rests per voice per pedalled bar 1 in every session (1,760 pedalled bars): pass. **LR8c** 0 of 8,494 held-bass notes (voice 4) over one bar-line tie: pass. **LR8d** 0 accents per min by default: pass; "voice" 1.0-23.2 per min. | Drivers (scratch breakdown, aggregates only): ties are intra-bar spelling splits of long pedal-filled values (median tie pieces 3-4 per bar with hands.js voices, 2-3 with the MIDI 60 split; bar-line ties median 0; chord-collapsed tie arcs 3). Tuplet brackets come from d = 3 / 6 beats on inferred beats. On oracle-beat fixture copies the same medians are 0 ties and 0 brackets. The dynamics rule (4 s windows, 2-window hold, the opening mark counted) passes pooled but not on 3 sessions. Not retuned here. |
| 2026-09-15 | **Design note: key areas shorter than 30 s never change the signature** (LS3, marks.js `keySignatureChanges`; plan 6.6 rule; no receipt threshold) | None | summary.json key areas can be 12.5-18.7 s (S6, S12, S14, S23). Without the rule LR8's key signature proxy read 2.33 changes per 30 s on an S12 area; with it, 0.97. | Plan 6.6: "the clean pass uses the closed session's key areas (30 s minimum area), so a 19 s excursion stays accidentals." The minimum had been assumed of summary.json. |
| 2026-09-15 | **Header hold after a family press** (LS3, meter.js `createHeaderModel`; new receipt, set at the first measurement) | New: 0 blank ticks after a press; every tick until bpmShown confirms (within 3% of the request) shows the request dimmed with the word `hold`; every press confirmed. | The LS1 button pins (6/8 arp 50, 12/8 arp 50, 4/4 arp 63) × 4 ratios through index.js: 12 of 12 presses confirmed, 0 blank ticks, 211 pending ticks all showing the request, request within 3% of the number shown before × ratio on 100%. Waits (s) ×2: 1.5, 2.75, 2.25; ×1.5: 2, 3.5, 2.75; ×2/3: 4, 7.5, 6.25; ×0.5: 5.25, 10, 8. No press, 9 tempo-suite pieces: the header equals bpmShown in full ink on every tick outside a hold. | The ls1-rulings.md rule and the LS3 checklist item. The waits match the checklist's bpmShown confirmation times. |
| 2026-09-15 | **Knock-on of hands.js voices and the meter model on LS2 receipts** (LS3; no threshold change) | None | `score_quantize` 68 passed, 0 failed: LR4a-h unchanged (the fixtures inject truth voices). **LR4i precision 0.9058 -> 0.877** (335 of 382 shown; floor 8.0 unchanged, pass): the live transcriber's default voices changed, and pins follow voices. Construction audits on the 294 inferred-beat takes: 0, live and clean. `score_settle --sessions` (run alone) 41 passed, 0 failed: LR5 S12 p95 **0.2821** ms per group (was 0.2192; limit 0.5); LR6a 0 changes over 2,064 settled bars; LR6b lag 4 vs 8 0.0002 (range 0-0.0013); lag 1 0.0113; LR6c median 63.7 per min; bar-line ties live 417, clean 401; five-property audit 0. The session count grew to 18 used, so these do not compare one to one with the LS2 close. | From LS3 the transcriber takes hands.js voices unless voiceOf is injected (section 0 rule 3). hands.js voice convention: staff 1 voices 1 (moving or lone) and 2 (sustained lower); staff 2 voices 3 (moving above a sustained lower note) and 4 (sustained lower or lone), so C7's held bass always reads voice 4. The inferred phase scorer moved to meter.js unchanged. |
| 2026-09-15 | **Design note: every lower-staff note within 3 ledger lines, by a clef change or an octave line at that bar** (repair LS3 r1, `hands.js` `clefsAndOctaves`; plan 6.5; new construction receipts `LS3-clefs-fixtures` and `LS3-clefs-sessions`, no threshold moves) | None (a construction rule; the receipts gate 0 note pieces beyond 3 ledger lines on every bar some clef and octave line hold) | **Verifier's case, confirmed by probe.** Staff-2 bars [60,64], [62], [36], [36], [48,76], [48]: the old code wrote C2 in treble clef at octave 0 (bars 2-3, 11 ledger lines below) and E5 in bass clef at octave 0 (bar 4, 5 ledger lines above). Two gaps: in treble clef only notes above E6 were checked, and the switch back waited for 2 bars; in bass clef `bassMax` (67) was never read. The contract test asserted the defect (bar 4: D3 in treble clef, 4 ledger lines). **Rule:** ranges in written pitch are treble 53-88 and bass 33-67. A bar whose notes do not all fit the hysteresis clef takes the first option holding all of them: the other clef (a change at that bar line, which becomes the state), an octave line on the state's clef, then an octave line on the other clef. A bar no option holds (span over 34 semitones) takes the fewest notes beyond, then the fewest semitones beyond. Staff 1 stays treble: 0, else the octave line that fits. The probe now reads bass 0, 0, 0, 0, **8va**, 0, with 0 notes beyond. The hysteresis contract moved to A3 and G3 so it stays inside the range, and the old bar 4 is pinned as a change for range. There is also a 400 × 16 random-bar sweep: 0 beyond on holdable bars, prefix-stable, so a settled bar's clef never changes. **Receipts, old rule → this rule, clean copies.** Fixtures (213 takes, 3,363 bars): staff-2 pieces beyond 3 ledger lines 225 (166 bars, up to 12 semitones) → 52, all on the 48 bars no option holds. Clef changes 0 → 61 (4 by hysteresis, 57 for range). Staff-2 octave-line bars 0 → 145 (101 bass 8va, 44 treble 8vb): left-hand figures A2-C5 that fit no clef unshifted, since hands.js puts some arpeggio tops on staff 2. S1..Sn (18 sessions, 1,969 bars, 130.7 min): staff-2 pieces beyond 315 (145 bars with any piece beyond, up to 25 semitones) → 25; staff 1 67 → 55. Every remaining piece is on the 47 bars no option holds: 27 staff-1 bars, 15 of them above C7 (15ma territory), and 20 staff-2 bars, all spanning 35-47 semitones. Clef changes 16 → 84 (17 by hysteresis, 67 for range; worst session 13.3 per 100 bars). Staff-2 octave-line bars 278 → 326 (275 bass 8vb). `outOfRange: "octave"` (octave line before clef change), reported: the same 0 / 80 beyond, changes 50 (worst 6 per 100 bars), octave-line bars 378 (67 bass 8va), and one-bar clef round trips (a change for range and back the next bar) 3 against the default's 19 (fixtures 4 against 21). Unchanged on this code: LR7 0.9658 / 0.8588, LR3d, LR8, LR8b-d, and the session audit 0 on all five properties; `score_hands` 69 passed, 3 failed (the three FAIL rows above, identical numbers), `score_fixtures` 1885 passed, 0 failed, `score_quantize` 68 passed, 0 failed, `score_beat` 133 passed, 0 failed, `score_settle --sessions` (run alone, no jam timing process) 41 passed, 0 failed: LR5 S12 p95 0.2701 ms per group, LR6a 0 changes over 2,064 settled bars, LR6b lag 4 vs 8 0.0002, LR6c median 63.7 per min, bar-line ties live 417 / clean 401, five-property audit 0. | Plan 6.5: "3 ledger lines allowed; 8va or 8vb for 1+ bar beyond that". transcription.md T5 puts octave lines above the treble staff and below the bass staff, so a left hand climbing out of bass clef is a clef matter first. Octave lines are per bar (the score schema's `octave` per staff per bar), so a bass 8va on a C3-E5 bar also writes C3 as C2. The default order and the per-bar granularity are open for the conductor (see the LS3 r1 report). |
| 2026-09-15 | **LR11f S1..Sn** (LS5; "set at the first measurement") | Reported → **pooled overlapping heads per tape minute <= 3.0** over S1..Sn (meter 4/4, ribbon.js over index.js defaults); per session reported. The 1.5 s fixture row stays 0 (gated). | `tests/score_layout.test.mjs --sessions`: pooled **2.38** per tape minute (18 sessions used). Worst sessions: S6 24.7 (172 overlaps in 6.95 tape minutes), S17 5.07 (3 in 0.59), S7 3.34, S14 2.54, S12 2.11 (66 in 31.3; 959 of 4,198 columns clamped at D_max). 6 sessions 0. Fixture: 1.5 s run 0 overlaps, 0 clamped (15 columns); 4 s run 19 overlaps, 20 of 40 columns clamped. **Repair LS5 r1 (2026-09-15): the limit is now compared.** The first build recorded this row but `tests/score_layout.test.mjs` pushed the LR11f-sessions receipt with `pass: null` and no check, so a regression would still exit 0. It now checks pooled ≤ 3.0 and sets the receipt's and the bench JSON's `pass` from it. Re-run: pooled **2.3794** (314 overlaps in 131.97 tape minutes, 18 sessions), pass; every session row identical to the first run apart from tick timing. A mutant copy of the test with a 2.0 limit fails and exits 1. | Overlaps happen only where C6's debt clamps at D_max (180 px), which the rule allows and counts, so the number measures how often his playing outruns the band's right margin. A pooled limit a little above the first measurement catches a regression in the placer or the column widths; S6's rate is a property of that session's density, not a defect, so no per-session limit is set. |
| 2026-09-15 | **LR11a reading** (LS5; no threshold moves) | "paintOpenBar / paintTape for 43- and 67-onset windows p95 <= 2 ms / <= 3 ms" is read at its strictest: paintOpenBar <= 2 ms at 43 and 67, paintTape <= 2 ms at 43 and <= 3 ms at 67. | `tests/score_lab.test.mjs --browser --sessions` (Chrome 152 headless, ANGLE D3D11 on the RX 9070 XT, three.js r186 scene running; page clock resolution **0.1 ms**, so per-call p95 values are bounds at that resolution): LR11a paintOpenBar p95 0.1 / 0.1 ms and paintTape 0.1 / 0.1 ms at 43 / 67 onsets (PASS). LR11b engraveBar (layout plus draw) p95 0.1 / 0.2 / 0.2 / 0.2 ms at 12 / 24 / 43 / 67 onsets, drawn with Bravura; draw plus a texture upload of the tile (forces the 2D raster) p95 0.4 / 0.5 ms at 43 / 67 (PASS). LR11c one 400 x 280 upload with gl.finish p95 0.1 ms (n 180), a 561 x 360 bar tile 0.1 ms; in-situ settled-tile uploads during replays p95 1.1-1.4 ms (PASS). LR11d 84 settled tiles over two fixture replays and 180 s of S12: 0 drawing calls after settling, 0 re-engraves, 0 re-uploads (PASS). LR11g 0 collisions with widening in the page (PASS). Seeks: a new ribbon each, 0 RangeErrors; after a x2 press 6 ticks show the request dimmed with "hold", 0 blank. 0 page exceptions; burst 5.5 s. **Re-run after repair LS5 r1 (2026-09-15; accidentals per staff, notes tied in from tape left out, the page's stats line shows `tiedFromTape`):** `score_lab --browser --sessions` 24 passed, 0 failed, burst 7.1 s. LR11a p95 0.1 / 0.1 / 0.1 / 0.1 ms; LR11b 0.1 / 0.2 / 0.2 / 0.2 ms, with upload 0.5 / 0.4 ms; LR11c 0.1 ms; LR11d 84 settled tiles, 0 draws after settle, 0 re-engraves, 0 re-uploads (in-situ upload p95 1.1-1.2 ms); LR11e-page in-bar 0.166 s fixed beats, 0.042 s inferred; LR11g 0; seek-hold 2 ribbons, 0 RangeErrors, 0 blank, 6 hold ticks dimmed. 0 exceptions; the one console error is the favicon 404, as before. All pass. | The receipt's two slashes can pair by function or by window size; the gate takes the lower limit wherever the readings differ. |
| 2026-09-15 | **LR11e restated measure** (LS5; no threshold moves) | "Mean head x shift at settle <= 0.5 s on beats that did not widen (0 by construction)" is measured against the **beat's own line** (beat-local); the shift inside the bar and widened beats are reported. | Fixtures (`score_layout`), open layout against the first engraved layout, matched by note id, staff and position. Oracle beats (every LS0 family, 213 takes): beat-local **0.1475 s** (31,799 heads), in-bar 0.1879 s, widened beats 0.3002 s (7,523), 240 heads that changed position, block x0 moved 1.79 px on average over 3,413 transitions. Inferred beats (tempo suite): beat-local 0.0089 s, in-bar 0.0203 s. Settling to settled: beat-local 0 on both. | Not 0: the open bar keeps its beat lines fixed and pushes an overflowing column into the next beat, while the settled bar moves the next beat line instead, so a beat after a crowded one differs; a bar's content can also change between its last open build and its first engraving. A widened beat earlier in the bar moves every later beat line, which the in-bar number carries. |
| 2026-09-15 | **C15 reading for LR11g** (LS5, house engraver; no threshold moves) | A column's width adds **1.0 s per accidental column** (a chord's accidentals stack into columns, one per 6 steps of pitch), **0.6 s per augmentation dot** and **0.8 s for an unbeamed flag on an up stem**; when two voices' heads touch at one position the down-stem chord moves right past the up chord's rightmost head and flag. "Widening off" for the report means heads at their slots with no push. | Dense bench bars (`score/bench.js`: 12, 24, 43, 67 onsets, a 10-head chord column with seconds in both hands, triplets, sextuplets, 16ths, rests, 30% chromatic notes): **0 collisions with widening** at every size; with widening off 1 / 3 / 12 / 44. Clean copies of every LS0 family (oracle beats, 3,347 metric bars): 30 collisions (0.009 per bar: flag/head 10, dot/dot 8, head/head 6, flag/dot 6), reported, not gated (before the voice-shift and dot rules: 42). | C15's "+ 1.0 if accidental" leaves a 10-head chord's stacked accidentals and dotted or flagged columns overlapping the next column, which LR11g counts. |
| 2026-09-15 | **Design note: the ribbon's metric rule and tape wait** (LS5, `score/ribbon.js`; no receipt threshold) | None (construction rules) | A bar draws metric live while no tick inside its own span drew tape (index.js freezeBar's rule read live); groups with no bar wait up to **3 s** while bars draw; groups of a bar whose kind is undecided wait while bars draw; a settled metric bar holding a note already placed as tape is shown as tape. Tempo suite (inferred beats): 382 metric bars in the transcriber, 373 frozen blocks, **9 shown as tape** (52 notes). With a 1.5 s wait: 22 (112 notes). With the first rule (the last tape tick before the bar's start): 74 (578 notes), because a tape tick after a settling bar ended disqualified it. Fixed-beat families: 0. Invariants over 294 fixture takes and S1..Sn: 0 frozen-block changes, 0 tape-column moves, 0 scroll reversals. **Repair LS5 r1 (2026-09-15): notes tied in from tape.** The commit test read `b.notes`, the onsets inside the bar, but the frozen model draws every piece in `measure.voices`, bar-line tie continuations included. A note whose onset went to tape was drawn again as a tied-to head at position 0 of the next frozen bar, with a half-tie from the tile's left edge. The ribbon's conflict counter never saw it, so the first report's "no note is drawn twice" did not hold. **Verifier's probe, confirmed:** tempo suite, 22 notes in 14 of 81 takes, all `tieStop` at position 0; oracle families and S1..Sn 0. A per-tick probe found a second case: **86 notes on tape also drawn in a live block** (the verifier counted frozen blocks only). **Rule (`ribbon.js` `tapedTies: "omit"`, the default):** every piece of a note already placed as tape leaves the drawn voices of a later bar, live or frozen (`stats.tiedFromTape`). Its tape column and duration line draw it. A bar whose own onset went to tape is still shown as tape. **Measured by a per-tick probe on the tempo suite (81 takes, replays padded 6 s past the last event; pre-repair → omit → the alternative `"demote"`, which shows such a bar as tape):** frozen-and-tape notes 22 → 0 → 0; live-and-tape notes 86 → 0 → 87; frozen blocks 373 → 373 → 353; demoted bars 9 → 9 → 29 (conflicts 52 → 52 → 76); notes left out 0 → 22 → 0; notes never drawn 0 in all three. Oracle families (213 takes): 0 notes drawn twice in every mode, 3,521 frozen blocks, 0 demoted. S1..Sn (18 sessions): 0 notes drawn twice in every mode, 0 frozen blocks (`rung4Floor` 8), 1 demoted bar and 8 conflicts in every mode. The test (3 s pad) reads 22 and 86 on the pre-repair rule, 3,788 frozen blocks over the 294 takes. **Receipt:** LR11d-model now also requires 0 notes drawn twice (in a frozen block, any piece, and on tape; in two tape columns; an onset in two frozen blocks) and 0 live blocks drawing a taped note, on the fixtures and in the `--sessions` invariants. A mutant with the pre-repair rule fails it (22 and 86). | Settled means never repainted, and a note is drawn once: the ribbon cannot move a tape column into a bar that settles later, so it waits where waiting is cheap and shows the rare remainder as tape. Leaving the tied-in pieces out keeps the bar metric and keeps live blocks honest. Demoting keeps 20 more bars off the page on the tempo suite and still draws 87 notes twice while live. The price: the frozen bar shows no head for that voice until its next onset, while the tape duration line runs to the note's sound end. |
| 2026-09-15 | **Design note: accidentals carry per staff** (repair LS5 r1, `layout.js` `modelBar`; no threshold moves) | None (a construction rule) | **Verifier's probe, confirmed:** C major, 4/4; staff 1 voice 1 F#4 (MIDI 66) at position 0, staff 2 voice 4 F#4 at 24. The running accidental was keyed by letter and octave only, so the bass note got no sharp and read F natural (a wrong pitch on the page). The key is now staff, letter and octave. Within a staff an accidental still carries across voices to the bar line, and a tied-to piece never reprints one. `tests/score_layout.test.mjs` pins three cases: the verifier's (both sharp, both drawn), a staff's own cancel (F4 in the bass after a treble F#4 is plain, F4 in the treble after it takes a natural), and one staff's two voices (F#3 twice, one sharp). Against the old key, 2 of the 3 checks fail. **Knock-on (more accidentals drawn, so more beats widen):** LR11e oracle beat-local 0.1475 → **0.1496 s** (unwidened heads 31,799 → 31,658; widened 7,523 → 7,664 at 0.3002 → 0.303 s; in-bar 0.1879 → 0.1867 s); inferred beat-local 0.0089 → **0.0096 s** (in-bar 0.0203 → 0.0211 s; this suite also carries the tied-in rule above). Both pass (≤ 0.5). LR11g: bench bars 0 with widening at every size, family clean copies 30 collisions, the same kinds as before. | Standard notation: an accidental holds for its staff position to the bar line and does not reach the other staff. |
| 2026-09-15 | **LS5 scope notes** (no receipt change) | (1) The slice's orders say VexFlow is not approved, while ls1-rulings.md's LS6 rulings (later the same day) approve it for a beta. LS5 ships the house engraver only, behind the section 0 interface; `engrave_vexflow.js` is left for the slice that loads VexFlow. (2) `score_cli.mjs` (LS4) does not exist yet, so the lab server that serves `/session/S<n>/events.jsonl` lives in `tests/score_lab.test.mjs --serve` (port 8921) until the CLI's `bench` lands. (3) The lab spells with `spell.js` (the TN2 shared speller, now on disk) in the key its own key tracker settles, in place of section 0 rule 2's `spellInKey` approximation; still injected, still labelled as a lab approximation (no chord cue). | | |
| 2026-09-15 | **LR9a, LR9b, LR9c** (LS4; no threshold moves) | None: LR9a 100%, LR9b 0 ms, LR9c 100%. Read by two readers: `verifyExport` in `arsenal/score_cli.mjs` and `tests/test_score_export.py` (Python stdlib: xml.etree and its own SMF parser). LR9a also requires the sounding notes to equal the score's own notes (tick, pitch, length), and in Python `<tied>` to agree with `<tie>`. | Fixtures (`node tests/score_export.test.mjs`): 437 exports (every LS0 family on held-out seeds with oracle beats (213) and inferred beats (213), 9 free pieces, 2 tape runs). LR9a 437 / 437: 79,986 sounding notes = 79,986 log note-ons, 20,537 voice streams sum to their measure, 0 unmatched ties. LR9b 437 / 437: 79,986 ons, 79,986 logged offs plus 3,483 end offs (the free pieces log no offs), 10,813 CC64 crossings, 0 differences. LR9c 437 / 437: 79,986 notes, 0 tick / pitch differences. S1..Sn (`--sessions`, i.e. `score_cli bench`, all 23 sessions with a note-on, none skipped): LR9a 23 / 23 (30,352 = 30,352; 6,080 streams; 0 unmatched ties); LR9b 23 / 23 (30,352 ons, 30,352 offs plus 2 end offs, 4,008 crossings, 0 non-crossing pedal events, 0 velocity clamps); LR9c 23 / 23. Python on the same files: identical counts; pedal sequence 0 bad, 0 bad attributes. LS4-audit (the five audit.mjs properties on the export view, forced bars included): 0 on 437 fixture views (92,365 pieces) and 23 session views (38,826). CLI exports (S6 lr10; a 30 s S12 span in 3/4; an S14 span with --taps and --one; S10 with --bpm 70 --feel triplet): 4 / 4 on both readers. `score_export` 71 passed, 0 failed. | Plan 10.3. Before the lane rule in the next row, the same run failed LR9a and LR9c on 3 inferred fixture exports and on S1, S2, S12 and S14: one tie chain each was written stop-before-start (10 unmatched ties on sessions). Both readers reported it identically. |
| 2026-09-15 | **Design note: extra MusicXML voices keep a note's lane** (LS4, `musicxml.js` `lanesOf`; no receipt threshold) | None (a writer rule) | A score voice can hold pieces of different lengths at one position (a chord member released early, C1), and rarely an onset while an earlier note still sounds. MusicXML gives a chord one length, so those pieces go to an extra voice (voice × 100 + lane) padded with `<forward>`. First fit put a later piece of a tie chain back into lane 0, which is written first, so the stop came before its start in the document: the failures in the row above. **Rule:** a note's later pieces take the lane its earlier pieces took, and a bar-line continuation takes the lane it had in the previous bar, when that lane is free. S1..Sn: 369 chord splits, 22 overlap splits, 342 extra lanes. Ties still crossing MusicXML voices: 84 on sessions (18 in the LR10 take), 104 on fixtures; both readers pair ties by staff and pitch, so these pass. | Order-dependent importers read document order. The rule removes every stop-before-start without moving a note. Cross-voice ties stay open until LR10. |
| 2026-09-15 | **Design notes: the export view and the writers** (LS4, `export.js`, `musicxml.js`, `midi.js`; no receipt threshold) | None (construction and presentation rules) | (1) **Forced bars (C3).** clean() leaves notes outside every segment as tape (783 of 30,243 notes on the 18 sessions with 100+ onsets). `forceTape` re-transcribes each run on a fixed grid and merges the bars in time order. A run splits at a 2 s gap or a measure start. The grid uses the median tactus beat of the nearest segment, with the first beat 3% of a beat before the run's first onset. S1..Sn: 82 runs, 892 notes, 144 of 2,113 measures, 0 notes left as tape. 11 tape notes lie inside a clean measure's span; their bars are written after it. Fixtures (inferred): 287 runs, 2,825 notes, 417 measures. (2) **Marks.** `freely` goes on every forced or non-metric measure (all 112 in the LR10 take). A metronome `c. N` goes at each phrase start: N is the phrase's median tactus BPM, with a dotted-quarter unit in compound meters. `rubato` is added when the phrase's bar tempos have p90 / p10 > 1.08 and the phrase is not forced (0 of the 9 rub0 4/4 pedal takes, 9 of the 9 rub2). Every measure gets `<sound tempo>`. (3) **Pedal.** Marks are placed again by time on the merged measures. A stop or change with no open pedal, a start while one is open, and a pedal still open at the end are repaired: 13 on sessions, 144 on fixtures, mostly con Ped. phrases that dropped a start or a stop. (4) **--bpm** pins a fixed tactus grid: rhythm `unverified`, every measure forced. (5) **Key.** The key areas come from summary.json. S1-S4 have none there, so they use the key the page's tracker logged on chord events. Spelling uses the page's `spellForKey`, built read-only from piano.js with its spell.js and chordread.js imports (piano.js no longer has the old `const ODD_NAMES` anchor): 0 unspelled notes. (6) **index.js.** Bars gain `beats_ms` (tactus beat times, not in the bar signature), and output measures gain `cut`. Re-run alone: LR5 S12 p95 **0.2919** ms per group (LS3 r1 0.2701; limit 0.5); LR6a 0 over 2,064 settled bars; LR6b lag 4 vs 8 0.0002; LR6c median 63.7. `score_settle --sessions` 41 passed, `score_hands --quick` 59 passed. | C3 and plan 7.4. The rubato band is the plan's 4% "a tempo" band read both ways. |
| 2026-09-15 | **LR10** (LS4; dated drill receipt pending) | None | Prepared with `node arsenal/score_cli.mjs export S6 --take lr10`, which writes `state/arsenal/score/<S6 session>/lr10/take.musicxml`. S6: 6.9 min, 2,135 note-ons, 112 measures (4 forced), pedal start / change / stop 27 / 31 / 27, 8 metronome marks (4 with rubato), key from summary.json; LR9a-c pass on both readers. Not opened here: no MuseScore or Dorico on this machine, and no installs. The receipt waits for Daniel. | Plan 10.3. |
| 2026-09-15 | **LS4 scope notes** (no receipt change) | (1) `score_cli.mjs` has no `serve` command, so the LS5 lab server stays in `tests/score_lab.test.mjs --serve`. (2) Not in LS4: grace notes; `<arpeggiate>` for rolls; chord symbols; courtesy accidentals; a pickup written without its leading rests (a pickup measure is `implicit="yes"` and keeps the rests before its first onset); notes already sounding at a span start carried in as ties (plan 8 step 1). Directions (pedal, dynamics) are placed by `<offset>`. (3) musicxml.js's fallback names (sharps in sharp keys, flats in flat keys) run only when a note has no injected spelling: 0 times on every run. | | |
| 2026-09-15 | **Design note: the pin is per voice, and pinMerged is counted** (ls1-rulings.md "LS2close to LS5 rulings", the pin; a construction rule, no receipt threshold) | Whole onset group pinned to the frozen continuation's end → **only the group's notes in the conflicting voice move** (`index.js` build); its notes in other voices stay where they were heard. `stats()` now counts when a bar settles, so a pin that a later build undid is not counted: `pinned` (onset groups with a pinned note), `pinnedNotes`, and `pinMerged` (a group's pinned voice landing on the tick of another group's note in that voice; the two merge into one chord). | Pre-change copy → this code, live copies. **294 inferred-beat fixture takes:** 16 pinned notes in settled bars, 9 of them staff-1 notes in voices 1 and 2 (moved for no reason, since a bar-line continuation is always C7's held bass, voice 4) → 7, all voice 4. Bar ties 1,725 → 1,725; overlaps, dangling ties, untiled notes, two-bar-line notes and bad values 0 → 0; note pieces 70,779 → 70,896. **S1..Sn (18 sessions):** 6 pinned notes, 3 on staff 1 → 3, all voice 4; bar ties 417 → 417; audit 0 → 0; note pieces 37,549 → 37,551. **Tracker sweep** (3,456 variants: 4/4, 3/4, 6/8; beats 450, 600, 800 ms; tempo ×0.7-×1.5 as a step or a 4-bar ramp; the later bass as a single note, a 40 ms bass chord, or with a treble note): pinned notes 280 (55 on staff 1) → 225 (0 on staff 1); overlaps 0 → 0; late changes 0 → 0; bar ties 22,407 → 22,407. Freeze-time stats on this code: fixture takes pinned 7 groups, pinnedNotes 7, pinMerged 0; S1..Sn 3, 3 and 0. The old build-time counter read 160 pinned groups on the fixture takes, most of them pins later builds undid. **Tests (`score_settle.test.mjs`):** a new per-voice fixture from the sweep (4/4, a 450 ms beat stretched ×1.3 over a 4-bar ramp from bar 5, bass 38 at 0.9 of odd bars with a treble 79). At 17500 ms the 38 is pinned 372 → 376 and the 79 stays at 372; pinned 1, pinnedNotes 1, pinMerged 0. The existing tracker-beat fixture now asserts pinMerged 1: its pinned 38 joins the bass 36 at tick 324. Run against the pre-change modules, these 3 checks fail. | The ruling. It cited 12 of 19 fixture pins and 3 of 7 session pins, counted at LS2close; on the LS3-LS5 code (hands.js voices) the same census reads 9 of 16 and 3 of 6. |
| 2026-09-15 | **Rule change: clefs and octave lines** (ls1-rulings.md "LS2close to LS5 rulings", LS3: clef versus octave lines, and beyond three ledger lines; supersedes the LS3 r1 design note above "every lower-staff note within 3 ledger lines") | The LS3 r1 rule (a bar the clef cannot hold takes the other clef at once, else an octave line on either clef; `outOfRange` "clef" or "octave") → **`hands.js` `clefStep` / `createClefTracker`**. (1) 8va and 15ma only above a treble clef, 8vb and 15mb only below a bass clef; staff 1 is always treble. The ruling says "8va only above the treble staff and 8vb only below the bass staff"; this reads it by clef, so a lower staff in treble clef takes no 8vb, as the LS3 r1 verifier's count of treble-clef 8vb bars implied. (2) A lower-staff clef change needs 2 consecutive bars that want the new clef. A bar wants the clef whose plain or single-octave-line range holds it when only one does; when both do, treble if every note is at or above C4, else bass (plan 6.5); when neither does, nothing. The rule is read with **lookahead**: the change sits at the first of the 2 bars (`clefLookahead: true`); the causal reading, at the second, is reported. A clef holds for at least 2 bars, so there are no one-bar round trips. (3) A shorter excursion keeps the clef: an octave line where the clef allows one, else ledger lines, counted as overflow "excursion". (4) 15ma / 15mb only for a passage of at least 2 distinct notes still beyond 3 ledger lines under the better of no line and the single line; a single note stays on its ledger lines and is counted ("single"); a bar no clef holds keeps the fewest notes beyond ("span"). (5) The clef never changes mid-bar (decisions are per bar). **Live:** `ribbon.js` commits each resolved bar once, with the next known bar, so a frozen bar's clef and octave line are final; `index.js` notate, `clean()` and `export.js` use the finished sequence. **Drawing and export:** `layout.js` staffStep ±15, `engrave.js` 15ma / 15mb glyphs (SMuFL U+E515, U+E51D), `musicxml.js` octave-shift size 15, placed above for 8va / 15ma and below for 8vb / 15mb (was: by staff). Scores carry `ledgerOverflow`. `outOfRange` is removed, because the ruling fixes the order. | **S1..Sn clean copies** (18 sessions, 1,969 bars, meter 4/4), LS3 r1 rule → ruled rule. Clef changes 84 (4.27 per 100 bars, worst S18 13.3) → **30 (1.52 per 100 bars; 5 "bars", 25 "range")**; worst session of 100+ bars S14, 7 in 216 (3.24); shorter sessions S13 2 in 71, S15 1 in 34, S18 2 in 30, the rest 0. One-bar round trips 21 → **0**. Octave-line bars: staff 1 8va 300; staff 2 bass 8vb 275, bass-clef 8va 30, treble-clef 8vb 19, treble-clef 8va 2 (49 now forbidden) → **staff 1 8va 291, staff 1 15ma 9; staff 2 bass 8vb 275, treble-clef 8va 1**. Note pieces beyond 3 ledger lines: staff 1 55 → **23** (18 bars: 15 single, 3 span); staff 2 25 → **156** (81 bars: 47 single, 16 excursion, 18 span). Causal reading: 31 changes, 0 round trips, staff-2 overflow 241 pieces on 106 bars (41 excursion). **Fixtures** (213 takes, 3,363 bars): 61 changes (4 "bars", 57 "range"), 21 round trips, 145 octave-line bars (101 bass-clef 8va, 44 treble-clef 8vb, all now forbidden), 52 staff-2 pieces beyond on 48 bars → **0 changes, 0 round trips, 0 octave lines, 225 staff-2 pieces beyond on 166 bars (123 single, 43 span)**. The fixtures' left-hand figures A2-C5 fit neither clef, so without a bass-clef 8va they stay on ledger lines. Causal reading: identical. **Receipts (`score_hands.test.mjs`):** an independent rule check on the clean copies (forbidden lines; beyond counts; an unused or missed octave line; 15ma without a passage; a change not wanted by that bar and the next; a return within 2 bars; a missed change) reads 0 on fixtures and on S1..Sn (`LS3-clefs-fixtures`, `LS3-clefs-sessions`: pass); on the causal reading it flags 37 on S1..Sn. 12 new contracts plus a sweep of 400 × 16 random bars: 0 violations, 0 round trips, the tracker equals the finished sequence, and a prefix gives the same rows except its last bar. `score_export`: a 15ma run writes octave-shift size 15 above staff 1. | The ruling. The overflow rise is its price: bass-clef 8va and treble-clef 8vb are forbidden, a clef change needs 2 bars, and a single note stays on ledger lines. |
| 2026-09-15 | **LS3-clefs-sessions target** (ls1-rulings.md LS3: "at most 5 clef changes per 100 bars on S1..Sn, and 0 one-bar round trips"; newly fixed, recorded before the claim) | Target in words → gated as **≤ 5 clef changes per 100 bars pooled over S1..Sn and on every session with at least 100 bars**, plus **0 one-bar round trips on every session**. A session under 100 bars reports its change count; it has no rate gate. | Pooled 1.524; sessions of 100+ bars: worst S14 3.24 (7 in 216). Under 100 bars: S18 2 in 30 (6.67 per 100), S13 2 in 71 (2.82), S15 1 in 34 (2.94), 8 sessions 0. Round trips 0 on every session. Pass. | "Per 100 bars" is a rate, and the smallest legal excursion is 2 changes (to treble and back, with the 2-bar hold). On a 30-bar session that is 6.67 per 100 bars. The LR3b ruling set the same reading for rates on short sessions. |
| 2026-09-15 | **LR7 voice F1, LR3d hit and LR8: reported as PENDING RULING** (no threshold moves; how `tests/score_hands.test.mjs` gates them) | FAIL (the LS3 rows above) → still FAIL against their thresholds, and the RECEIPT lines keep `pass: false`. The suite now reports each as `PENDING RULING` rather than a failure while it is no worse than its recorded value, and fails it on regression. The guards are the recorded values: LR7 voice F1 ≥ 0.8587; LR3d hit ≥ 3 of 27 takes; LR8 worst session tuplet brackets ≤ 4.5, ties ≤ 6 and dynamics ≤ 4.4135 per min, with voices per staff, pedal marks and key signature changes at their thresholds. The summary line counts the pending receipts separately. | This run: LR7 staff 0.9658 (pass), voice F1 0.8588; LR3d grid hit 0.1111 (false within 32 beats 0.0469, gated, pass); LR8 worst session tuplet brackets 4.5, ties 6, dynamics 4.41 per min (S18). **LR7 breakdown** (clean copies, 213 takes, voice links pooled: 51,643 truth, 50,965 estimated, 43,852 shared). Missed links: 3,450 through a wrong staff, 3,572 through a voice split, 759 through a different onset tick. Extra links: 3,208 staff, 1,837 voice merge, 2,068 onset. Arpeggio textures carry the staff losses (1,500 missed, 1,855 extra) and ballads the voice-rule losses (2,723 missed, 1,287 extra). | None of the "LS2close to LS5 rulings" covers these receipts, and none is a code defect with a failing case. The losses sit in the conventions the LS3 rows left open: the T5 cost for left-hand arpeggio tops and the sustained-lower voice rule (LR7), the tracker's level for 12/8 under 4/4 (LR3d), and how LR8 counts ties, metric bars and short-session dynamics. So the thresholds stay, and an awaited ruling is not reported as a regression. **Open for the conductor:** the three questions in those rows. |
| 2026-09-15 | **Design note: a group the per-voice pin split goes to tape only for its undrawn notes** (a defect found in this round, repaired in `ribbon.js` resolve; no receipt threshold) | A group placed as tape → only its notes not already drawn in a committed bar go to tape; the notes the bar drew stay there (`stats.splitTaped` counts such groups). | **Defect, found by the LR11d model check** after the per-voice pin: `score_layout --sessions` 61 passed, 1 failed, with **29 notes drawn twice**, all on the inferred-beat tempo suite and all frozen first, then taped. In each case the pin moved one bass note of a group to the continuation's end on the next bar line; that bar was dropped when the segment closed, so the bass went to tape. The ribbon then placed the whole group as tape, and its other notes were already frozen in the bar (for example a bar 4 holding 69 and 72 whose 45 had no bar). **Isolated:** a scratch copy of this code with the whole-group pin restored reads 0 drawn twice. **After the repair:** 0 drawn twice on 294 takes, 13 split groups taped (tempo suite), and commits, demoted bars and tied-from-tape notes the same as the pre-change and whole-group copies (373 and 3,415 commits; 9 demoted; 22 tied from tape). `score_layout --sessions` 62 passed, 0 failed. | A note is drawn once, and the ruling makes a group splittable across voices, so the drawer handles a split group. |
| 2026-09-15 | **LS5 leave-out rule: a hidden rest keeps the two-voice layout** (ls1-rulings.md "LS2close to LS5 rulings", LS5; no receipt threshold) | A left-out tied-in piece removed from its voice → **the voice keeps a hidden rest** at that piece's position and value (`ribbon.js` `omitPieces`, used by `drawable`). `layout.js` `modelBar` counts a voice with a hidden rest as present, so a staff that had two voices keeps them for that bar; `layoutBar` gives a hidden rest no column, glyph or box. | The LS5 r1 verifier's probe, now a contract (`score_layout.test.mjs`): staff 1 voice 1 plays E5 quarters; voice 2 holds a C5 half tied in from tape and then a half rest at 48. Voice 1 stays twoVoices with stems up, voice 2 keeps a hidden half rest at 0, and the half rest at 48 is drawn as a lower-voice rest (below the middle line); the hidden rest draws nothing. A control without the hidden rest flips voice 1's stems down. Against the pre-change modules, the 2 leave-out checks and the 15ma staff-step check fail. **Knock-on on LS5 receipts** (with the clef rule and the pin; no threshold moves): LR11e oracle beat-local 0.1496 → **0.1494 s** (unwidened heads 31,658 → 31,680, in-bar 0.1867 → 0.1797, widened 0.303 → 0.3002 on 7,664 → 7,642); inferred beat-local 0.0096 → **0.0047 s** (in-bar 0.0211 → 0.011). LR11g bench bars 0 with widening at every size; family clean copies 30 → 29 collisions. LR11d model 0 changes, 0 moves, 0 reversals, 0 drawn twice, 0 live on tape (22 tied from tape, 9 demoted, 3,788 commits). LR11f sessions pooled 2.3794 per tape minute (unchanged), 1.5 s fixture 0. C16 band x 110-940, bottom 1436. | The ruling: stems must not flip mid-phrase. |
| 2026-09-15 | **Must-fix: a beam group never spans an unbeamable item** (ls1-rulings.md "LS2close to LS5 rulings", LS4 beam writer; a writer and construction rule. LR9a gains a beam structure check on both readers; no threshold is loosened) | `measures.js` made one group per beat and voice over every beamable piece, and `musicxml.js` wrote begin / continue / end across every item between them → **`measures.js` splits each beat into runs of consecutive beamable positions**: a rest, or a position holding a quarter or longer piece, ends a run, and groups carry `pos`. **`musicxml.js` writes begin..end only over consecutive beamable chords of the lane's own stream**, cut by a rest, an unbeamable or out-of-group chord, or a `<forward>` gap. Reading: "split at any quarter or longer note or rest" is read as any rest, and any note of a quarter or longer. **LR9a** (`verifyExport` and `tests/test_score_export.py`) now also fails on any of: a beam on a quarter or longer note; a continue or end with no open beam; a begin inside an open beam; an unbeamed note, rest or `<forward>` of the voice inside an open beam; a beam still open at the measure end. | **Failing cases pinned** (`tests/score_export.test.mjs`). The verifier's S4 shape, voice 4, beat 4 (a sextuplet 16th, a sextuplet quarter, a 16th): `measures.js` grouped ids 1 and 3 across the quarter, and now makes no group. The writer, handed the old group, writes no beam. The reader counts 2 violations on the injected begin / quarter / end XML. 6/8 end to end: eighth, eighth rest, eighth was written begin / rest / end and is now unbeamed; the 16th run stays one begin..end. **Pre-change modules → this code, with the new checker on both.** Fixtures (426 oracle and inferred family exports): beam violations 422 → **0**, `measures.js` groups spanning an unbeamable item 264 → **0**, groups 17,395 → 17,175. S1..Sn (23 sessions): violations 310 → **0**, spanning groups 208 → **0**, groups 7,525 → 7,349, beams written 7,469 → 7,343; the writer had to cut 0 groups. Against the pre-change modules, the 4 new beam contracts fail. | The ruling. An importer can misattach a beam that crosses a quarter or a rest (an LR10 drill risk). The same groups drew one horizontal beam across the quarter in the lab's engraver, so the split is made at the source and again in the writer. |
| 2026-09-15 | **Rule change: "freely" once per free-time passage, "a tempo" where a tracked beat resumes** (ls1-rulings.md LS4; supersedes C3's "every measure marked freely" and the LS4 design note's point (2); no receipt threshold) | `<words>freely</words>` on every forced or non-metric measure → **`musicxml.js` `freeTimeMarks`**. A measure has a tracked beat when it is metric, not forced, and drawn from the jam, song or taps rung. "freely" goes on the first measure of each run of measures without one, and "a tempo" on the first tracked measure after such a run. Rung-4 (inferred) bars count as free time, per the ruling's "a clean-copy export with no taps and no jam beat is one free-time passage". `export.js` `marks.freely` and the new `marks.aTempo` use the same rule. Metronome "c. N" marks per phrase and `<sound tempo>` per measure are unchanged. | S1..Sn: "freely" 2,111 → **23** (one per session; all 23 are unverified takes), "a tempo" 0. Fixtures (426): 4,029 → 229, "a tempo" 0 → 16. `--bpm` pinned take: 16 → **1**. The S14 `--taps` take: "freely", then "a tempo" at the tapped segment, then "freely" again (2 and 1). Contracts: fixed beats 0; an inferred copy with 17 free measures has 1 at the top; a jam copy with measures 2-4 forced has "freely" on 2 and "a tempo" on 5. Against the pre-change modules, the 4 freely contracts fail. | The ruling. |
| 2026-09-15 | **Rule change: `implicit="yes"` only for the opening pickup; a short measure writes its own time signature** (ls1-rulings.md LS4 short measures; LR9a gains the check on both readers; no threshold is loosened) | `implicit="yes"` on every pickup (mid-score segment starts included), and short cut bars left unmarked → **implicit only on measure 0 when it is a pickup shorter than the bar**. Any other measure whose length differs from the meter writes a `<time>` of its own (`timeSignatureOf`: the fewest beats over the meter's beat type or a shorter power of two, so 48 ticks in 4/4 is 2/4, 36 is 3/8, 72 in 12/8 is 6/8), and the next measure writes the meter again. **LR9a:** a measure's length must equal the time signature in force, unless it is the implicit opening measure; `implicit="yes"` anywhere else fails. | S1..Sn: mid-score implicit measures 73 → **0**; measures whose length disagreed with the time signature 75 → **0** (73 implicit, 2 unmarked); `<time>` elements 23 → 173 (75 short measures, each set and then restored). Fixtures: mid-score implicit 245 → **0**, disagreeing measures 253 → **0**, 9 implicit opening pickups, 262 short measures. No length needed a beat type below a 64th (`timeUnwritable` 0). Under the new LR9a, the pre-change writer passes 157 of 426 fixture exports and 4 of 23 sessions; this code passes 426 / 426 and 23 / 23. Against the pre-change modules, the 4 short-measure contracts fail. | The ruling: never an unmarked short measure. |
| 2026-09-15 | **Rule: accidentals, one rule for the export and the lab** (ls1-rulings.md LS4 and LS5; `score/accidentals.js` `decideAccidentals`, called by `musicxml.js` and `layout.js` `modelBar`; no receipt threshold) | Per staff, letter and octave in position-then-pitch order, with the first copy deciding: a clash at one position printed only one of its members, duplicates in two lanes printed only one copy, and there were no courtesy accidentals → **per staff and staff position, in position-then-pitch order**. Two alterations of one staff position at one time position both print. After a tied-in note (a tie-stop piece at the bar's start), a later different alteration of that staff position prints a **courtesy accidental in parentheses** when no accidental would print otherwise: MusicXML `<accidental cautionary="yes" parentheses="yes">`; the lab draws SMuFL accidental parentheses (U+E26A, U+E26B), and the accidental column widens to glyph + 2 × 0.35 sp. Duplicate pitches at one time position share the decision. Readings: "that letter" means the same staff position (letter and octave), the unit the state is kept in; after a clash the state is the last written alteration (the higher pitch). | Contracts on both writers: F4 + F♯4 → a natural and a sharp. A tied-in F♯4, then F4 → a courtesy natural, then F♯4 → a sharp. The same spelling after a tie-in → its own accidental, no courtesy. Duplicate A♭5 in two MusicXML lanes, and F♯3 in the lab's voices 3 and 4 → both copies print. Against the pre-change modules, 3 export and 6 layout checks fail; one of the layout failures comes only from the old model having no courtesy field. S1..Sn: accidentals 2,151 → 2,164, courtesy 0 → **3**, 35 clash positions, clash members printed without an accidental 10 → **0**. Fixtures: 6,142 → 6,170; duplicates that disagreed 3 → **0**; silent clash members 25 → **0**; courtesy 0. **Knock-on on LS5** (`score_layout --sessions` 68 passed, 0 failed; the beam split adds flags to pieces that are now unbeamed, which also widens columns). LR11e oracle beat-local 0.1494 → **0.1557 s** (unwidened heads 31,680 → 31,539; widened 7,642 → 7,783 at 0.3027 s; in-bar 0.1847 s). Inferred 0.0047 → **0.0035 s** (in-bar 0.0112 s). LR11g: bench bars 0 with widening at every size (1 / 3 / 12 / 44 without); family clean copies 29 → 29. LR11d model unchanged (0 changes, 0 moves, 0 reversals, 0 drawn twice, 0 live on tape; 22 tied from tape, 9 demoted, 3,788 commits). LR11f pooled 2.3794 per tape minute. C16 unchanged. | Standard engraving practice, per the ruling. |
| 2026-09-15 | **Run notes: LS4 export conventions round** (no receipt change) | (1) `tests/score_lab.test.mjs` ports 8921 / 9921 → **8981 / 9981**, this build's range on this machine. (2) The CLI takes were regenerated with the current CLI: S6 `lr10`, S10 `--bpm 70 --feel triplet` (with and without `--one 2`), the 30 s S12 span in 3/4. The S14 `--taps` take's tap file is not on disk, so its `take.musicxml` was rewritten from the stored export view (`score.json`) by the current writer; its measure lengths equal its manifest, and the MIDI files are untouched. (3) LR10 still waits for Daniel's drill, on the regenerated S6 `lr10` take (112 measures, 1 "freely", 8 metronome marks). | `score_export --sessions` **88 passed, 0 failed**: LR9a / LR9b / LR9c 437 / 437 fixtures (79,986 sounding = 79,986 note-ons; 20,537 streams; 0 unmatched ties; 10,813 CC64 crossings, 0 differences) and 23 / 23 sessions (30,352 = 30,352; 6,080 streams; 4,008 crossings); LS4-audit 0 on 437 and 23 views. `py tests/test_score_export.py`: cli 5, fixtures 437, sessions 23, all three receipts pass. Before the CLI takes were regenerated, it failed LR9a on 4 of the 5 old CLI folders under the new checks. `score_layout --sessions` 68 / 0. `score_lab --browser --sessions` **24 / 0** (Chrome headless, ANGLE D3D11 on the RX 9070 XT, three.js r186; waited for the GPU lock another build held, burst 5.5 s, lock released): LR11a paintOpenBar / paintTape p95 0.1 / 0.1 ms at 43 and 67 onsets; LR11b engraveBar p95 0.1 / 0.2 / 0.2 / 0.2 ms at 12 / 24 / 43 / 67, with upload 0.4 / 0.3 ms; LR11c 400 × 280 upload p95 0.1 ms (n 180); LR11d 84 settled tiles, 0 draws after settle, 0 repaints, 0 re-uploads (in-situ upload p95 0.9-1.3 ms); LR11e-page in-bar 0.164 s fixed beats, 0.001 s inferred; LR11g bench 0 with widening (1 / 3 / 12 / 44 without); seek-hold 2 ribbons, 0 RangeErrors, 0 blank, 6 dimmed hold ticks. Unchanged elsewhere: `score_fixtures` 1885 / 0, `score_beat` 133 / 0, `score_quantize` 68 / 0, `score_hands --sessions` 69 / 0 with the same 3 pending receipts, `score_settle --sessions` (run alone) 44 / 0 (LR5 S12 p95 0.3045 ms per group; LR6a 0 over 2,064 settled bars; LR6b lag 4 vs 8 0.0001; LR6c median 63.7). | |

---

## 6. Slice checklist (LS0-LS5)

None of these slices touch jam-held files. No git state changes: the seat-holder commits.

**LS0: fixtures and metrics**
- [ ] `tests/fixtures/score/gen.mjs`: deterministic by seed, TPQ 24, with an `s3Compat` mode (LR1).
- [ ] New synthetic families, all with ground-truth durations and voices:
  - [ ] sextuplet arpeggios, 5+ groups in 10-22% of beats;
  - [ ] 32nd figures;
  - [ ] pedal model: changes per harmony, 30% of notes released at 20-60% of written length under the pedal, `sound_end` events with `by`;
  - [ ] long pedalled bass (about 20% ringing past a bar);
  - [ ] voiced top line (+15-25 velocity);
  - [ ] 12/8 played under 4/4;
  - [ ] count-in taps;
  - [ ] tape density runs (1.5 s and 4 s at 10 groups/s).
- [ ] Metric helpers: beat F ±70 ms, Acc1/Acc2, flips, bands, `onsetsExact`, `beatsExact`, `barsOnsetExact`, `barsFullyExact`, duration exact, rung-4 shown precision, rests per voice per bar, accents per minute.
- [ ] `tests/score_fixtures.test.mjs`: LR1.

**LS1: onsets and beat**
- [ ] `onsets.js` and `beat.js` per plan 6.1-6.2, plus `subdivision` output and count-in rung-3 validity (K = 4 start).
- [ ] `tests/score_beat.test.mjs`: LR2a-g, LR3a.
- [ ] Local bench over S1..Sn (`score_cli.mjs bench` arrives in LS4; a scratch runner is fine here): LR3b, LR3c, and the C2 ×2-family report. Outputs to `state/arsenal/score/`.

**LS2: rhythm and settle**
- [ ] `quantize.js`: the C2 alphabet and λ tables, feel, `div8` off, loose export on the d = 6 grid.
- [ ] `measures.js`: the C1 and C7 duration rules, injected `voiceOf`.
- [ ] `settle.js`.
- [ ] `index.js`: `clean()` options `taps`, `one`, `feel`, and the `rhythm` flag. `clean()` builds a new beat tracker per pass and per seek: `beat.js` has a monotonic clock, and `tick()` throws a `RangeError` when time goes back (ls1-rulings.md).
- [ ] `tests/score_quantize.test.mjs`, `tests/score_settle.test.mjs`: LR4a-k, LR5, LR6a-c.
- [ ] Re-run the section 3 tables with the amended alphabet and the compound set; put them in the slice receipt. Decide `div8` and record it in section 5.

**LS3: hands, marks, meter**
- [ ] `hands.js`.
- [ ] `marks.js`: accents off, `"voice"` option, pedal notches.
- [ ] `meter.js`: meter suggestion plus subdivision chip.
- [ ] Header after a family press (ls1-rulings.md): until the readout confirms, show the requested tempo (`levelBpm`), dimmed, with the word `hold`. Never show a blank. Since LS1fix r1 the readout (`bpm`) reads the request from the tick after the press. The bar-confirmed `bpmShown` keeps the old number until the new tempo has held for a bar: on the button pins that takes 1.5-2.75 s for ×2, 2-3.5 s for ×1.5, 4-7.5 s for ×2/3 and 5.25-10 s for ×0.5 (the longest is 12/8). The header covers that wait.
- [ ] Spelling injected (section 0 rule 2).
- [ ] `tests/score_hands.test.mjs`: LR7, LR3d.
- [ ] LR8 with LR8b-d on local S1..Sn clean copies.

**LS4: export and CLI**
- [ ] `musicxml.js`: `freely` once per free-time passage and `a tempo` where a tracked beat resumes (ls1-rulings.md LS4; was "on every measure of unverified spans"), `sign="no"` on line pedals, `implicit` only for the opening pickup, beams never across an unbeamable item, the shared accidental rule.
- [ ] `midi.js`: SMF type 0 for `performance.mid` with CC64 0/127 crossings; SMF type 1, PPQ 480 for `quantized.mid`.
- [ ] `arsenal/score_cli.mjs clean|export|bench` with `--at`, `--seconds`, `--meter`, `--one`, `--bpm`, `--feel`, `--taps`; writes only under `state/arsenal/score/`.
- [ ] `tests/score_export.test.mjs`, `tests/test_score_export.py`: LR9a-c. Prepare one `take.musicxml` for LR10 (Daniel's drill).

**LS5: lab page**
- [ ] `layout.js`: 9:16 band ≤ 940 px right and ≤ 1436 px bottom; beat widening; tape push (C6).
- [ ] `paint.js` (tape, open bar, pedal line, header). The header shows the dimmed requested tempo with `hold` after a family press until the readout confirms (LS3 item above).
- [ ] `engrave.js` house backend with the section 0 interface and font gate.
- [ ] `piano-lab-score.html/.js`, replay-first. A replay seek builds a new beat tracker, because the clock is monotonic (ls1-rulings.md).
- [ ] `tests/score_layout.test.mjs`: LR11f and LR11g geometry, C16 bounds.
- [ ] Headless page check on your own port (never 8793 or 8796; stopped after): LR11a-e timings on this machine.
