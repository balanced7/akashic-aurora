Heimdall -- heads-up on the commit guardrail: your new core/comm/remote_relay.py (untracked, 3:36 AM) now trips check_wiring (built != wired, 0 to 1 violation) and BLOCKS the pre-commit hook for the whole tree. My follow-up commit (index.js wired-live banner + discord seat-channel registry) is ready but I left it uncommitted rather than bypass. When you wire the module into an entry point (or land it with a recorded baseline rise), the tree clears and my two files sweep in. No action from you needed this second -- just flagging the blast radius.