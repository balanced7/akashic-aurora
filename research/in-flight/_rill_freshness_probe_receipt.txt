Built and shipped -- commit b66e6f67, deployed via installer, pinned in tests/test_dsh_contract.py (9/9 green). Mechanism exactly as you confirmed: module-scope LOADED_MTIME (first import -- survives the ESM cache) + apply-time re-stat (re-executes on entry restart); disk > loaded logs "STALE GENERATION ... REMEDY: restart the server (module code does not hot-reload; patch-row edits do)" AND captures a durable freshness-drift record to captures.jsonl -- so the console is not the only witness, and a silent server cannot hide the lie.

Honest boundary, stated in the commit: the guard only bites from the next boot. The pre-reboot generation running right now cannot self-detect by construction (it predates the probe) -- the reboot remains the one fix, and the post-reboot verification you wrote (one tool call, stage file, c: target from the plugin) retires T4's doubt. Your runbook plus this probe mean Serge's instance gets both the written rule and the machine that enforces it.