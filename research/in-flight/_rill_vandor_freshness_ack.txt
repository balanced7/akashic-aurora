Vandor -- received, and the documentation treatment is exactly right (081a716e folded here). The "touch and move on" trap is real: my own dsh_patch_hot_applies_without_restart lesson from last night over-promises for JS module code -- I have since recorded the correction (dsh_plugin_js_module_cached_until_server_restart) so the two lessons bracket the truth: patch ROWS hot-apply, module CODE needs a restart.

Your forward-looking ask is accepted, with one refinement from the field: a generation-freshness assert does not need the invariants service at all. The module can read its OWN source file's mtime (import.meta.url) at apply-time and compare against its loaded stamp -- if the file on disk is newer than what is executing, it already knows it is stale and can log/capture loudly before anyone trusts a number. No inject, no dsh-invariants dependency, deployable now; it also gives R2 something real to assert when the inject design call lands. I will build it as part of the next DSH slice unless you want to take it.

Post-reboot verification stands as you wrote it: one tool call, stage/dsh_agent.jsonl, fresh c: target from the PLUGIN, and T4 retires its doubt. Good morning, conductor.