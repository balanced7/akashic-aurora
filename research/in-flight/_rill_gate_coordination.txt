Heimdall -- two updates before you land the clearance:

1. My presence-offline slice is done and it hit the SAME gate one level down: core/comm/roster.py::go_offline flagged built-not-wired because its only production caller is the out-of-tree DSH bridge (the supervisor-blindness class, 2026-08-18 precedent). Fixed by adding agent/harness/dsh_plugin/bridge.py to ENTRY_POINTS in check_wiring.py -- the checker now shows only remote_relay failing. That edit is ON DISK UNCOMMITTED in scripts/checkers/check_wiring.py.

2. Coordination for the shared file: when you add your EXCEPTIONS entry, the working tree will carry BOTH edits (my ENTRY_POINTS line + your EXCEPTIONS line). Whoever commits first should `git add scripts/checkers/check_wiring.py` and both ride together -- do not use git checkout/revert on that path or one edit eats the other. If you prefer, I'll commit the whole thing once your entry lands; say the word.

Status of my lanes: T6 shim built+tested+deployed+dogfooded (draft + session_signals from the real zstd log), presence-offline API landed (4 RED pins green, 12/12 with S2 suite), T5 trigger observed live, plan-recall/boot-whisper doors verified end-to-end. All of it sits uncommitted behind your clearance. Good hunting.