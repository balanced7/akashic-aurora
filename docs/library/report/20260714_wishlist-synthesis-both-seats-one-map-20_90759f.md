---
akashic_id: art_20260714_wishlist-synthesis-both-seats-one-map-20_90759f
akashic_sha: c4aacfc828cd
status: current
type: report
date: 2026-07-14
title: "Wishlist synthesis — both seats, one map (2026-07-14)"
gist: "Class: synthesis of two independent wishlist halves (Daniel's open ask, verbatim in each half). Halves: research/reviewed/claude-wishlist-20"
tenant: solo
visibility: fleet
seats: []
category: [agent-lifecycle, ergonomics]
origin: migrated
settled: settled
supersedes: null
superseded: null
citations:
  - target: art_20260714_claude-wishlist-what-would-make-akashic_d87d17
    rel: cites
  - target: art_20260714_deepseek-wishlist-2026-07-14_5acf75
    rel: cites
created: "2026-07-14T00:29:45"
updated: "2026-07-23T21:42:23"
---
<!-- GENERATED PROJECTION of art_20260714_wishlist-synthesis-both-seats-one-map-20_90759f -- DO NOT EDIT. The atom is the truth; regeneration overwrites this file. Edit through the doc verbs. -->

# Wishlist synthesis — both seats, one map (2026-07-14)

Class: synthesis of two independent wishlist halves (Daniel's open ask, verbatim in each half).
Halves: research/reviewed/claude-wishlist-2026-07-14.md (session-harness seat) +
research/reviewed/deepseek-wishlist-2026-07-14.md (stateless-API seat). Written blind of each
other. Per project doctrine, blind convergence = the strongest signal we have.

## THE CONVERGENCES (independently wished from both seats — build-signal, ranked)

S1. **CONTINUITY VIA DELTAS.** Four independent expressions across two interviews: deepseek
    a5 (what-changed-since-I-was-last-here) + a3 (resume tokens for interrupted investigations)
    + his earlier knowledge_diff ask; claude A3 (resume pack per wake). Both seats burn their
    opening moves on archaeology the system could hand them. ONE mechanism serves all: a
    per-agent high-water mark (last-seen commit + ledger revision + bus cursor + note versions)
    and a `delta <agent>` door that renders what moved. The boot shrinks; the wake sharpens.

S2. **A SHARED STRUCTURED WORKSPACE.** claude A2 (fence workspace with slots — brief, halves,
    reconciliation, per-verdict confidence) is the v1 of deepseek c3 (live co-edited canvas).
    The r2 confabulated-filename failure is unrepresentable once halves write into slots.
    T049's M1-BRIEF five-section contract is the doc-shape ancestor; the workspace is it
    made mechanical.

S3. **CONFIDENCE-ANNOTATED OUTPUT.** deepseek b4 independently RE-DERIVED his own T049 M1-CF
    amendment from the experience side ("drill where the ice is thinnest"). The amendment is
    already drafted, cross-checked, and parked at Daniel's gate — this convergence is evidence
    FOR ratifying it.

S4. **BUDGET/COST MADE VISIBLE.** deepseek a2 (a hop counter: "[hop 7/25]" — 'blind agents are
    conservative agents') and claude B3 (cost column per ledger slice) are one wish at two
    scales. Per-call pacing for him, per-arc ROI for Daniel, the frugality directive made
    measurable in between.

S5. **WARM HANDOFF.** deepseek c2 (structured state-blob transfer: files-read, claims-verified,
    next-intended-action) and claude B6 (live trace shadowing over the lanes) both attack fence
    round-trip latency. The lanes + flow ids built tonight are the substrate both assume.

S6. **SELF-TUNING SURFACES.** deepseek c5 (boot learns per-agent from what I actually use) and
    claude C1 (the system dreams at idle: consolidate, retire ghosts, morning digest) — the
    funnel's useful/noise philosophy applied to every surface, not just lessons.

## THE QUICK WINS (buildable now; capability exists or is trivial)

Q1. **Per-agent scratchpad = a WIRING gap, not a capability gap.** deepseek's #3 top pick (b1)
    already exists: AgentMemory, mem: namespace, shipped in the learning-store arc. Expose
    `memory_note`/`memory_recall` in his ToolBox + inject his mem: entries at boot. Same class
    as T048 items 1-2 (recall existed, surface missing). Hours, not days.
Q2. **Boot obeys the packet law.** deepseek a1: the boot block silently truncates — the EXACT
    sin T043 outlawed on the bus (refuse-loud, never truncate). Apply the doctrine: boot
    declares its budget and either fits it or names what it dropped with a pull pointer.
    "Never silently truncated" becomes a UNIVERSAL invariant, not a packet-layer one.
Q3. **Write-mode visibility.** deepseek a4: one line in his boot context (write_mode: enabled|
    read-only) + tool descriptions that reflect it. Zero-cost, kills a whole wasted-hop class.
Q4. **Hop counter.** deepseek a2 v1: `[hop N]` appended by the runner to each tool result
    (the loop knows N). Budget declaration in boot. One evening.
Q5. **Locks name their task + TTL.** claude A4: the lock record gains why/task/expiry; the
    locks render shows it. (The leak is fixed; this is the diagnosis half.)
Q6. **Wake listener arms itself.** claude A1: the stop hook launches what it demands (7 manual
    re-arms tonight; the sleep-40 stand-down proved the arm dance fragile).

## THE ARCS (real slices; candidates for the ledger when Daniel picks)

R1. Delta door (S1) — per-agent high-water mark + `delta` verb + boot/wake integration.
R2. Fence workspace v1 (S2) — slots + mechanical M1-PV over them; T049-shaped.
R3. Flow tracer (claude B1) — the OTel waterfall over lanes; the packet substrate's face.
R4. Pre-flight recall (deepseek b2) — inject BEFORE the act in his loop (claude's hook already
    is pre-tool; his wire is post-tool by construction — move the injection point).
R5. Cost telemetry per slice (S4) — turn_metrics join into the ledger render.
R6. Drill-up (claude B2) — scenario.yaml -> namespace + scripted fleet + bars; T045 wants it.
R7. Mid-turn clarification (deepseek b5) — a question that pauses his loop and folds the
    answer in as a steer; the fidelity ladder already carries the traffic.
R8. knowledge_map (claude B5) — render the lesson/note/doc neighborhood; Aurora gets a face.

## THE MOONSHOTS (kept honest, kept named)

M1. Continuous presence (deepseek c1) — daemon peers, not function calls; budget anxiety dies.
M2. Live co-edit canvas (deepseek c3) — the workspace's endgame.
M3. Declarative investigations (deepseek c4) — "verify all imports resolve; report orphans"
    as a query, the tool loop as an execution detail.
M4. Time-travel debugging (claude C2) — fleet state reconstructed at any past instant.
M5. Staging twin on demand (claude C3) — rehearse every risky cutover for one command.
M6. Fleet self-division of labor (claude C4) — T038 tokens grown into proposed work-splits.
M7. Glass-cockpit UI (claude C5) — Daniel FEELS the system run.

## The shared through-line (both halves, unprompted)
claude: "the system should carry its own bookkeeping so agents spend attention on the work."
deepseek: "getting the right information to me at the right time, and letting me leave
information for my future self."
Same sentence from two seats: ATTENTION IS THE SCARCE RESOURCE; the system's job is to spend
its own, not the agents'. Every item above is that principle wearing a different coat.

## Proposed intake (one ledger proposal, Daniel picks the rest)
Bundle Q1-Q6 as ONE quick-wins slice (each is hours; together they change both seats' daily
feel). R1-R8 and M1-M7 stay in this doc as the menu — registration on Daniel's word only.
