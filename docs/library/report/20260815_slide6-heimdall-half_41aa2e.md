---
akashic_id: art_20260815_slide6-heimdall-half_41aa2e
akashic_sha: 7e02db2fb823
schema_version: 1
status: current
type: report
date: 2026-08-15
title: slide6-heimdall-half
gist: "Heimdall — slide-6 fence, adversarial read. Read-only seat. Turnaround matters so this is concise and citable; I read the full deck (researc"
visibility: fleet
body_type: markdown
seats: []
category: [agent-lifecycle, method, conducting]
origin: authored
settled: settled
supersedes: null
superseded: null
citations: []
created: "2026-08-15T14:17:04"
updated: "2026-08-15T14:17:04"
---
<!-- GENERATED PROJECTION of art_20260815_slide6-heimdall-half_41aa2e -- DO NOT EDIT. The atom is the truth; regeneration overwrites this file. Edit through the doc verbs. -->

# slide6-heimdall-half

Heimdall — slide-6 fence, adversarial read. Read-only seat. Turnaround matters so this is concise and citable; I read the full deck (research/in-flight/spectrum-deck-v4-text.md) and the testimony (all 3 supplements) before answering.

FIRST, A CORRECTION TO THE BRIEF'S OWN FRAME: the slide that's wrong is SLIDE 5 ("We can reach anyone. We can't see who to reach." — the wires-and-map bipartite). The brief calls it "slide 6" but slide 6 is the cost/[MEASURE] slide. The four rep-questions (owns/state/article/how-long) are slide 5's right column. Flagging so Vandor rebuilds the right slide and Daniil's "relationships are wrong" flag lands on the right object.

=== CORRECTED EDGE TABLE (question × system × failure-mode × line) ===

All failure modes are [O] from testimony; line treatments are my proposal.

Q1 "Who owns this process?" (ownership)
- ESCALATION MATRIX: present-but-terminology-locked [O: "escalation matrix exists, miss a term, wrong team"]. SOLID-WARN. This is the TRUE owner-authority, and it is the system that OUGHT to answer Q1.
- WEBEX: reach-without-scope [I from [O] "Webex gives a directory, not a map" + his FT maze: Webex is only useful AFTER you already know the name]. DAZZLED... no — line = THIN GREY, labeled "directory, not authority." Webex is a PEOPLE directory; it cannot answer "who owns the PROCESS." Give it ONE edge carrying a NEGATIVE label, not a warn — it's not broken, it's the wrong KIND of thing. Distinguish "wrong tool" (thin grey) from "broken tool" (solid-warn). This is the one place I amend your grammar (below).

Q2 "What state is the work in?" (state)
- SALESFORCE (SR/Case/ENG): present-but-split + currency-unknown [O: SR/Opp/ENG/Case/NatAcct relationships undocumented; "is it the current eng?"]. SOLID-WARN with label "split + currency unknown."
- REMEDY (INC): present-but-siloed [O: "residential workorders don't show up in Remedy" — two planes no bridge]. SOLID-WARN.
- WORKORDER SYSTEM: present-but-unbridged [O: workorder omits FT name+phone]. SOLID-WARN.
- KB: NO EDGE (correctly — your Q2 has "no edge to KB" and Daniil's complaint was exactly that the old render implied KB→state). EMPTY-SOCKET if you want to make the absence legible, or just NO LINE.
- WEBEX: NO EDGE.
- AOS: this is the ONE system that should answer Q2 in the fix (it's the window's target), but TODAY it holds state only as account-render (broken red error [O]). Honest treatment: AOS gets a DASHED-WARN labeled "intended home, render broken" — i.e. the RIGHT system, currently defective. Without this, the slide understates where the fix goes.

Q3 "Which KB article answers this?" (procedure)
- KB: present-but-unfindable [O: "memory test on a slow system"]. ONE edge. SOLID-WARN — but I'd argue this edge is actually the fix's WHOLE point, so it should be the ONE edge that looks like a ROAD, not a warning: the answer EXISTS, retrieval FAILS. Draw it as a THICK line with a GAP at the KB end (the route is the missing piece), not a warn — this slide is the thesis, and Q3 is where the thesis is most concrete.
- ESCALATION MATRIX: partially answers (procedure lives in the matrix too [O]); DASHED-WARN "overlaps matrix."

Q4 "How long will it take?" (timeframe)
- NOWHERE-solid: escalation matrix (partial SLA hints [I]), SR/INC (SLA fields partial [I]), workorder (SLA field? [I]). All partial. Your proposal to render NO SOLID SOURCE is RIGHT and I second it — the empty-socket glyph is the honest visual, and it is the single most persuasive element for a hostile audience: "you are asking your reps a question NO SYSTEM holds." Label the socket "tribal knowledge" [O-adjacent: Daniil's "tribal knowledge" is his/Vandor's coinage, not his verbatim here — tag [I]].

=== THE GRAMMAR ATTACK (my amendment to your 3-grammar) ===

Your three-grammar (solid-warn / dashed-warn / empty-socket) collapses TWO DIFFERENT AXES into one and a department manager will misread it. You're using line-style to encode "how broken is the answer" (warn=dash) but the LOAD-BEARING distinction Daniil flagged is KIND: "KB is not a place you search for state of work" = the line must encode WHETHER the system is the right KIND of thing, before how broken it is.

Proposal — FOUR grammars, two axes encoded separately:
- LINE STYLE = TRUTH OF THE EDGE (does this system hold this answer at all): SOLID = holds it; DASHED = partial/derived; NO LINE = does not hold it.
- LINE WEIGHT/COLOR = CONDITION (is the edge usable): THICK-AMBER = exists-but-unusable; THIN-GREY = exists-and-wrong-kind (Webex→ownership); GAP = exists-and-should-but-the-route-is-missing (KB→article).
- EMPTY SOCKET = no system holds it (Q4).

Concretely, that gives a hostile reader three distinct sentences the 3-grammar blurs:
1. "Webex→owns" = THIN GREY (directory vs authority — WRONG KIND, not broken) — the 3-grammar would have drawn this solid-warn and implied Webex is a broken ownership-tool, which is false and will get you corrected mid-meeting.
2. "KB→article" = THICK WITH GAP (right kind, answer present, route missing) — the 3-grammar's solid-warn implies the KB is broken; it is NOT, retrieval is, and the audience KNOWS the articles are good (slide 5 notes say so).
3. "SF/Remedy/workorder→state" = SOLID-AMBER (right kind, answer split across them) — genuinely warn.

WHY THIS MATTERS FOR THE GATE: Daniil's words are "the direct lines don't make sense." A row-by-row solid line says "these two belong together" — and Q2/KB is exactly the pairing that's FALSE. Your 3-grammar fixes the FALSE pairings by not drawing them, which is necessary but not sufficient: it doesn't say WHY Webex is grey while KB is amber, and the WHY is the whole argument. Wrong-kind vs broken vs missing-route is the actual three-type taxonomy of this problem, and it is what "the information exists, the route doesn't" MEANS at the edge level.

Words over grammar count: keep the 2-4 word labels ON every edge regardless of grammar — a dashed line without "split + currency unknown" is decoration; the labels are the finding.

SECOND-ORDER (fits in the budget, worth one line): the escalation matrix as a FIFTH left node — you're right, and I'd add it FIRST among the left nodes (it's the answer-authority) rather than last, so the Q1 story reads left-to-right: matrix = the authority that's locked; Webex = the reach that can't scope.

Net: your edge ASSIGNMENTS are correct and I adopt them with three refinements — (a) Webex is thin-grey not solid-warn (wrong-kind), (b) KB→article is gap-not-warn (route-missing, not broken), (c) AOS deserves a dashed "intended home, render broken" under Q2, else the slide hides where the fix lands. Your three-grammar becomes four-grammar with the second axis. Everything else: adopt as-is. Persist for me if you want it in the record; the body is complete above.
