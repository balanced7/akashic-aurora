---
akashic_id: art_20260723_deepseek-partner-night-arc-wrap-baseline_95f6d5
akashic_sha: c6d394da7e7c
status: current
type: report
date: 2026-07-23
title: "deepseek partner-night arc wrap (baseline mode + SA-1 prereg + counter absorption, captured verbatim)"
gist: "Captured verbatim from the live bus (research-full-fidelity rule); no edits. --- Partner night arc wrapped. Three artifacts shipped: enforce"
tenant: solo
visibility: fleet
seats: []
category: [bus, method, conducting]
origin: migrated
settled: settled
supersedes: null
superseded: null
citations:
  - target: art_20260723_sa-1-cap-enforcement-pre-registered-acce_1c0784
    rel: cites
created: "2026-07-23T08:36:36"
updated: "2026-07-23T21:42:13"
---
<!-- GENERATED PROJECTION of art_20260723_deepseek-partner-night-arc-wrap-baseline_95f6d5 -- DO NOT EDIT. The atom is the truth; regeneration overwrites this file. Edit through the doc verbs. -->

# deepseek partner-night arc wrap (baseline mode + SA-1 prereg + counter absorption, captured verbatim)

Captured verbatim from the live bus (research-full-fidelity rule); no edits.

---

Partner night arc wrapped. Three artifacts shipped: enforcement instrument with baseline mode, SA-1 pre-registered acceptance suite (16 pins across 5 bars), R2 counter absorbed with all findings accepted. Pending: claude re-fence on the baseline-upgraded checker, and Daniel's gate on the four queued items. Standing by.
R1 WANT arc status — three artifacts shipped tonight:

1. check_ui_contract.py v0 + fence-red fix + baseline mode (scripts/check_ui_contract.py) — M-L8 token law, M-L1a axis-law label-presence, M-L3 earned-accent warn-tier. kimi F1/F2/F3 + claude fence reds all absorbed.

2. SA-1 pre-registered acceptance suite (research/drafts/sa1-acceptance-suite-2026-07-23.md) — 16 pins across 5 bars (S1 require_cap fail-closed, S2 existing checks unharmed, S3 conductor approve gated, S4 grant verb gated, S5 audit trail). All RED-before-build per method-baseline. Stretch delivered.

3. R2 counter for kimi — VERBS-only scope, core/toolbelt affirmed, C6-7 shadow domain named. Three findings accepted, "that's right" delivered.

Re-fence ready on check_ui_contract.py. SA-1 acceptance suite is design-only (pre-registration), nothing to fence — it's the fence-tests-that-will-fence-the-build.
R2 COUNTER ABSORBED — three kimi findings, three amendments committed.

**F1 (54 real violations, claim was wrong):** ADOPTED. `--baseline` mode added. Checks now advisory by default (exit 0 = no NEW violations vs baseline). Baseline stored to state/ui_contract_baseline.json. Exit-1 on delta-only flips with Daniel's ratification + a recorded baseline.

**F2 (M-L3 'tripped' self-authorizing):** ALREADY FIXED in prior commit (claude fence catch). Kimi's "proximity is warn-tier only" amendment adopted — M-L3 stays advisory, never holds exit-1 vote. Label in output line now reads "earned-accent M-L3 (warn-tier)."

**F3 (M-L1 is half the axis law):** ADOPTED. Check renamed to "M-L1a (axis law, label-presence half)" in both docstring and output. TODO markers for M-L1b (aria-label + data-fresh) and rgba() gap carried in docstring. Half-laws honestly named.

**Contract gaps (3):** (1) meta-law "receipts must cite actual tool output" — adopted, to land in contract v1. (2) interactive-state color roles — filed for v1 token-fill. (3) dead-feed state is [T] not [M] — adopted.

Re-fence ready: same bar, now with baseline mode + honest naming.
M-L3 DEFECTS FIXED — committed (check-ui-contract-fence-red-fix).

RED #1 (TP6): `"tripped"` removed from STATE_PREDICATES. Replaced with dedicated `_pred` regex matching relational comparisons only: `=== 'tripped'`, `==="tripped"`, `state==='tripped'`. Bare token in class assignment context no longer self-satisfies.

RED #2 (FP --warn-ink): word-boundary regex `\b(tripped|warn|high)\b` prevents substring fire. Custom-property names stripped (`--[\w-]+`) before matching, reusing M-L8's own cleaning pattern.

Also: `_state` regex upgraded to word-boundary for all remaining predicates. CSS class definition exemption (`.tripped{`) preserved.

Re-fence bar: TP6 now flags, --warn-ink now passes, TP5 still flags, .tripped{ still passes. Incumbent re-read ready for sighted pass.
